# ROBBED_ — Decision Register

**Status:** Consolidated 2026-07-09 by robbed-architect. This register indexes every open item / flagged interpretation raised by the four service design docs and records its disposition. The authoritative record of *decided* items is `launchpad-spec.md` §12; the authoritative list of *open* items is §13. This file is the cross-reference — if it ever disagrees with the spec, the spec wins and this file gets fixed.

Conventions:
- **DECIDED** — ratified by the architect, recorded in spec §12 (entry number given), propagated into the affected service docs.
- **OPEN** — genuinely undecided; listed in spec §13 with an owner and a latest-decision point. Implementing agents must not self-resolve these.
- **DELEGATED** — an implementation choice inside one service's lane; the owning agent decides and documents it in their service doc, no spec entry needed.

---

## 1. Contracts (`docs/services/contracts.md` §8, O-1…O-11)

| ID | Item | Disposition | Rationale / owner |
|---|---|---|---|
| O-1 | `GRADUATION_ETH` gross vs net of trade fee | **DECIDED — §12.11.** Net-of-fee real curve reserves | Net reserves are what actually funds the LP; the threshold must describe LP funding, not gross inflow. M0 sizes the mcap target from net reserves + LP tranche at graduation price. Blocks constants freeze (M0↔M1 handoff) |
| O-2 | Sells during the `ReadyToGraduate` window | **DECIDED — §12.12.** Lock both directions | §6.5 "sells always open" is a *no-pause-authority* guarantee, not a liveness guarantee for a deterministic, permissionlessly-exitable, typically same-block protocol state. Lock-both avoids threshold-oscillation games and matches pump.fun UX. Spec §6.2/§6.5 amended. Resolves web item 5 identically |
| O-3 | "Residual dust burned" — WETH leg included? | **DECIDED — §12.13.** Token dust → `0xdEaD`; WETH dust → treasury | Burning WETH destroys real ETH value for no trust benefit; "dust burned" reads as the token leg. Spec §6.3 step 5 amended |
| O-4 | V3 Factory / NPM addresses on 4663 | **RESOLVED — §12.28 (2026-07-09).** Uniswap V3 confirmed on 4663; Factory/NPM/SwapRouter02/QuoterV2 recorded in constants + CLAUDE.md; runtime assertions mandatory | Also closes indexer OI-13, web item 11 (V3 leg) and E-1 |
| O-5 | Compiler pin 0.8.35 vs Blockscout verifier | **OPEN — §13.** Owner: robbed-contracts verifies before first M1 deploy; architect records the final pin in §12 | Throwaway-contract verification test; if 0.8.35 fails, nearest supported exact pin chosen explicitly, never silently |
| O-6 | Safe deployment on 4663 + signer set | **OPEN — §13.** Owner: architect + ops. Canonical-Safe check at M1; signer set (M-of-N) blocks deploy step 7 only | Bespoke multisig remains forbidden (§6.6) |
| O-7 | Anti-sniper mechanism + values | **Mechanism DECIDED — §12.18.** **Values DECIDED — §12.32 (2026-07-10):** `windowSeconds = 8`, `maxEarlyBuyWei = 0.201922 ETH` (2.5% of `GRADUATION_ETH`); M0 default, tunable in the capped beta | Values in `constants.json`, never inlined |
| O-8 | Arb-back params (`TOLERANCE_TICKS`, `MAX_ARB_ITERATIONS`, `MIGRATION_SLIPPAGE_BPS`) + inventory budget rule | **Token-leg budget rule DECIDED** (§13 note). **Param values DECIDED — §12.33 (2026-07-10):** `100 / 8 / 100`; M0 default, tunable in beta. **WETH-leg budget *definition* still an M1 OBLIGATION on robbed-contracts** (undefined on the WETH side since the mint takes all WETH — threat-model §8.1; define + prove in gate-2 invariant 6) | Tolerance derived from acceptable value-skew at mint |
| O-9 | Graduation caller reward value | **DECIDED — §12.34 (2026-07-10).** `callerRewardWei = 0.002822 ETH` (~$5 @snapshot), immutable ceiling 5×; re-validate ≥10× `graduate()` gas at M1; M0 default, tunable in beta | Risk-free number, but market-dependent — never inlined |
| O-10 | Beta cap values (gate 7) | **OPEN — §13.** Owner: robbed-security + architect before mainnet beta deploy | Ops/risk numbers, not code; testnet placeholders fine |
| O-11 | Initial-buy size in `TokenCreated`? | **DECIDED — §12.15.** No — derived from the first `Trade` in the same tx. `TokenCreated` instead carries `metadataUri` (see OI-1) | Keeps the event minimal while satisfying the indexer |

## 2. Indexer (`docs/services/indexer.md` §10, OI-1…OI-13)

| ID | Item | Disposition | Rationale / owner |
|---|---|---|---|
| OI-1 | Exact event ABI shapes (post-trade reserves? `metadataUri`?) | **DECIDED — §12.15.** Canonical shapes fixed: `TokenCreated(token, curve, creator, name, symbol, metadataHash, metadataUri, pool)`; `Trade(trader, isBuy, ethGross, tokenAmount, fee, vE, vT, realEth — post-trade)`; `Graduated` per contracts.md §2.5. contracts.md is authoritative for ABIs; indexer.md mirrors | Indexer must need zero hot-path RPC reads (<500ms budget, §8). Contracts doc updated (metadataUri added to factory/Router signatures + event); indexer doc updated to canonical shapes |
| OI-2 | `Trade.ethAmount` gross vs net | **DECIDED — §12.15.** Gross, with `fee` as a separate field; net = gross − fee | One representation, both values derivable; matches contracts' emission point |
| OI-3 | LaunchToken `Transfer` as 6th event family | **DECIDED — §12.16.** Adopted; sole source of holder-balance truth | Only exact way to satisfy §5.2 holder distribution + §5.4 portfolio readiness. Spec §8 amended to six families |
| OI-4 | Pre-graduation V3 pool activity | **DECIDED — §12.16.** Not indexed; curve is sole venue until `Graduated`; gate-7 alerting covers pool griefing | Anomaly-swap indexing adds cost for data the product never shows |
| OI-5 | V3 per-EOA cost basis approximate (router recipients) | **DECIDED — §12.16.** Accepted; balances exact via Transfer, v3 cost basis best-effort until Phase-2 portfolio spec | Exactness where it matters (balances); precision debt documented |
| OI-6 | ETH/USD source on 4663 | **CLOSED — §12.51 (2026-07-11; disposition §12.48a).** Chainlink ETH/USD feed CONFIRMED on 4663: proxy `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9` (aggregator `0x6091E64eb7138EEF066a80FD3A0d7427B91f2721`), live-verified via `eth_call` (§11 below). Chainlink branch selected; **startup assertions mandatory** (`description()=="ETH / USD"`, `decimals()==8`, fail-closed) when the indexer.md §3.9 poller lands; 4663-mainnet-only — LOCAL/TESTNET use the HTTP fallback | Shared with api OI-A10; §13 OI-6 closed |
| OI-7 | Candle interval set | **DECIDED — §12.17.** `1s, 15s, 1m, 5m, 15m, 1h` | Spec gave only endpoints; set covers all chart zooms; derived data, cheap to change later |
| OI-8 | `safe`/`finalized` RPC tag support | **CLOSED — verified 2026-07-11 (disposition §12.48b, annotated).** Both tags SUPPORTED on the official 4663 RPC (verbatim probe evidence §11 below); the M2-6 tracker's tag path runs. **M2-3b (L1-watermark reader) NOT funded** — dormant pre-sanctioned fallback only; plan item flipped N/A per its own conditional | Fallback documented in indexer.md §5.1; graceful no-op kept as resilience; §13 OI-8 closed |
| OI-9 | Confirmation propagation: watermark broadcast vs per-row | **DECIDED — §12.20.** O(1) watermark broadcast on `global:confirmations`; clients upgrade locally; REST serves the materialized column | Per-row fanout is O(rows) for zero added information |
| OI-10 | Server-side `imageHash` verification | **DECIDED — §12.23.** Deferred post-v1; hash carried in metadata JSON, client-verifiable in Trust panel | Metadata-hash verification (the chain commitment) is unaffected |
| OI-11 | External `confirmation_state` UPDATE vs sidecar table | **CLOSED — version gate resolved 2026-07-11 (disposition §12.48c, annotated).** Direct in-column UPDATE **NOT tolerated by ponder 0.16.8** for hot rows (cache re-flushes all columns from cached copies → external upgrades on `tokens` silently reverted, §5.1 monotonicity violated; evidence §11 below). **Sidecar `event_confirmations` is MANDATORY**, not merely default; the current direct-UPDATE code in `apps/indexer` is a recorded mismatch under rework (robbed-indexer, in flight) | Evidence §11; indexer.md §7.3/§10; §13 OI-11 annotated |
| OI-12 | WS replay buffer | **DECIDED — §12.23.** None in v1; REST-heal on reconnect/seq-gap | Replay buffers add fanout-tier state for a problem REST already solves |
| OI-13 | V3 Factory / NPM addresses | **RESOLVED — §12.28.** = contracts O-4 (addresses recorded) | Startup assertions still mandatory |

## 3. API (`docs/services/api.md` §9, OI-A1…OI-A10)

| ID | Item | Disposition | Rationale / owner |
|---|---|---|---|
| OI-A1 | "R2 presigned uploads": browser presign vs API-mediated | **DECIDED — §12.19.** API-mediated; presign exists only on the API→R2 leg. Spec §5.3/§8 amended; web.md corrected (`POST /v1/uploads/image`, no `/uploads/presign`) | §8.4's sniff + re-encode must run before any byte reaches public storage; browser presign would need a quarantine bucket for strictly more complexity |
| OI-A2 | Search ranking (ticker ×1.2, volume tiebreak, floor 0.25) | **DECIDED — §12.22.** As proposed, explicitly *tunable config* | Pure product tuning; ratified so M2 isn't blocked, revisit with beta data |
| OI-A3 | `trending` + King-of-the-Hill formulas | **DECIDED — §12.22.** KotH = `progress × ln(1+vol24h)`; trending = `vol24h × e^(−age/24h)`, τ=24h; tunable config | Spec gave intent, not math; formulas are indexer/API-owned, frontend renders only (resolves web item 3) |
| OI-A4 | Moderation queue tech (BullMQ vs Redis list worker) | **DELEGATED.** robbed-indexer's implementation choice; recommendation (minimal Redis worker) stands | Inside one service's lane; no cross-service or spec surface |
| OI-A5 | `pending_review` visibility + WS ticker moderation | **DECIDED — §12.21.** `pending_review` stays listed; WS `global:launches` unmoderated in v1 (hide propagates via REST) | Vendor outage must not blank the site; moderation-aware fanout would need hot-path DB reads (violates latency rule) |
| OI-A6 | Hidden token on direct fetch: 404 vs flag | **DECIDED — §12.21.** Visible-with-hidden-flag | Hiding is listing-only (§8.4); the token exists on-chain regardless |
| OI-A7 | Moderation vendor + mandated-reporting flow | **OPEN — §13.** Owner: architect + ops. M2 ships stub vendors behind a boot guard | Legal dimension is part of vendor selection |
| OI-A8 | Admin SIWE allowlist membership | **OPEN — §13.** Follows the Safe signer-set decision (O-6) | Config-driven; populate when decided |
| OI-A9 | Impersonation watchlist source + cadence | **DECIDED — §12.23.** Curated, source-cited, dated data file; refreshed ≥ monthly | §2 discipline applied to list data; never hardcoded metrics |
| OI-A10 | ETH/USD endpoint source | **Aligned** (single source = `eth_usd_snapshots`); underlying feed **DECIDED — §12.48a** (= OI-6, config-driven; Chainlink-existence env-gated) | One producer (indexer poller), one reader (API) |

## 4. Web (`docs/services/web.md` §9, items 1–11)

| # | Item | Disposition | Rationale / owner |
|---|---|---|---|
| 1 | WS + REST contract ratification | **DECIDED (ratified interfaces).** Canonical = indexer.md §8.1/§8.2 channels/messages and api.md §3 routes; web.md corrected (channel taxonomy, message type names, `/v1/` paths). One gap web exposed was real: `GET /v1/trades/:txHash` did not exist — **added to api.md §3.4** for optimistic reconciliation | The owning service's doc is authoritative for its interface; web's names were proposals |
| 2 | ETH/USD endpoint existence | **RESOLVED (no decision needed).** `GET /v1/eth-usd → { price, source, asOf }` already in api.md §3.5; web.md corrected (`fetchedAt` → `asOf`) | Was a doc-sync gap, not a design gap |
| 3 | Ranking formulas | **DECIDED — §12.22** (= OI-A3) | Frontend renders, never computes |
| 4 | `quoteBuy`/`quoteSell` views on chain | **RESOLVED (no decision needed).** They exist on Router + Curve (contracts.md §2.3/§2.4), incl. graduation-clamp outputs; web.md updated to prefer them | Was a doc-sync gap |
| 5 | Graduating-interstitial sells | **DECIDED — §12.12** (= O-2). Both directions locked; two-sided disabled interstitial is the ratified UX; copy must not say "paused" | Same ruling as contracts O-2 |
| 6 | WalletConnect projectId + Robinhood Wallet on 4663 | **OPEN — §13.** Owner: robbed-frontend verifies at M3 start; architect records | Ops/runtime check |
| 7 | `next/og`/satori under Bun; Multicall3 on 4663 | **OPEN — §13.** Owner: robbed-frontend, M3 start; fallbacks documented in web.md §6 | Runtime checks with in-doc fallbacks |
| 8 | LP sentence wording divergence (§5.2 vs canon) | **DECIDED — §12.14.** Canonical: **"LP principal permanently locked; trading fees claimable by treasury."** Spec §5.2 amended to the canonical sentence; longer "immutable vault" phrasing allowed only as explanatory sub-copy, never as the constant | One string constant, zero drift; grep-enforceable |
| 9 | Dark-only v1 | **DECIDED — §12.23.** Dark-only, no toggle | "Dark-first, dense, fast" + zero theme-flash; a toggle adds surface for no v1 value |
| 10 | Large-value disclosure threshold (ETH notional) | **DECIDED — §12.47 (2026-07-10, M3-10):** `largeValueEthThreshold = 1.0 ETH` (config default, retunable in beta; ≈ 12.38% of `GRADUATION_ETH`, ≈ $1,771.51 @ §12.35 snapshot). Config keys `NEXT_PUBLIC_LARGE_VALUE_ETH_THRESHOLD` (web) + API mirror; wiring is a robbed-frontend + robbed-indexer follow-up, never a literal | §2.1 disclosure requirement; consumes the §12.35 M0 input |
| 11 | Upstream §13 items (V3 addresses, brand, ToS, M0 constants) | **OPEN — §13** (pre-existing) | Tracked in spec §13 with blockers noted |
| 12 | UI component library (design not ready; must be trivially re-themeable) | **DECIDED — §12.24 (2026-07-09).** **shadcn/ui**: Tailwind-native (existing §9 stack), components vendored into `apps/web/components/ui/` (owned, no runtime component dep), theming exclusively via CSS custom-property design tokens — future design = token-value swap + selective restyles; no bespoke styling may bypass the token system (lint-enforced). Alternatives rejected: raw Radix (more assembly, same result), HeroUI (runtime dep), Mantine (parallel styling system vs Tailwind) | web.md §7 amended; implementation-plan M3-2 |

---

## 5. Ratification pass 2026-07-09 (findings docket `docs/review/findings-2026-07-09.md`)

Dispositions from the closing verification docket. **DECIDED** rows land in spec §12; **OBLIGATION** rows are implementation decisions delegated to the owning agent per the decide-it-yourself protocol; **DEFERRED** rows are roadmap.

| Finding | Disposition | Where |
|---|---|---|
| C-1 / UM-1 treasury-pointer sell-freeze | **DECIDED — §12.25.** Pull-payment fee escrow: fees accrue in-contract, permissionless non-phase-gated `sweepFees()`; no trade path calls treasury. Solvency/exact-fee invariants updated; reverting-treasury sell test added to gate-2 pause matrix | spec §12.25; contracts.md §2.3/§3.2–3.4/§5.7/§6; threat-model §0/§4.2/§4.5/§8/R5; CLAUDE.md |
| U-3 graduation fee | **DECIDED — §12.26.** Small flat cost-based (≈ migration gas + thin margin), not %-of-raise; exact number at M1, formula/placeholder in M0 | spec §6.4/§12.26; contracts.md §4 |
| U-3 anti-sniper | **DECIDED — §12.27 + DEFERRED.** v1 ships §12.18 fixed timestamp window; decaying+size-based redesign deferred to pre-caps-lift | spec §6.5/§11/§12.27 |
| U-4 / O-4 / E-1 V3 on 4663 | **DECIDED — §12.28.** V3 confirmed; Factory/NPM/SwapRouter02/QuoterV2 recorded; runtime assertions mandatory; trade fee kept 1%. Closes O-4/OI-13/web-11(V3)/E-1 | spec §12.28/§13; CLAUDE.md; contracts.md §4/§7.2 |
| U-2 workspace | **DECIDED — §12.29.** pnpm workspaces; Bun stays runtime/test runner. Conversion by robbed-shared | spec §12.29; development-flow §4 |
| X-1 name limit | **DECIDED — §12.30.** 32 bytes on-chain = source of truth; API/shared/OpenAPI align to byte limits | spec §12.30; api.md §3.2/§5 |
| X-13 metadata version | **DECIDED — §12.31.** `version:1` frozen inside hash preimage | spec §12.31; api.md §3.2/§5 |
| X-2 V3 price orientation | **FIXED (doc).** Invert when token is token1 (`token > WETH`), not token0 | indexer.md §3.2 |
| X-3 Trade.trader plumbing | **FIXED.** `trader` plumbed into curve `buy`/`sell`; Router forwards `msg.sender` | contracts.md §2.3/§2.4/§3.2–3.3 |
| X-4 real_token_reserves + balance ownership | **FIXED.** `real_token_reserves` maintained incrementally from `Trade.tokenAmount±`; `balance`/`holder_count` written only by Transfer handler | indexer.md §3.2/§3.4/§3.6 |
| X-5 Transfer idempotency | **FIXED.** Per-event `transfers` table keyed `(tx,log)` anchors balance-delta idempotency | indexer.md §3.6/§7.3 |
| X-6 fee_collected WS msg | **FIXED.** Message schema added; robbed-shared adds to `ws-messages.ts` | indexer.md §8.2; api.md §5 |
| X-7 OG candles param | **FIXED.** OG uses `from`/`to` (not `limit`) | web.md §6 |
| X-8 architecture routing | **FIXED.** `graduate` → BondingCurve, `collect` → LPFeeVault (not Router) | architecture.md §1 |
| X-9 admin re-verify | **FIXED.** API publishes `control:reverify` on Redis; indexer is sole writer of `metadata_verifications` | api.md §3.6; indexer.md §6.2 |
| X-10 TokenCreated seam | **FIXED.** API moderation worker subscribes to `global:launches`; writes API-owned `moderation_status` | api.md §4.4 |
| X-11 confirmation tier | **FIXED.** Watermark broadcasts (§12.20), not per-event messages | web.md §4.1 |
| X-12 graduation recordEthDelta | **FIXED.** `graduate()` registers `recordEthDelta(−realEth)` (non-reverting) | contracts.md §3.4 |
| X-13 TokenDetail.creator | **RATIFIED.** Card = address, Detail = `{address, tokensCreated}` by design | api.md §5 |
| UM-2 grief-lock | **OBLIGATION (M1, robbed-contracts) + gate-6 cost proof.** Bounded-retry + escape-hatch design; escape hatch mandatory if griefing not proven uneconomic | threat-model §8/§8.1 |
| O-8 arb-back WETH-leg budget (T-2) | **OBLIGATION (M1, robbed-contracts).** Define + prove the WETH-side budget rule (undefined by "inventory above mint requirement" since mint takes all WETH) | threat-model §8.1; contracts.md §3.4 |
| Cross-entrypoint reentrancy (T-3) | **OBLIGATION (M1, robbed-contracts).** Trade fns + `graduate()` + `sweepFees()` `nonReentrant` + CEI; explicit cross-entrypoint unit | threat-model §8.1; contracts.md §5.4 |
| UM-4 single-RPC | **ACCEPTED v1 + gate-10 disclosure;** second RPC = pre-caps-lift enhancement | threat-model §8.1; spec §11 |
| UM-5 stored-link XSS | **OBLIGATION (M3, frontend+api).** https-only scheme allowlist + `rel=noopener` + CSP + e2e XSS assertion | threat-model §8.1; web.md §5/§8.2; api.md §6.4 |
| UM-9 CREATE2 squatting | **OBLIGATION (M1, robbed-contracts, low).** Accept + document, or mix salt nonce if gate-6 shows cheap front-run | threat-model §8.1 |
| T-5 coverage map | **FIXED.** §7 completion addendum maps the previously-omitted §4 threats | threat-model §7.1 |
| Plan runnability (P-1…P-9, E-3, E-4, Bucket 6) | **FIXED.** P0-6 split (local commit vs USER push), M0 `derive`/`out/constants.json`, compose hoisted to M2, addresses codegen in M1-14, search in M2-9, G-5b assertable-layers+waivers, verify-line rot, owners ratified, Ponder same-tx spike (M2-0b), OI-8 fallback (M2-3b), ops docs pre-M4 | implementation-plan; development-flow §4 |

### M0-3 / M0-4 pass (2026-07-10) — M0-owned §13 numbers recorded

robbed-contracts computed the values into `tools/m0/out/constants.json` (M0-1/M0-2); robbed-architect records the dispositions here. **Each value is an M0 default, tunable in the capped beta (gate 7) — not an immutable-at-deploy v1 product commitment.** These are engineering/economic defaults the user was shown and did not override (anti-sniper "ship simple now", §12.27); recorded, not re-decided.

| Finding | Disposition | Where |
|---|---|---|
| O-7 anti-sniper values (M0-3) | **DECIDED — §12.32 (2026-07-10).** `windowSeconds = 8`, `maxEarlyBuyWei = 0.201922 ETH` (exactly 2.5% of `GRADUATION_ETH`). M0 default, tunable in beta; multi-wallet bypass acknowledged; decaying redesign stays deferred (§12.27) | spec §12.32; §13 (O-7 values closed); `constants.json` `antiSniper.*` |
| O-8 arb-back param values (M0-3) | **DECIDED — §12.33 (2026-07-10).** `toleranceTicks = 100`, `maxArbIterations = 8`, `migrationSlippageBps = 100`. M0 default, tunable in beta. **WETH-leg budget *definition* stays a robbed-contracts M1 obligation** (threat-model §8.1, gate-2 invariant 6) — not defined here | spec §12.33; §13 (O-8 param-values closed, WETH-leg budget → M1); threat-model §8.1; `constants.json` `v3.*` |
| O-9 graduation caller reward (M0-3) | **DECIDED — §12.34 (2026-07-10).** `callerRewardWei = 0.002822 ETH` (~$5 @snapshot), immutable ceiling `maxCallerRewardWei` = 5×. Re-validate **≥10× `graduate()` gas** at M1. M0 default, tunable in beta | spec §12.34; §13 (O-9 closed); `constants.json` `fees.callerRewardWei`/`maxCallerRewardWei` |
| web-10 large-value threshold — M0 input (M0-3) | **INPUT FURNISHED — §12.35 (2026-07-10).** M0 supplies the sourced ETH/USD basis (`ethUsdSnapshot`, coingecko + timestamp); the **threshold config value itself stays the architect's decision before M3 exit** (M3-10). web-10 **remains OPEN** in §13 | spec §12.35; §13 (web-10 still open); `constants.json` `ethUsdSnapshot` |
| Frontend framework pin: Next.js 16 + React 19 | **DECIDED — §12.37 (2026-07-10).** User directive: frontend targets **Next.js 16 + React 19 only, exact majors, no ranges**. Docs-first via context7 (`/vercel/next.js`, `/react/react`) + official upgrade guide (`version: 16.2.10`, 2026-05-13): NJS16 released, App Router uses/requires React 19.2, Node ≥ 20.9 / TS ≥ 5.1 (satisfied). Pins to record at M3 scaffold: `next@16` / `react@19` / `react-dom@19` / `@types/react@19` / `@types/react-dom@19`. **No peer bump mandated** (wagmi v2 / viem / TanStack Query v5 support React 19; lightweight-charts + satori are React-agnostic; RainbowKit must be on a React-19 line) — authoritative peer confirmation is robbed-frontend's M3-start web-6/7 check. **Doc text swept; actual dependency pin lands in `apps/web/package.json` at M3, owned by robbed-shared (catalogs, §12.29), not the architect.** | spec §12.37 + §9; CLAUDE.md Stack; architecture.md; web.md; plans/web-plan.md; decisions §5. Peer-version confirm → web-6/7 (§13) |
| M0-4 governance: organic-volume floor + cluster thresholds | **COMPUTED + RECORDED — §12.36 (2026-07-10), SPLIT disposition.** (a) **Organic-volume floor (Gate G-A.1, §14):** M0 default `N = 5` graduations-equiv / 7d = `floorWei = 40384344110704909120` (5 × `GRADUATION_ETH`, self-referential — no headline/USD metric, §2), `windowDays = 7`, `organicFlowDiscountMaxPct = 50`, measured vs own-indexer organic-volume (§8.5) under the ≤50% discount (§2.2). **Magnitude N is a product/policy go/no-go call → NOT settled: requires architect + effectively user ratification before Gate G-A, recalibrate at M2 with real organic-volume series. Stays a live Gate-G-A input in §13.** (b) **Cluster-alert thresholds (gate 7, §10 amend):** `perTokenPct = 25` (X), `platformPct = 10` (Y, `≤ perTokenPct` asserted), `windowHours = 24` — advisory-only, never gates chain state (§8.4/§8.5). **Tunable engineering default → final tuning routed to robbed-security before beta (gate 7).** | spec §12.36; §13 (organic-floor STAYS OPEN as Gate-G-A input, cluster thresholds → security); `constants.json` `governance.*` |

### M3-10 / M2-3 §13 checkpoint pass (2026-07-10) — Phase-P production-prep

robbed-architect decided web-10 (M3-10) and recorded the M2-start §13 dispositions (M2-3). Recorded in spec §12.47–§12.48; §13 rows updated.

| Finding | Disposition | Where |
|---|---|---|
| web-10 large-value disclosure threshold (M3-10) | **DECIDED — §12.47 (2026-07-10).** `largeValueEthThreshold = 1000000000000000000` wei (**1.0 ETH**). Trade ETH notional ≥ 1.0 ETH ⇒ stronger posted/finalized disclosure (§2.1). Config, never a literal; ≈ 12.38% of `GRADUATION_ETH` (self-referential cross-check) and ≈ $1,771.51 at the §12.35 `ethUsdSnapshot` (recorded basis, not stored). **M0/config default, retunable in the capped beta.** Config keys `NEXT_PUBLIC_LARGE_VALUE_ETH_THRESHOLD` (web, already a `.env.example` placeholder) + API mirror; **wiring is a robbed-frontend + hoodpad-indexer follow-up — NOT done in this pass**. *Update 2026-07-11 (M3-10 verify):* web key present + consumed (`apps/web/src/shared/lib/env.ts`, trade-widget `large-value.ts`, default `1.0`); API mirror key `LARGE_VALUE_ETH_THRESHOLD` added to `apps/api/src/config.ts` (Zod, default `"1.0"`, derived `largeValueEthThresholdWei`) + env-inventory §2 row. Remaining follow-ups: frontend populates `.env.example` value `1.0` (currently empty placeholder; code default already correct); API surfaces the value where trade DTOs carry confirmation state (robbed-indexer) | spec §12.47; §13 (web-10 closed); decisions §4 web-10; `docs/runbooks/env-inventory.md` §2–3 |
| OI-6 ETH/USD source (M2-3) | **DECIDED — §12.48a.** Config-driven `ETH_USD_SOURCE_URL`; Chainlink-if-present else DefiLlama/Coinbase (always-available default). **Chainlink-existence verification ENV-GATED (needs live 4663 RPC)** — finalized at M2 start. *Update 2026-07-11:* env-gated leg CLOSED — feed CONFIRMED, addresses + mandatory startup assertions recorded in **spec §12.51** (evidence §11 below) | spec §12.48a + **§12.51**; §13 (OI-6 closed); indexer.md §3.9; decisions §2 OI-6 / §3 OI-A10 |
| OI-8 `safe`/`finalized` RPC tags (M2-3) | **DECIDED — §12.48b.** Use tags if supported; else pre-sanctioned L1-watermark fallback (M2-3b). **Tag-support verification ENV-GATED (needs live 4663 RPC)** — decided M2 day 1; M2-6 tracker already no-ops gracefully. *Update 2026-07-11:* env-gated leg CLOSED — both tags SUPPORTED on the official RPC; **M2-3b NOT funded** (dormant fallback; plan item N/A) (evidence §11 below) | spec §12.48b (annotated); §13 (OI-8 closed); indexer.md §5.1; decisions §2 OI-8 |
| OI-11 Ponder external-write tolerance (M2-3) | **DECIDED — §12.48c.** Sidecar `event_confirmations` = sanctioned default (any Ponder version); direct in-column UPDATE only if the pinned version tolerates it. **VERSION-GATED, not RPC-gated**. *Update 2026-07-11:* version gate RESOLVED — ponder 0.16.8 does NOT tolerate direct UPDATEs on hot rows; **sidecar MANDATORY**; direct-UPDATE code in `apps/indexer` is a recorded mismatch under rework (robbed-indexer, in flight) (evidence §11 below) | spec §12.48c (annotated); §13 (OI-11); indexer.md §5.1/§7.3/§10; decisions §2 OI-11 |

**Env-gated on a live Robinhood RPC (recorded conditionally, no chain fact invented):** OI-6 (Chainlink feed existence) and OI-8 (`safe`/`finalized` tag support). OI-11 is version-gated (pinned Ponder), not RPC-gated. web-10 is fully closed. *Update 2026-07-11:* all three gates resolved with evidence (§11 below) — OI-6 feed confirmed (spec §12.51), OI-8 tags supported (M2-3b not funded), OI-11 sidecar mandatory on ponder 0.16.8. Plan items M2-0b / M2-3 / M2-3b flipped.

### Integration-seam reconciliation sweep (2026-07-10) — parallel-vertical-slice cross-service seams

The parallel M-track build (contracts + indexer + api + frontend) surfaced cross-service seams needing ratification before Phase I. hoodpad-architect decided each; recorded in spec §12.38–§12.43. **Highest priority: seam #1 (read-function ABIs) was actively blocking frontend M3-5 on-chain reads and the indexer curve-constants startup read.**

| Seam | Item | Disposition | Owner / where |
|---|---|---|---|
| #1 | Read-function ABIs missing from `packages/shared` (only event ABIs present) — blocks M3-5 Trust-panel live reads + indexer startup curve-constants read (env-interim) | **DECIDED — §12.38.** Compilation-time codegen (`forge build`, no deploy) emits full ABIs of all six contracts → `packages/shared/src/abi/*.json`. Distinct from `events.json` (§12.15) and the deploy-time `addresses` codegen (M1-14) | **hoodpad-contracts** (extend M1-3 codegen → new **M1-3b**); consumers: hoodpad-indexer (replace env read), hoodpad-frontend (M3-5). spec §12.38; contracts.md §7; indexer.md §2/§3.1; web.md §3.2; architecture.md §4 |
| #2 | `ICurveFactory.FactoryConfig` struct shape not itemized (M1-8 flag / F-5) | **DECIDED — §12.39.** Shape = the §2.2 storage field set (operational owner-settable + immutable ceilings + immutable curve-shape defaults), returned whole by `config()`; per-token live values read from the curve, not `config()` | **hoodpad-contracts** aligns M1 impl. spec §12.39; contracts.md §2.2 |
| #3a | Shared response-type aliases `ConfirmationsResponse`/`EthUsdResponse`/`ModerationQueueItem` | **DECIDED — §12.40a.** Added to `api-types.ts` | **hoodpad-shared.** spec §12.40a |
| #3b | `conflict`/409 vs 400 for metadata imageHash mismatch | **DECIDED — §12.40b.** No genuine conflict path in v1 (content-addressed = idempotent); imageHash-reference mismatch = **400 `invalid_request`**; `conflict`/409 dropped from the shared error-code union | **hoodpad-shared** (union + enum doc); api.md §3.2. spec §12.40b |
| #3c | Vendor interface placement (`CsamHashMatcher`/`ContentClassifier`) — shared vs API | **DECIDED — §12.40c.** API-only (`apps/api/src/moderation/vendors/`) — fails the ≥2-consumers anti-drift test; api.md §4.3 corrected (they do NOT live in shared) | **hoodpad-shared** confirms exclusion; api owns files. spec §12.40c; api.md §4.3 |
| #3d | `tokens.trade_fee_bps` — Trust panel source (config vs on-chain per-token) | **DECIDED — §12.40d.** Indexer stores per-token immutable snapshot (curve `TRADE_FEE_BPS` read at `TokenCreated` via §12.38 ABI); Trust panel sources per-token value, never factory config (`setTradeFeeBps` = future curves only) | **hoodpad-indexer.** spec §12.40d; indexer.md §3.1; api.md §3.4 |
| #3e | `change24hPct` 24h-open anchor | **DECIDED — §12.40e.** Anchor = close of most-recent 1h candle ≤ `now−24h`; <24h-old ⇒ first-trade price; no trades ⇒ 0. Tunable | **hoodpad-indexer.** spec §12.40e; indexer.md §4 |
| #3f | `moderation_audit_log` shape (api-local vs shared) | **DECIDED — §12.40f.** API-local table (fails ≥2-consumers test); DDL in api.md §6.2, not `packages/shared` | **hoodpad-indexer** documents; **hoodpad-shared** confirms exclusion. spec §12.40f; api.md §6.2 |
| #4 | M2-0b Ponder same-tx factory-child spike (E-3) — timing + fallback | **DECIDED — §12.41.** Runs at Phase-I/M2 start (stack up, I-2 fixture); **receipt-derived initial-buy** (parse first `Trade` log from `createToken` receipt) is the **pre-sanctioned** fallback — no re-escalation on spike failure | **hoodpad-indexer.** spec §12.41; indexer.md §7.4 |
| #5 | Indexer CI `typecheck` fails without Ponder virtual-module types | **DECIDED — §12.42.** `typecheck` = `ponder codegen && tsc --noEmit` | **hoodpad-shared** (CI/workspace scripts §12.29). spec §12.42 |
| #6 | `IBondingCurve` "frozen" interface reconciled to §12.25 pull-payment shape (F-1) | **CONFIRMED — §12.43.** Legitimate reconciliation to a landed §12 decision, not a freeze violation; baseline is now the §12.25-reconciled shape | hoodpad-contracts (already landed). spec §12.43; contracts.md §2.3 |

**No new §13 open items.** Seam #4's spike *result* is an implementation finding (recorded in indexer.md §7.4), not an open decision — the decision (timing + fallback) is closed. All six seams are DECIDED/CONFIRMED in §12.

## 7. Cancun EVM-target amendment + interface follow-up sweep (2026-07-10)

Recording pass: one substantive spec amendment (Cancun evm_version) plus a sweep of accumulated minor cross-service interface follow-ups so they are not lost. The architect records dispositions + owners; implementation is routed, not done here.

### 7.1 Substantive: Cancun EVM target (§12.44)

| ID | Item | Disposition | Owner / where |
|---|---|---|---|
| Cancun evm_version | `foundry.toml` pins `evm_version = "cancun"` after OZ v5.5 `Bytes.sol` `MCOPY` (EIP-5656) is reached transitively via the mandatory `ERC20Permit → ECDSA → EIP712 → Strings → Bytes` chain (pre-Cancun cannot compile) | **DECIDED — §12.44.** Safe on 4663: ArbOS 20 "Atlas" added `MCOPY`; Robinhood Chain runs ArbOS ≥ 32; Blockscout verifier lists `cancun`. **Only `mcopy` relied upon; transient storage (`TSTORE`/`TLOAD`) deliberately unused** — storage-based `ReentrancyGuard` stays. Supersedes the "do not assume Cancun features" wording. Final `cancun`-target verification folded into the **O-5** pin check at **M1-2/O-5**. Owner: **hoodpad-contracts.** spec §12.44 + §6.7 + §13 (O-5); contracts.md §7.1 + §3.4-step-3 + §8 (O-5 row) |

### 7.2 Interface follow-up sweep (minor — dispositions + owners)

None of these open a new §13 item or a new §12 decision (the two anti-drift/wiring items are follow-through on already-ratified §12.40d/§12.40e; the rest are optional refinements or ops calls). Recorded so they are tracked to their owners.

| # | Item | Disposition | Owner / where |
|---|---|---|---|
| 1 | `TokenRow.trade_fee_bps: number` — shared `db-rows.ts` `TokenRow` type + API card/detail projection must consume the indexed `tokens.trade_fee_bps` column (§12.40d), **not** `apps/api/src/config.ts` (factory-current, misreports older curves) | **WIRING FOLLOW-UP (implements §12.40d).** Add `trade_fee_bps: number` to shared `TokenRow`; API projection reads the column | **hoodpad-shared** (type) + hoodpad-indexer/API (wire). indexer.md §3.1 |
| 2 | `change24hPct` resolver placement — the §12.40e 24h-anchor logic has ≥2 consumers (indexer materialization + API `card` projection), so per anti-drift it lives once in `packages/shared`; today it is duplicated in `apps/indexer/src/change24h.ts` and `apps/api/src/projections/card.ts` returns `change24hPct: null` | **ANTI-DRIFT FOLLOW-UP (implements §12.40e + §12.29 anti-drift).** Host the resolver in `packages/shared`; both services import; API stops returning null | **hoodpad-shared** hosts → API consumes. indexer.md §4.5 |
| 3 | `mcapEth` on `TokenCard` — OG/cards render mcap **ETH-first** (§2) but the card only carries a `UsdValue`; OG derives ETH from `usd/ethUsd` | **OPTIONAL REFINEMENT.** Add a native `mcapEth` decimal-string to `tokenCardSchema`, materialized by the indexer, so ETH-first display needs no client-side derivation. Not blocking; §2 ETH-first discipline | **hoodpad-shared** (schema) + hoodpad-indexer (materialize). Deferred/optional |
| 4 | WS `trade` payload aggregates — Discover (M3-4) can only live-patch `priceEth`; true live mcap/progress/Δ% need recomputed `TokenCard` aggregates on the `trade` WS message (or a dedicated token-summary WS message) | **OPTIONAL REFINEMENT.** Recompute `TokenCard` aggregates onto the `trade` WS message, or add a token-summary WS message. Not blocking M3-4 (priceEth patch is the v1 behaviour) | **hoodpad-indexer** + **hoodpad-shared** (`ws-messages.ts`). Deferred/optional |
| 5 | `/metrics` surface — gate-7 in-process metrics are on the indexer's `METRICS_PORT 9464` (M2-12), not the API port | **OPS DISPOSITION (Phase-I/M4, not now).** Either scrape the indexer directly or have the API proxy `/metrics` — a M4 ops/observability decision, not a v1 code change | **hoodpad-security**/ops at M4. No action now |
| 6 | `config()` curve-shape getters — the M1 CurveFactory `config()` implementation omits the 5 curve-shape defaults (`virtualEth0`/`virtualToken0`/`curveSupply`/`lpTranche`/`graduationEth`) that §12.39 / contracts.md §2.2 itemize in `FactoryConfig`; per-curve reads (per-token `TRADE_FEE_BPS` §12.40d + curve immutables) serve all consumers today | **REPORTED DIVERGENCE (§12.39 "report any divergence") — LOW-PRIORITY CONVENIENCE ADD.** Bringing `config()` into full §12.39 alignment (adding the 5 fields) is a convenience follow-up, not blocking, since consumers read curve-shape constants per-curve and the indexer startup read (§12.38) can also read them per-curve. contracts.md §2.2 already reflects the ratified full shape — the **implementation** catches up, the doc does not change | **hoodpad-contracts.** Optional; no doc change |

## 6. Spec Addendum v1.2 (2026-07-09) — adversary model, organic-flow, project framing

Authoritative user addendum; incorporated faithfully (not re-litigated). All entries are **spec-resident** (v1.2 §2.2/§3/§5.2/§8.5/§10/§11/§13/§14) — this register cross-references and records the binding planning assumptions.

| Decision | Disposition | Where |
|---|---|---|
| **Organic-flow discount** | **BINDING PLANNING ASSUMPTION.** Assume **≤50% of headline DEX volume is organic** until own indexer says otherwise; all market-sizing/revenue estimates carry it explicitly | spec §2.2; M0-4 (floor), G-A.1 |
| **Day-1 users are bots** | **BINDING.** Product must be correct + profitable under bot-dominant flow (fee capture actor-agnostic — pull-payment §12.25 accrues regardless) while protecting human UX (§5.3 atomic buy, §6.5 caps) | spec §2.2 |
| **Two-phase framing + Gate G-A** | **DECIDED (spec §14).** Phase A = the plan's existing Goal (testnet, portfolio-grade); **Gate G-A** (market/competition/personal/legal — all NEEDS-USER) gates Phase B (M4/M5). Legal moves open→blocking at G-A. §14 does not expand the Goal | spec §14; plan "Gate G-A" section |
| **M1 hard timebox (pre-made)** | **DECIDED (spec §11).** If V3Migrator fuzz/invariant not green by end of week-1 of M1 → V2+burn fallback (§6.3) same day. **Cost is explicit and eyes-open:** flips LP copy §12.14→"burned" (§6.3, only sanctioned flip), kills post-grad fee revenue (§6.4 LPFeeVault descoped); **trigger is migrator gate-failure, NOT V3 availability (§12.28 confirms V3 on 4663)**; **§12.25 curve-fee escrow unaffected** (curve-side) | spec §11; plan M1 timebox block |
| **§8.5 bot/farm detection** | **DECIDED (spec §8.5).** M2 SQL views/jobs over existing trades+transfers; **advisory labeling only, never gates chain state** (§8.4 philosophy); feeds Trust panel + internal dashboard | indexer.md §8.5; api.md trust payload/holders; web.md §3.2; plan M2-13 |
| **Trust-panel organic metrics** | **DECIDED (spec §5.2).** Organic-holder estimate as a RANGE, flow quality, funding-cluster grouping — cheapest differentiation, no new on-chain surface | web.md §3.2; plan M3-5 |
| **Gate 6/7/10 amendments** | **DECIDED (spec §10).** Gate 6 parameterized vs observed §2.2 patterns; gate 7 cluster-volume alert (M0 thresholds); gate 10 heuristic-metrics disclosure | threat-model §3.1/§7; plan M2-12 |
| **hood.fun = mainnet incumbent** | **DECIDED (spec §3).** Weekly traction snapshot (own indexer/Dune, source+timestamped) → Gate G-A.2 | indexer.md §8.5.3; plan M2-14 |
| **M0 additions** | organic-volume floor (G-A.1) + funding-cluster alert thresholds X%/Y% (gate 7) in the M0 notebook | spec §13; plan M0-4 |

**Reconciliation notes (recorded, not re-litigated):**
- **V2+burn timebox vs §12.28/§12.25:** the fallback is triggered only by migrator gate-failure within week 1, not by V3 absence (§12.28 confirms V3 on 4663). Its cost — LP-copy flip (§12.14→"burned") + loss of post-grad fee revenue — is accepted up-front. The pull-payment curve-fee escrow (§12.25) is orthogonal to graduation venue and survives the flip.
- **§14 vs the plan Goal:** identical scope. §14 Phase A == the Goal ("production-ready, not production-launched"); §14 only names the phase/gate structure and makes the mainnet decision explicit + conditional. No Goal expansion.

## 8. Hosting / deployment-target decision (2026-07-10)

User-directed, authoritative — recorded, not re-litigated. Resolves the architecture.md §6 "Hosting" deferral ("concrete host/orchestration decided at M4") ahead of schedule. Full detail in **spec §12.45**; buildable configs + runbook at **`docs/runbooks/deploy-komodo-cloudflare.md`**.

| Decision | Disposition | Where / owner |
|---|---|---|
| **Backend → self-hosted Komodo Stack** (Postgres+pg_trgm, Redis, Ponder indexer Node container, Hono/Bun API + Bun WS fanout) via Komodo core + periphery agent, git-synced compose | **DECIDED — §12.45.** WS fanout stays co-located with Redis so the Redis→socket hop keeps the <500ms budget (§8); long-running stateful processes are not an edge workload | spec §12.45; architecture.md §6; runbook Part A. Owner: **hoodpad-indexer** (Dockerfiles + Stack def, per P-9 infra ownership) |
| **Frontend → Cloudflare Workers** via OpenNext (`@opennextjs/cloudflare`, `nodejs_compat`) — NOT Pages-edge | **DECIDED — §12.45.** OpenNext supports Next.js 16 (docs-first confirmed 2026-07-10; re-verify at impl time — deploy blocker if it regresses) | spec §12.45; architecture.md §6; runbook Part B. Owner: **hoodpad-frontend** (after the `apps/web` redesign) |
| **OG rendering: native `@resvg/resvg-js` → WASM** (`@resvg/resvg-wasm`+`satori` or `next/og`) | **DECIDED — §12.45.** `workerd` cannot run N-API addons; **supersedes implementation-plan M3-8's native raster backend for the Workers target.** satori layout unchanged; raster backend swapped; native dep removed from deps + `serverExternalPackages` | spec §12.45; runbook B.6; web.md §6 (native-resvg note superseded for deploy target). Owner: **hoodpad-frontend** |
| **Cloudflare = R2 + CDN** for images + metadata (bucket `robbed-assets`, account `0b1b0b8753489a11d35ee922961f6b72`) | **CONFIRMED — §12.45.** Unchanged role (architecture.md §6, §12.19); on-chain hash stays the commitment | spec §12.45; architecture.md §6 |

**P-3 / P-2 plan-item guidance updates (report to plan owner — NOT edited here):**
- **P-3** — remove the web Dockerfile obligation and the "host-agnostic / M4 decision" framing. New split: Dockerfiles for **indexer (Node) + api (Bun)** only, built for the **Komodo Stack** (runbook Part A.5); the **web Dockerfile is moot** (frontend → Workers). Add: buildable **`wrangler.jsonc` + `open-next.config.ts` + OpenNext `deploy` pipeline** for `apps/web` (runbook Part B), and the **Komodo Stack resource** (git-synced compose, `tools/deploy/komodo/`). Verify leg becomes: `docker build` exits 0 for **indexer + api** (not web); `opennextjs-cloudflare build` succeeds for web; config files present. Owner split: indexer/api → **hoodpad-indexer**; Workers config → **hoodpad-frontend**.
- **P-2** — under "indexer/API/WS/web hosting steps," replace the generic hosting line with two concrete targets: **Komodo Stack deploy** (runbook Part A) for indexer/API/WS, and **Cloudflare Workers deploy** (runbook Part B) for web; DNS/CDN/R2 setup points `robbed-assets` + the Workers custom domain + the Komodo public endpoints. Cross-reference `docs/runbooks/deploy-komodo-cloudflare.md`.

---

## 9. Project rename `hoodpad` → `ROBBED_` (2026-07-10)

User-directed, authoritative: "rename all project name mentions from hoodpad to robbed_ in the repo." Full detail in **spec §12.46**; ordered execution checklist + verify commands in **`docs/archive/rename-hoodpad-to-robbed.md`** (executed 2026-07-10, commits `f83967a` + `3d6cbec`; plan archived per §14 below). **Deferred, not executed** (as written 2026-07-10): a live `apps/web` redesign and the running `hoodpad-*` agents make an in-flight scope/agent rename build-breaking — the rename runs as one atomic pass **after** the redesign foundation lands and **before** the Workers adaptation.

| Decision | Disposition | Where / owner |
|---|---|---|
| **Brand / display / copy / logo / OG / titles → `ROBBED_`** (lowercase `robbed_` where the trailing-cursor motif is intended; `_` is a display motif only, never in an identifier) | **DECIDED — §12.46.** Resolves the *name* portion of the §13 Name/brand open item | spec §12.46, §13. Owner: **hoodpad-architect** |
| **Identifiers → `robbed` (no underscore):** npm scope `@hoodpad/*`→`@robbed/*`, all package names, agent names/files `hoodpad-*`→`robbed-*` + call-sites, docker/pg/redis/minio names, env + metric prefixes, Solidity `HoodpadConstants`/`HOODPAD_CONSTANTS`→`Robbed…`/`ROBBED_…` | **DECIDED — §12.46.** `@robbed_` is not a valid npm scope; trailing `_` is illegal/awkward in npm, Postgres, docker, Solidity grammars | spec §12.46; plan doc. Owner: **hoodpad-shared** (scope/infra/agents) |
| **Solidity *type* names NOT renamed** (LaunchToken, BondingCurve, CurveFactory, Router, V3Migrator, LPFeeVault, CurveMath) — product-neutral; only literal "hoodpad" NatSpec strings change | **CONFIRMED — §12.46.** | spec §12.46; `contracts/src/*` |
| **Already-final external names stay:** R2 bucket `robbed-assets` ✓, Cloudflare account ✓ (§12.45). Worker name `hoodpad-web` (plan only, undeployed) → `robbed-web` | **CONFIRMED — §12.46.** Nothing must remain `hoodpad` | spec §12.45/§12.46; deploy runbook Part B |

---

## 10. Three-environment model (2026-07-10)

User-directed, authoritative — recorded, not re-litigated. Full detail in **spec §12.49**; per-env config matrix at **`docs/runbooks/environments.md`**; per-var dev/testnet/prod columns already present in **`docs/runbooks/env-inventory.md`**. Official chain facts docs-first from `docs.robinhood.com/chain/connecting` (2026-07-10) — never invented; per §2 they live in env/config, not source (chain id stays a `@robbed/shared` constant asserted at startup).

| Env | Chain id | RPC (public / Alchemy-recommended) | WS | Explorer | Domain | Phase | Contract addresses |
|---|---|---|---|---|---|---|---|
| **LOCAL** | anvil (fork = 4663) | `http://localhost:8545` | `ws://localhost:8545` | — | `localhost` | Phase I | fresh local deploy codegen; mock/fork V3+WETH |
| **TESTNET** | `46630` | `https://rpc.testnet.chain.robinhood.com` / `https://robinhood-testnet.g.alchemy.com/v2/{KEY}` | `wss://feed.testnet.chain.robinhood.com` / `wss://robinhood-testnet.g.alchemy.com/v2/{KEY}` | `explorer.testnet.chain.robinhood.com` | `testnet.robbed.fun` | Phase T (§14 Phase A) | `tools/deployments/testnet.json` → codegen |
| **MAINNET** | `4663` | `https://rpc.mainnet.chain.robinhood.com` / `https://robinhood-mainnet.g.alchemy.com/v2/{KEY}` | `wss://feed.mainnet.chain.robinhood.com` / `wss://robinhood-mainnet.g.alchemy.com/v2/{KEY}` | `robinhoodchain.blockscout.com` | `robbed.fun` | Phase B (Gate-G-A-gated, §14) | `tools/deployments/mainnet.json` → codegen; V3/WETH §12.28 constants |

| Decision | Disposition | Where / owner |
|---|---|---|
| **3 environments — LOCAL (anvil/fork, zero creds), TESTNET (46630), MAINNET (4663)** — each a distinct chain + endpoint set + domain | **DECIDED — §12.49.** | spec §12.49; environments.md. Owner: **robbed-architect** ratifies |
| **Phase mapping:** LOCAL = Phase I stack; TESTNET = Phase T = §14 Phase A (`testnet.robbed.fun`); MAINNET = Phase B, only after Gate G-A (`robbed.fun`) | **DECIDED — §12.49.** Legal wrapper (§13) blocking at G-A; no fees/mainnet before G-A | spec §12.49, §14 |
| **Testnet chain params (chain id/RPC/WS/explorer)** furnished from official docs | **RESOLVED — §12.49** (was §13 open). **Still open: testnet faucet URL** (Phase-T deploy start, robbed-contracts) | spec §12.49, §13 |
| **Contract-address sourcing:** LOCAL = fresh-deploy codegen; TESTNET/MAINNET = `tools/deployments/{testnet,mainnet}.json` | **DECIDED — §12.49.** Uniswap V3/WETH §12.28 constants are 4663-mainnet-only; testnet from artifact, LOCAL mock/fork; all assert at startup | spec §12.49; env-inventory.md §1. Owner: robbed-contracts (deploy) + robbed-shared (codegen) |
| **Per-env Cloudflare Workers = Wrangler named environments** (`env.testnet`/`env.production`, `<name>-<env>` Worker name, non-inheritable vars/bindings/routes per env; `wrangler deploy --env`) | **DECIDED — §12.49** (docs-first, `developers.cloudflare.com/workers/wrangler/environments`) | environments.md §3; deploy-komodo-cloudflare.md Part B. Owner: robbed-frontend + robbed-shared |
| **DNS prerequisite (BLOCKING for custom domains):** `robbed.fun` registered but NOT yet on Cloudflare DNS; nameservers → account `0b1b0b…f6b72`; `*.workers.dev` until then | **OPEN — §13** (domain leg). Add zone + point NS before any Worker custom domain | spec §12.49, §13. Owner: ops/user |

**Implementation follow-ups (routed, NOT done here):** per-env `.env.*` files + `wrangler.jsonc` environments → robbed-frontend/robbed-shared; per-env deploy-artifact → addresses codegen → robbed-contracts/robbed-shared; per-env indexer/API config (RPC/WS/CORS/`START_BLOCK`) → robbed-indexer. Detail in environments.md §5.

### M3-11 ratification + §12.50 four-page redesign record (2026-07-11)

| Decision | Disposition | Where / owner |
|---|---|---|
| **Four-page redesign deviations recorded** (user-directed via `docs/design/robbed-redesign-plan.md`): Portfolio `/portfolio` → v1 (read-only, no new tx types, no `collect()` UI); `/launch`→`/create`; brand `ROBBED_` (per §12.46); terminal-mono skin supersedes §12.24 look; mobile-first primary. Data/protocol rules (§2, §6.5, §12.14, §12.19, §12.20, §12.25) UNCHANGED | **RECORDED — §12.50 (2026-07-11).** §5 heading/§5.3/§5.4 amended by reference + inline notes | spec §12.50, §5. Owner: robbed-frontend executes; architect recorded |
| **M3-11 user-flow catalog RATIFIED** — 36 flows (DISC-1..4, TD-1..12 incl. TD-3b, LAUNCH-1..3, COLLECT-1, ERR-1..14 incl. 6a/6b): §5.1 5/5, §5.2 8/8, §5.3 5/5; 9 tx types; 15 error paths (ERR-5 §12.25 full three-layer); 16 P-7 waiver rows = exactly the <3-layer flows | **RATIFIED 2026-07-11, as amended** for §12.50: `/launch`→`/create` route strings updated, `LAUNCH-*` IDs retained (stable-ID rule now in force) | `docs/user-flows.md` + `docs/user-flows-waivers.md` sign-off lines |
| **`PORT-*` Portfolio flows — explicit coverage gap** | ~~OPEN addendum, owner robbed-frontend (author) → architect (ratify), due before the I-5a `e2e:coverage` baseline freezes~~ **CLOSED 2026-07-11 — addendum authored (robbed-frontend) + RATIFIED (architect); see §13 below.** 44 flows are the I-5a coverage baseline | `docs/user-flows.md` §3b/§8; spec §12.50 (annotated); §13 below |

---

## 11. M2-0b spike + M2-3 env-gated verifications (2026-07-11)

robbed-indexer executed the M2-0b Ponder same-tx spike (§12.41) and the three M2-3 §13 checks whose *dispositions* were already DECIDED in §12.48a-c but whose *verifications* were env/version-gated. These are **verification outcomes recorded with evidence** — the decisions themselves are unchanged; anything needing a new spec §12 entry is flagged to robbed-architect, not self-recorded.

| Finding | Outcome + evidence | Where |
|---|---|---|
| **M2-0b** Ponder same-tx factory-child capture (E-3/Q18, §12.41) | **VERIFIED — NATIVE CAPTURE; receipt fallback NOT needed.** Pinned Ponder (`^0.16.6` → lockfile **0.16.8**) indexes a child's event emitted in the **same tx** as its factory registration event, on **both** sync paths. Source mechanism (installed package source): `isAddressMatched` matches children with `registrationBlock <= log.blockNumber` (`src/runtime/filter.ts:74-92`); realtime fetches **all** block logs unfiltered and resolves factory children **before** log filtering (`src/sync-realtime/index.ts` ~470-752); historical syncs factory logs first, then queries child logs over the same interval (`src/sync-historical/index.ts` ~434-599). Empirical spike (anvil + `ponder dev` on embedded PGlite, no Docker): same-tx `ChildCreated`(logIndex 0) + child `Trade`(logIndex 1) both captured in order, historical (tx `0x1fda3550…`) and realtime (tx `0x9282a0ab…`), zero handler errors. ponder.sh factory docs are silent on same-block behavior — source + spike are the authority. Full record: indexer.md §7.4 (incl. the §12.15 emission-order assumption M2-5 handlers rely on) | indexer.md §7.4; M2-5 relies on native capture. *Architect 2026-07-11:* recorded in **spec §12.41 (annotated — fallback retired/dormant; §12.15 emission-order invariant recorded as contracts-side, owner robbed-contracts)**; plan M2-0b flipped |
| **OI-6** Chainlink ETH/USD on 4663 (§12.48a env-gated leg) | **VERIFIED — FEED EXISTS; Chainlink branch selected.** Chainlink's official registry lists **Robinhood Chain Mainnet** (`smartcontractkit/documentation` `chains.ts`; RDD `feeds-robinhood-mainnet.json`, 66 feeds incl. **ETH/USD**: proxy `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9`, aggregator `0x6091E64eb7138EEF066a80FD3A0d7427B91f2721`, feedCategory "low"). On-chain confirmed via `eth_call` on `rpc.mainnet.chain.robinhood.com` (2026-07-11): `description()` = "ETH / USD", `decimals()` = 8, `latestRoundData()` answer `181564468052` (= $1,815.64 @ `updatedAt` 2026-07-11T15:38:14Z — source+timestamped observation per §2, never stored/hardcoded). Per §12.48a the M2 poller reads this feed (source label `chainlink:4663`, indexer.md §3.9) with a staleness check; DefiLlama/Coinbase HTTP stays the documented configured fallback (env-inventory.md §3) and is what LOCAL/TESTNET use (feed is 4663-mainnet-only). **Flag to robbed-architect:** record the feed proxy address in spec §12 (as §12.28 did for V3) + mandate startup assertions (`description == "ETH / USD"`, `decimals == 8`) — *done 2026-07-11: spec §12.51* | spec §12.48a + **§12.51 (recorded; §13 OI-6 CLOSED)**; indexer.md §3.9/§10 |
| **OI-8** `safe`/`finalized` tags on Robinhood RPC (§12.48b env-gated leg) | **VERIFIED — BOTH TAGS SUPPORTED; M2-3b (L1 watermark reader) NOT funded** — stays a dormant pre-sanctioned fallback. `eth_getBlockByNumber ["safe",false]` and `["finalized",false]` against `https://rpc.mainnet.chain.robinhood.com` (2026-07-11) both returned full block objects (Arbitrum fields incl. `l1BlockNumber`; sequencer miner `0xa4b…sequencer`): safe = block `0x6c1332` (7,082,802; `l1BlockNumber 0x1854286`), finalized = block `0x6c0437` (7,078,967; `l1BlockNumber 0x1854268`), finalized < safe < latest as expected; `eth_chainId` = `0x1237` (4663) on the same endpoint. The M2-6 tracker's tag path is live; its graceful no-op remains as resilience only | spec §12.48b (annotated 2026-07-11; **§13 OI-8 CLOSED**; M2-3b flipped N/A — not funded); indexer.md §5.1/§10 |
| **OI-11** Ponder external-write tolerance (§12.48c version-gated leg) | **VERIFIED against ponder 0.16.8 — direct in-column UPDATE is NOT tolerated for hot rows; the §12.48c sidecar default is REQUIRED.** Mechanism (installed source): the indexing-store cache retains rows in memory across realtime blocks and `prefetch` **keeps cached copies without re-reading the DB** (`src/indexing-store/cache.ts` — predictions dropped for already-cached keys); flush rewrites **ALL columns** from the cached row (full-column `UPDATE`/`onConflictDoUpdate`, cache.ts ~596-670). So an external `confirmation_state` upgrade on `tokens` (a row mutated on every Trade) is silently reverted by that token's next trade flush → states flap backwards, violating §5.1 monotonicity. Insert-only tables (`trades`/`transfers`/`graduations`/`fee_collections`) are not re-flushed after insert, but the version gate fails on `tokens` alone. Ponder docs are silent on external writes (ponder.sh/docs/database, checked 2026-07-11) — no documented tolerance either. **Repo mismatch found:** `apps/indexer/src/confirmation.ts` (`materializationStatements` over all five Ponder tables incl. `tokens`) + `confirmationStore.ts` currently do the direct UPDATE (self-described "§7.3-sanctioned") — must be reworked to the sidecar `event_confirmations` join at M2 (robbed-indexer follow-up; recorded, not built in this pass). The `public`-schema offchain tables (migrations 0001/0002/0004+) are fine — Ponder manages only its own `DATABASE_SCHEMA`; the 0003 GIN indexes applied *into* the Ponder schema are DDL-only and conditionally re-applied by `migrate.ts` | spec §12.48c (annotated 2026-07-11: **sidecar MANDATORY** — version gate resolved; no new §12 entry needed); §13 OI-11 annotated; indexer.md §5.1/§7.3/§10; M2 follow-up: robbed-indexer (in flight); *update 2026-07-12: rework LANDED — read-derivation variant (no per-row join table), §15 + spec §12.48c* |

---

## 12. Phase-T testnet (46630) external-contract set (2026-07-11)

T-1 escalation decided by robbed-architect ("documented substitute if Uniswap is mainnet-only there — escalate, do not improvise"). Full record: **spec §12.52**. Every address below was live-re-verified by the architect on 2026-07-11 against `rpc.testnet.chain.robinhood.com` (`eth_chainId == 46630`); the official Uniswap registry was re-checked the same day (Robinhood Chain page = chainId 4663 only, exactly the §12.28 addresses; no testnet page). Per §12.49 all values are consumed from the testnet artifact / `tools/m0/external.testnet.json` / env — never inlined.

| Item | Disposition | Where / owner |
|---|---|---|
| **WETH on 46630** — `0x7943e237c7F95DA44E0301572D358911207852Fa` (official `docs.robinhood.com/chain/protocol-contracts`; proxy bridge pattern matches mainnet; `symbol()=="WETH"` live) | **CONFIRMED FACT — §12.52.** | spec §12.52; testnet.md §6 |
| **Safe v1.4.1 canonical set on 46630** (safe-deployments: "canonical"; six addresses bytecode-matched; ProxyFactory + SafeL2 singleton re-verified live; Safe{Wallet} UI supports the chain) | **CONFIRMED FACT — §12.52.** Satisfies §6.6 for the T-2 testnet treasury (dev signers). **Mainnet O-6 (deployment + signer set) stays OPEN** | spec §12.52, §13 (O-6 annotated); testnet.md §6 |
| **Multicall3** at canonical `0xcA11bde05977b3631167028862bE2a173976CA11` | **CONFIRMED FACT — §12.52.** | spec §12.52 |
| **Uniswap V3 on 46630** — §12.28 addresses have **zero code** on testnet; official registry is mainnet-only | **DECIDED — §12.52, option (i):** adopt the **Blockscout-VERIFIED community deployment**, TESTNET-ONLY: Factory `0xdf9e3D6ffaC4513dD7b053212bbECcbCD15ec932`, NPM `0xFFe6CFc4f759b65f9B62c9D05A9E21a78cE93e12`, SwapRouter02 `0xb79cB26e90EBBD9bC02c75267c9a86dBa1AFedB7`, QuoterV2 `0xDDcBe4989C8171F721c5e683C9C6339B59718213`. Assertions pass live (`feeAmountTickSpacing(10000)==200`, `NPM.factory()`, `NPM.WETH9()`). **Trust caveat:** factory owner = deployer EOA `0xe7A35BD4…F831F3` (can `enableFeeAmount`/`setFeeProtocol`; cannot upgrade/pause/break pools or touch LP principal) — acceptable at zero real value. Unverified clones rejected outright. Options (ii) own-deploy and (iii) block-Phase-T rejected (G-7/G-8 correctness is assertion+invariant-6-protected regardless of pool-infra provenance). **Scope: TESTNET-ONLY — mainnet stays §12.28; LOCAL stays mock/fork (§12.49)** | spec §12.52; testnet.md §6; consumer: robbed-contracts (T-3 deploy, fail-closed assertions) |
| **Faucet mechanics** — official `faucet.testnet.chain.robinhood.com`: 0.05 ETH + 5 of each stock token per 24h after verification (Robinhood support article); fallbacks Chainlink `faucets.chain.link/robinhood-testnet`, QuickNode `faucet.quicknode.com/robinhood/testnet` | **CONFIRMED FACT — §12.52.** Closes the §13/§12.49 faucet leg | spec §12.52, §12.49 (annotated), §13; testnet.md §3 |
| **Blockscout v2 verifier on testnet explorer** — no API key; `solc v0.8.35+commit.47b9dedd` in supported list | **CONFIRMED FACT — §12.52** (recorded; the M1-2/O-5 pin+`cancun` verification itself stays O-5, executed at the T-3 deploy) | spec §12.52, §13 (O-5 unchanged) |
| `tools/m0/external.testnet.json` fixture fill | **FOLLOW-UP:** fixture absent at ratification time (being authored by robbed-contracts); architect fills with the §12.52 values once present | tools/m0; robbed-contracts + architect |

## 13. `PORT-*` catalog-addendum ratification + O-8 WETH-leg definition closure (2026-07-11)

Two ratification items executed by robbed-architect before the I-5a coverage baseline freeze. Both verified against the shipped artifacts, not the authoring agents' claims.

| Decision | Disposition | Where / owner |
|---|---|---|
| **`PORT-*` catalog addendum (PORT-1..8, user-flows.md §3b) + 8 waiver rows** | **RATIFIED 2026-07-11.** Verified against the shipped Portfolio implementation (`apps/web/src/views/portfolio/**`, `entities/portfolio/**`, `app/portfolio/page.tsx` — read-only pass) and spec §12.50a: §6 traceability table covers every §12.50a surface bullet (header, 3 stat cells, 3 tabs) plus the implemented extras (pagination, `?address=` subject, empty/error states); every P-7 layer declaration honest — read-only page ⇒ no on-chain layer anywhere; PORT-4/PORT-8 correctly UI-only (no subject ⇒ no request; failed read ⇒ no indexed payload); waiver table (24 rows) matches the catalog exactly. Catalog total 36 → **44 = the I-5a `e2e:coverage` baseline** | `docs/user-flows.md` (sign-off + §3b/§6/§7/§8), `docs/user-flows-waivers.md`; spec §12.50 annotated |
| **Portfolio advisory-read semantics (no live-patch flow)** | **RATIFIED 2026-07-11 — option (a): advisory reads as-is; NO portfolio WS-channel requirement routed to robbed-indexer for v1.** Rationale: spec §12.50a defines Portfolio as read-only and imposes no live-patch obligation (§5's live-patch requirements are Discover/Token-Detail-specific — no §5 conflict); `api.md` §3.4a and the shipped implementation (TanStack Query, ~15s staleTime + refetch, no WS subscription in `entities/portfolio`) already embody it; authoring a live-patch flow would violate P-7's livelock rule (`e2e:coverage` demanding an assertion the product cannot produce). Confirmation honesty holds: PORT-2 badges render indexed `confirmationState`, so staleness only ever shows a *more conservative* tier — never premature finality (§2.1/§12.20). A Phase-2 live portfolio surface would add a new `PORT-*` flow then (stable-ID rule) | `docs/user-flows.md` §3b freshness note + §8; `api.md` §3.4a (deviation flag dispositioned — §12.50a is the §12 record) |
| **O-8 WETH-leg arb-budget *definition* (§12.33 M1 obligation)** | **CLOSED 2026-07-11 — the implemented rule IS the demanded definition.** §12.33 demanded the WETH-leg budget be *defined* by robbed-contracts and *proven in gate-2 invariant 6* (threat-model §8.1 candidate (a)). Implemented (`contracts/src/V3Migrator.sol` decision #2 + `_arbStep`): `wethArbBudget = wethForMint × MIGRATION_SLIPPAGE_BPS / 10_000`; M1-10 Part-1 (M-10-A) made the token leg symmetric (draw down to `LP_TOKEN_TRANCHE × (1 − bps)`); `amount0Min`/`amount1Min` = pre-arb requirement × `(1 − bps)` independently re-enforce the parity floor both sides — no stated guarantee changes (§12.11 parity within `MIGRATION_SLIPPAGE_BPS`). Proven: M1-10 re-gate PASS — gate-2 invariant 6 (384 griefed lifecycles vs real V3, 0 hostile mints) + token-leg liveness invariant + directed `test_M10A_tokenLegGrief_*` regressions. Rule recorded in contracts.md §3.4 step 5 / §8 O-8 row. **NOT closed by this (tracked separately, caps-lift M4):** gate-6 griefing-cost quantification + UM-2 Part-2 residual (mispricing beyond the recoverable range → retriable revert) | spec §12.33 (annotated) + §13 (O-8 line updated); deploy.md H.2 (row moved to closed); threat-model §8.1 unchanged (UM-2 Part 2 stays open) |

---

## 14. Documentation taxonomy + plans-basename refactor (2026-07-12)

User-directed, authoritative (2026-07-12 docs-refactor directive, executed by robbed-architect). **Process-level per the P-9 precedent — no spec §12 entry needed** (governs doc placement/genre, not protocol or product behavior). The only spec text touched is the §12.46 pointer to the archived rename plan.

| Decision | Disposition | Where / owner |
|---|---|---|
| **Unique-basename rule for `docs/`: per-service plans renamed `plans/{api,contracts,indexer,shared,web}.md` → `plans/*-plan.md`** | **RATIFIED + EXECUTED 2026-07-12.** The `api.md` basename collision between `docs/plans/` and `docs/services/` broke doc-check named-doc resolution (the `findNamedDocs` fix, `scripts/doc-check.ts`); unique basenames retire the defect class. All consumers re-pointed: `docs/plans/README.md` links, `docs/plans/indexer-plan.md` scope note, `docs/plans/api-plan.md` cross-link, `docs/user-flows.md` plan-row pointer, this register's evidence cell for spec §12.37, `scripts/doc-check.ts` comments (multi-candidate acceptance retained as defense-in-depth), `packages/shared/src/ws-messages.ts` D2 comment pointer | `docs/plans/*` · owner: robbed-architect (index) / service agents (plans) |
| **`docs/archive/` created (genre: completed/superseded planning artifacts, provenance only); executed rename plan moved there** | **EXECUTED 2026-07-12.** `rename-hoodpad-to-robbed.md` moved `docs/plans/` → `docs/archive/` (the rename itself executed 2026-07-10, commits `f83967a` + `3d6cbec`). Spec §12.46 + decisions.md §9 pointers updated; `docs/archive/README.md` records completion date + superseding record per entry | `docs/archive/` · owner: robbed-architect |
| **Genre taxonomy (7 genres) + documentation map ratified: `docs/README.md` rewritten as the authoritative per-doc genre/owner/consumer/relationship map, building on development-flow.md §1; per-directory genre READMEs added** (`services/`, `runbooks/`, `security/`, `review/`, `design/`, `archive/`; `plans/README.md` refreshed) | **RATIFIED 2026-07-12.** NORMATIVE / DESIGN / PROCESS / EXECUTION / LEDGERS / CATALOGS / RUNBOOKS (+ co-located component READMEs). Notables: `runbooks/env-inventory.md` is genre-CATALOG deliberately housed in `runbooks/` for its script consumer (`scripts/env-sync-check.ts`); `docs/Robbed.html` path frozen (40+ code comments cite it); machine-consumed paths frozen and listed in the map's "Frozen paths" rule | `docs/README.md` + directory READMEs · owner: robbed-architect |

---

## 15. Wave-1 goal-completion rulings (2026-07-12)

Architect rulings for the decision points D-1…D-4 of `docs/goal-completion-plan.md` §0 plus two landed-rework records, executed as the plan's Wave-1 architect items. D-1 is **user-ratified** (the user directed "record the deviation"); the rest are architect rulings within the spec's delegation. Plan-text amendments (T-3/G-7 path wording, G-9 wording, M3-4/M2-13/M2-14 annotations) are executed by `/goal` in `docs/implementation-plan.md` — **not** edited here.

| ID | Decision | Disposition | Where / owner |
|---|---|---|---|
| **D-1** | **Discover-surface deviation** — shipped Discover (`/`) = TRENDING carousel + live event tape; §5.1 KotH hero, token grid, 5 sorts / 3 filters, grid URL-state diverged with no recorded deviation | **USER-RATIFIED 2026-07-12 — deviation RECORDED, spec §12.50(f).** The retired §5.1 elements stay **API capabilities** (`GET /v1/tokens` params, §12.22 formulas — nothing removed from API/indexer); header search unchanged. §5 amendment note + a §5.1 supersession note point at (f). Consequences (in flight at ratification): DISC-1/DISC-3 amendment in `docs/user-flows.md` + dead-code removal (`king-of-the-hill-hero` widget, dormant grid URL-state + test, unused KotH client leg) | spec §12.50(f), §5/§5.1 notes. Owner: **robbed-frontend** executes; architect recorded |
| **OG** | **OG rendering relocated web → API** (Cloudflare Worker 3 MiB free-plan size limit; commit `9528121`) | **RECORDED — spec §12.53 (new entry); §12.45 superseded on its "WASM raster in the web OG route" point** (the workerd no-N-API constraint holds, satisfied by relocation). `GET /v1/og/{address}.png`, native satori + `@resvg/resvg-js` in `apps/api/src/og/render.ts`, R2-cached `og/{address}/{version}.png`, version-hash ETag/304; web `<meta og:image>` points at the API URL — single renderer, anti-drift. §13 web-7 satori-under-Bun leg annotated (now an API-runtime concern) | spec §12.53 + §12.45 note + §13 web-6/7 row. web.md §6/§9 rewrite: **robbed-frontend** (in flight) |
| **OI-11** | **Confirmation rework LANDED — read-derivation is the chosen sanctioned §12.48c variant** | **RECORDED 2026-07-12.** No per-row `event_confirmations` join table; sidecar = `confirmation_watermarks` singleton (indexer `migrations/0002`) + shared `stateForBlock` derived at read time (`packages/shared/src/confirmation.ts`; `apps/indexer/src/confirmation.ts`; `apps/api/src/lib/confirmation.ts` `confirmationStateSql`/`projectConfirmation`; boundary agreement asserted by `apps/api/test/confirmation-derivation.test.ts`). Rationale consolidated into spec §12.48c from §11 above + the code headers: zero write-back ⇒ nothing reverts/drifts, monotonicity structural, per-row join = O(rows) writes for a value determined by two integers. §12.20 O(1) watermark broadcast unchanged; "materialized-column REST read" satisfied by in-SELECT derivation (row shapes unchanged). Stale "under rework (in flight)" wording replaced in §12.48c + §13 | spec §12.48c (rework-landed record), §13 OI-11 row; §11 above (evidence + dated update) |
| **D-2** | **Deployment-artifact path** — plan T-3/G-7 said `tools/deployments/testnet.json`; the implemented pipeline writes `contracts/deployments/<chainId>.json` | **RULED 2026-07-12 — canonical path = `contracts/deployments/<chainId>.json`** (matches `Deploy.s.sol` + `emit-testnet-env.ts`; testnet artifact = `46630.json` at T-3, mainnet = `4663.json` at Phase B). On disk today: `31337.json` (local anvil) and a `4663.json` from a **mainnet-fork pipeline run** (anvil dev accounts — NOT a live mainnet deploy; treat as fork evidence only). Supersedes the `tools/deployments/{testnet,mainnet}.json` wording in spec §12.49 (annotated), decisions §10 table above (historical, not rewritten), and downstream docs. Re-pointing routed: implementation-plan G-7/T-3 → `/goal`; `runbooks/testnet.md` + `plans/contracts-plan.md` → robbed-contracts; `runbooks/docker.md` → robbed-indexer; `runbooks/environments.md` → architect follow-up (outside this wave's edit scope) | spec §12.49 (annotated); `contracts/deployments/`. Owner: routed per consumer |
| **D-3** | **G-9 wording** — demanded Dockerfiles for indexer/api/**web**, but web ships as Cloudflare Workers per §12.45 | **RULED 2026-07-12 — G-9 amended to:** "container images for **indexer + api** (Komodo Stack) + **OpenNext/Cloudflare Workers build for web** per §12.45" — no web Dockerfile exists or should (consistent with §8 P-3 guidance above and `runbooks/prod-images.md`). Verify evidence: `docker build` exit 0 for indexer+api images; `opennextjs-cloudflare build` succeeds for web. Plan text amended by `/goal` | implementation-plan G-9 (via `/goal`); prod-images.md unchanged (already correct) |
| **D-4** | **M2-13 / M2-14 verify clauses** — internal dashboard endpoints (flow-quality organic %; competitor snapshots) don't exist: build or amend? | **RULED 2026-07-12 — BUILD BOTH (recommendation ratified; verify clauses stand as written, not amended).** Two thin **read-only internal** endpoints in `apps/api`: (1) M2-13 `GET /internal/flow/:address` — organic-holder range, cluster stats, botFlags summary (reuse `apps/api/src/projections/trust.ts` internals); (2) M2-14 `GET /internal/competitor-snapshots` — paged, newest-first read of `competitor_snapshots`. Both feed **Gate G-A.1/G-A.2** (§14) — exactly the data the gate consumes. Constraints binding on implementation: admin-SIWE-gated or internal-network-only; documented in `openapi.yaml`; **§8.5 advisory-only** (never gates chain state); **§2 discipline** — snapshot rows carry source + timestamp, no hardcoded market metrics; organic % displayed as a range, never false precision (§5.2). If no snapshot source is configured, record "manual until configured" in the plan row (goal-plan W3.4) rather than leaving it implicit | Owner: **robbed-indexer**, Wave 2 (W3). api.md/openapi to be updated by owner; Gate G-A inputs §14 |

Follow-up wave rows (2026-07-12, after robbed-frontend's §12.50(f) execution landed — apps/web 217/217, `e2e-coverage` 44/44 exit 0, typecheck + build clean):

| ID | Decision | Disposition | Where / owner |
|---|---|---|---|
| **CAT-f** | **§12.50(f) catalog-amendment ratification** — DISC-1 (TRENDING carousel + tape paint, 3-layer), DISC-3 (tape seed / tab filter / registry metrics / navigate, 3-layer), DISC-4 step 3 (creator click → `/?q=` deep link seeding the header SearchBox), §6 §5.1 traceability table | **RATIFIED 2026-07-12** — verified against spec §12.50(f) **and the shipped code**, not the amender's claims: `DiscoverView.tsx` (isolated `Promise.allSettled` fetches; API-owned `volume24h` order, §12.22; `newest` seed + merged registry), `TrendingCarousel.tsx` (image · #rank · name/ticker · 24h Δ% cards → `/t/[address]`), `event-tape/model/events.ts` (real-LAUNCH seed; registry-by-reference mcap/Δ% with "—" fallback, §2; ALL/LAUNCHES/TRADES/GRADUATIONS view-local filter; `prependCapped` 60-buffer), `AppHeader.tsx` (`?q=` `useSearchParams` seeding). Stable IDs retained; both DISC rows full three-layer; no waiver change; 44-flow baseline unchanged | `docs/user-flows.md` header (`RATIFIED-BY: robbed-architect DATE: 2026-07-12`) |
| **TD-12** | **OG catalog/e2e re-point after §12.53** — `e2e/flows/td-12.spec.ts` + `e2e/harness/selectors.ts` `routes.og` + the TD-12 catalog row still assert the REMOVED web route `/t/[address]/opengraph-image` (blocks the G-5 Playwright run) | **RULED 2026-07-12 — KEEP TD-12 (stable-ID rule; the §5.2 "viral share unit" flow is unchanged in substance), re-point the PNG-contract leg at `GET {API_ORIGIN}/v1/og/{address}.png`** — the e2e stack serves the API, so the 1200×630 `image/png` + unknown-token-404 assertions still run end-to-end against the real renderer; the web-side leg becomes "SSR `generateMetadata` emits `og:image` as the ABSOLUTE API URL" (`apps/web/src/views/token-detail/model/metadata.ts` — verified shipped). Layers (indexed · UI) and waiver unchanged. Catalog row annotated in place by the ratifier; substantive row rewrite + spec/selector re-point → robbed-frontend | `docs/user-flows.md` TD-12 note; spec §12.53. Owner: **robbed-frontend** (implementation) |
| **DISC-2** | **Tape cap "~30" vs shipped 60** — catalog said in-memory cap ~30; shipped shared tape buffer = 60 (`event-tape/model/events.ts` `prependCapped(cap = 60)`) | **RULED 2026-07-12 — the cap is DESCRIPTIVE, not normative:** a client memory-buffer perf knob with no spec §5.1/§12.50 value behind it; freezing it in the catalog would turn a perf tweak into a catalog violation. Row corrected to "~60 (shared tape buffer)" during the ratification pass; **no code change routed.** If buffer size ever becomes product-meaningful, that is a new decision, not this row | `docs/user-flows.md` DISC-2 step 1 (corrected by ratifier) |

W3 cross-service rulings (2026-07-12, routed from the completed robbed-indexer W3 work per development-flow §6):

| ID | Decision | Disposition | Where / owner |
|---|---|---|---|
| **INT-DTO** | **Internal-endpoint composite DTO placement** — `{ token, organic, flagged }` (`GET /internal/flow/:address`) and `{ snapshots, nextCursor }` (`GET /internal/competitor-snapshots`) typed API-locally in `apps/api/src/routes/internal.ts` from shared primitives (`organicFlowSchema`, `BotFlag`, `CompetitorSnapshotRow`); api.md §3.7 routed the call rather than self-resolving | **BLESSED 2026-07-12 — API-LOCAL stands; NO ratification into `packages/shared`, no robbed-shared work.** The anti-drift test is **≥2 consumers**: these composites have exactly one (the admin-SIWE internal surface — never the public frontend), so they fail promotion exactly as §12.40c (vendor interfaces API-only) and §12.40f (`moderation_audit_log` API-local) did; the shared *primitives* are imported, never redeclared, so nothing exists to drift. Promoting single-consumer composition would dilute the shared-package boundary for zero protection. **Promotion trigger recorded:** if a second consumer materializes (e.g. a web admin UI consuming `/internal/*`), robbed-shared ratifies `internalFlowResponseSchema`/`competitorSnapshotsResponseSchema` into `api-types.ts` **then** — standard promotion path, new decision row at that time | api.md §3.7 (disposition settled — owner may drop the "or robbed-shared may ratify" alternative); `apps/api/src/routes/internal.ts`. No owner action now |
| **ORG-%** | **Organic-volume % point vs range** — §5.2 mandates the organic-HOLDER estimate as a RANGE; §8.5's outputs line + the ratified `organicFlowSchema` carry organic-VOLUME % (`volumePct`, `flaggedClusterVolPct24h`) as point values; W3 exposed what exists rather than fabricating a range | **RULED 2026-07-12 — the split IS the intended reading of §5.2/§8.5; NO shared-schema or indexer-job change scheduled.** §5.2's "displayed as a range, never false precision" attaches to the **holder** estimate specifically — an inference about *identity* (how many holders are human), inherently bounded-not-known, hence a range. The volume percentages are **arithmetic shares of observed, indexed volume with respect to the flag set** (wash exclusion is a deterministic rule, §8.5 heuristic 4): given the flags, the share is exact — the uncertainty lives in the flag heuristics and is disclosed via the mandatory methodology tooltip (§5.2) / `methodology` schema field. Fabricating volume error bars with no estimator behind them would be false precision of the opposite kind. §2.2's ≤50% organic discount is a *market-sizing planning assumption* for headline data, not this per-token display. Spec §8.5 outputs line annotated to kill the ambiguity | spec §8.5 (annotation, 2026-07-12); `packages/shared/src/api-types.ts` `organicFlowSchema` unchanged |

---

## Summary

**Decided 2026-07-09 (spec §12.11–§12.24):** O-1, O-2, O-3, O-7 (mechanism), O-8 (budget rule), O-11, OI-1, OI-2, OI-3, OI-4, OI-5, OI-7, OI-9, OI-10, OI-12, OI-A1, OI-A2, OI-A3, OI-A5, OI-A6, OI-A9, web-1/2/3/4/5/8/9, web-12 (shadcn/ui, §12.24).

**Decided 2026-07-09, ratification pass (spec §12.25–§12.31):** C-1/UM-1 pull-payment (§12.25), graduation fee cost-based (§12.26), anti-sniper freeze + deferred redesign (§12.27), V3 addresses on 4663 / O-4 / OI-13 / E-1 (§12.28), pnpm workspace (§12.29), name/symbol byte limits (§12.30), metadata `version` freeze (§12.31).

**Decided 2026-07-10, M0-3 pass (spec §12.32–§12.35) — M0 defaults, tunable in the capped beta:** anti-sniper values O-7 (§12.32), arb-back param values O-8 (§12.33; WETH-leg budget *definition* still M1), graduation caller reward O-9 (§12.34; re-validate ≥10× gas at M1), large-value-threshold M0 input web-10 (§12.35; threshold value still open → M3-10).

**Recorded 2026-07-10, M0-4 pass (spec §12.36) — SPLIT disposition:** governance values computed by M0 into `constants.json.governance`. Cluster-alert thresholds (`perTokenPct 25` / `platformPct 10` / `windowHours 24`) are tunable engineering defaults → hoodpad-security tunes before beta (gate 7). **The organic-volume floor (`N = 5` graduations-equiv / 7d, Gate G-A.1) is NOT settled — it is a product/policy go/no-go input that STAYS OPEN in §13, requiring architect + effectively user ratification before Gate G-A and recalibration at M2.**

**Decided 2026-07-10, M3-10 / M2-3 Phase-P pass (spec §12.47–§12.48):** large-value disclosure threshold web-10 (§12.47 = `1.0 ETH`, config, retunable in beta), and the M2-start §13 checks OI-6 (§12.48a, config-driven Chainlink-else-fallback — **verification env-gated on live RPC**), OI-8 (§12.48b, tags-else-L1-watermark — **verification env-gated on live RPC**), OI-11 (§12.48c, sidecar default — version-gated).

**Recorded 2026-07-11 (spec §12.50 + M3-11):** four-page redesign deviations recorded (Portfolio → v1 read-only, `/launch`→`/create`, terminal-mono skin, mobile-first; protocol rules unchanged); user-flow catalog (36 flows) + waivers RATIFIED as amended; `PORT-*` Portfolio flows opened as a REQUIRED catalog addendum — **subsequently authored + RATIFIED the same day (§13 above): PORT-1..8 + 8 waiver rows, 44-flow I-5a coverage baseline, advisory-read semantics ratified (no portfolio WS channel in v1)**. M3-10 verify completed: API mirror key `LARGE_VALUE_ETH_THRESHOLD` added to `apps/api/src/config.ts`.

**Verified 2026-07-11, M2-0b + M2-3 pass (§11 above):** M2-0b same-tx factory-child capture VERIFIED native on ponder 0.16.8 (source + anvil/PGlite spike) — §12.41 receipt fallback not needed; OI-6 Chainlink ETH/USD feed EXISTS on 4663 (proxy `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9`, on-chain verified — architect to record the address in §12 and close §13 OI-6); OI-8 `safe`/`finalized` tags SUPPORTED on the public 4663 RPC (M2-3b not funded; architect to close §13 OI-8); OI-11 direct in-column UPDATE NOT tolerated by ponder 0.16.8 for hot rows ⇒ §12.48c sidecar REQUIRED — current direct-UPDATE confirmation materialization in `apps/indexer` is a recorded mismatch, rework to sidecar is a robbed-indexer M2 follow-up.

**Recorded 2026-07-11, architect pass over the above:** spec **§12.51** records the Chainlink ETH/USD addresses + mandatory startup assertions (§13 OI-6 CLOSED); §12.48b annotated with the tag-support verification (§13 OI-8 CLOSED; M2-3b flipped N/A, dormant); §12.48c annotated — **sidecar MANDATORY** on ponder 0.16.8 (§13 OI-11 annotated; `apps/indexer` rework in flight); §12.41 annotated — spike PASSED, receipt fallback retired/dormant, **§12.15 emission-order invariant recorded as a contracts-side must-stay-true** (`TokenCreated` before the initial-buy `Trade` in the same tx; owner robbed-contracts). Plan items **M2-0b / M2-3 / M2-3b** flipped done (2026-07-11).

**Decided 2026-07-11, Phase-T inventory pass (spec §12.52):** testnet 46630 external-contract set — WETH/Safe v1.4.1/Multicall3 CONFIRMED canonical (live-verified); **Uniswap V3 on testnet = adopted Blockscout-verified community deployment (option i), TESTNET-ONLY** with mandatory fail-closed deploy assertions + explicit trust caveat (factory owner EOA; mainnet stays §12.28, LOCAL mock/fork); faucet mechanics confirmed (0.05 ETH + 5 stock tokens / 24h; Chainlink/QuickNode fallbacks) — §13 faucet leg closed; testnet Blockscout verifier supports solc 0.8.35 no-key (O-5 check itself unchanged). `external.testnet.json` fixture fill deferred (fixture not yet authored).

**Decided/recorded 2026-07-12, Wave-1 goal-completion pass (§15 above):** D-1 Discover-surface deviation USER-RATIFIED + recorded (spec §12.50(f) — shipped Discover = TRENDING carousel + event tape; KotH/grid/sorts/filters/URL-state retired from the page, retained as API capabilities; DISC-1/DISC-3 amendment + dead-code removal with robbed-frontend); OG relocation web→API recorded (spec §12.53; §12.45 WASM-raster point superseded; `GET /v1/og/{address}.png`, native satori+resvg, R2-cached); OI-11 confirmation rework LANDED as the **read-derivation** variant (spec §12.48c updated — `confirmation_watermarks` singleton + shared `stateForBlock` at read time, no per-row join table); D-2 deploy-artifact path canonicalized to `contracts/deployments/<chainId>.json` (spec §12.49 annotated); D-3 G-9 amended to container-images-for-indexer/api + OpenNext-Workers-build-for-web; D-4 ruled BUILD BOTH internal endpoints (M2-13 flow-quality + M2-14 competitor-snapshots → robbed-indexer, Wave 2). **Follow-up wave same day (§15 second table):** §12.50(f) catalog amendment (DISC-1/DISC-3/DISC-4 + §6 table) RATIFIED against the shipped code; TD-12 ruled re-pointed to the §12.53 API OG URL (`GET {API_ORIGIN}/v1/og/{address}.png`; implementation → robbed-frontend); DISC-2 tape cap ruled descriptive, row corrected to the shipped ~60. **W3 cross-service rulings (§15 third table):** internal-endpoint composite DTOs BLESSED API-local (single consumer, §12.40c/f precedent; promotion trigger recorded); organic-% representation split ruled intended — holder estimate = range (§5.2), volume % = point share w.r.t. the flag set (spec §8.5 outputs line annotated; no schema/job change).

**Open (spec §13, with owners):** O-5 (compiler pin + `cancun` — contracts, pre-deploy), O-6/OI-A8 (Safe + signer set + admin allowlist — architect/ops), O-10 (beta caps — security, pre-beta), OI-A7 (moderation vendor — architect/ops), web-6/7 (M3-start runtime checks — frontend), organic-volume floor magnitude (Gate G-A.1 — architect + user, recalibrate M2), plus domain + logo/brand-mark treatment (project **name** RESOLVED §12.46) and legal/ToS (blocking at Gate G-A). **O-4/OI-13 (V3 addresses) CLOSED (§12.28). O-7 values / O-8 param-values / O-9 CLOSED (§12.32–34). O-8 WETH-leg budget *definition* CLOSED (§12.33 update, 2026-07-11 — §13 above; gate-6 quantification + UM-2 Part-2 residual stay caps-lift M4). Project name CLOSED (§12.46). web-10 CLOSED (§12.47). OI-6 CLOSED (§12.48a disposition + §12.51 verified addresses/assertions, 2026-07-11). OI-8 CLOSED (§12.48b, tags verified SUPPORTED 2026-07-11; M2-3b dormant/not funded). OI-11 CLOSED (§12.48c — version gate resolved 2026-07-11: sidecar MANDATORY on ponder 0.16.8). Robinhood testnet parameters CLOSED (§12.49 chain params + §12.52 faucet mechanics / external-contract set, 2026-07-11). `PORT-*` catalog addendum RATIFIED + advisory-read semantics RATIFIED (2026-07-11, §13 above).**

**Delegated:** OI-A4 (queue tech — hoodpad-indexer).
