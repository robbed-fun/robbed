# SPEC — Pump.fun-Style Launchpad on Robinhood Chain

**Codename:** `ROBBED_` (working title, TBD)
**Status:** Specification v1.2 — incorporates external review + on-chain reconnaissance 2026-07-09 — no implementation
**Stack:** TypeScript + Bun · Solidity (pinned, §6.7) + Foundry + OpenZeppelin v5

**v1.2 changelog (addendum 2026-07-09, no architecture change):** adds §2.2 on-chain adversary profile (observed bot-fleet + gas-funder-farm activity; binding organic-flow discount ≤50%, day-1-users-are-bots, product-correct-under-bot-dominant-flow); §3 amended (hood.fun = mainnet incumbent, bot-operator trader cohort); §5.2 Trust panel gains organic metrics; §8.5 indexer bot/farm heuristics; §10 gates 6/7/10 parameterized against observed patterns; §11 M2/M3 scope adds + **M1 hard timebox** (V3Migrator fuzz/invariant by end of week 1 of M1 else documented V2+burn fallback the same day); §14 two-phase project framing + **Gate G-A** mainnet go/no-go; §13 open-items amended (legal → blocking at G-A; organic-volume floor + cluster thresholds → M0; weekly hood.fun snapshot). §12 decisions unchanged.

**v1.1 changelog:** graduation venue V2+burn → **V3 + locked fee vault (Option B)**; anti-sniper rewritten for ArbSys/timestamp (block.number unreliable on Orbit); pre-seeded-pool attack mitigated; pause split into granular flags; Safe replaces bespoke multisig; compiler pinned exactly; confirmation-state semantics added; Trust panel + on-chain metadata hash added; security program hardened with capped beta; volatile market metrics date-stamped or removed.

---

## 1. Thesis

Robinhood Chain launched July 1, 2026 and is in a memecoin discovery phase (CASHCAT et al., as of 2026-07-09 — treat all market metrics as snapshots, pull live from DefiLlama/Dune in any material). Pump.fun routes cross-chain flow *into* the chain but creates nothing native. One native competitor (hood.fun) launched July 9: bonding curve → Uniswap V3 1% → permanent LP lock.

The product is **soft-confirmed trading UX**: ~100ms blocks with FCFS sequencing mean a trade can be reflected in the UI in well under a second — but this is an AMM/bonding curve with soft confirmations, not a real-time order book, and the spec never claims otherwise. We win on perceived speed, trust transparency, and a tighter three-page product.

## 2. Chain Facts (verified against official docs, 2026-07-09)

| Property | Value |
|---|---|
| Type | Permissionless L2, Arbitrum (Orbit) stack, optimistic rollup settling to Ethereum |
| Chain ID | 4663 |
| Gas token | ETH |
| Block time | ~100 ms target (marketing figure; UX must still distinguish confirmation tiers, §2.1) |
| Sequencing | Single Robinhood-operated sequencer, **FCFS; priority fees do not jump the queue** (official docs) |
| `block.number` | **Returns an L1 estimate, not the L2 block count** (Orbit behavior). Use `ArbSys(0x64).arbBlockNumber()` or timestamps for any block-based logic |
| EVM tooling | Foundry, Hardhat, viem, wagmi unmodified; ERC-4337 first-class (docs-confirmed) |
| WETH | `0x0Bd7D308f8E1639FAb988df18A8011f41EAcAD73` (official docs) |
| Uniswap | v2, v3, v4, UniswapX live. **v2 Factory `0x8bceaa40b9acdfaedf85adf4ff01f5ad6517937f`, v2 Router02 `0x89e5db8b5aa49aa85ac63f691524311aeb649eba`** (official Uniswap deployments). v3/v4 addresses: pull from official registry at implementation time |
| Explorer | Blockscout (robinhoodchain.blockscout.com) — contract verification constraints drive compiler pin (§6.7) |
| Market metrics | TVL / addresses / volume / token performance: **never hardcode in product copy or docs; cite source + timestamp or query live** |

### 2.1 Confirmation semantics (product-wide)
Three explicit states, tracked by the indexer and surfaced in UI where it matters:
1. **Soft-confirmed** — included by the sequencer (sub-second). Default UI state for trades.
2. **Posted to Ethereum** — batch posted to L1.
3. **Finalized** — L1 finality / challenge-period considerations for withdrawals.
Trading UX runs on (1); bridge/withdrawal flows and any large-value displays disclose (2)/(3).

### 2.2 On-chain adversary profile (new, v1.2)

Documented, observed activity on mainnet as of 2026-07-09 (source: manual tx-level analysis; re-verify at M2 with own indexer, §8.5):

| Pattern | Observation | Implication |
|---|---|---|
| Sniper/arb executor | Unverified proxy contract (`0x65050a…`) accounting for ~⅓ of sampled swaps; atomically pulls WETH from multiple pools in the same second; invoked by dozens of EOAs | Professional bot operation with shared execution infra is live on-chain. Anti-sniper guard (§6.5) faces a real, deployed adversary — not a theoretical one |
| Gas-funder farm | EOA (`0x1887FA…`, ~1084 ETH) drip-funding ~0.0004 ETH to fresh addresses every 1–2 min | Multi-wallet fleets (hundreds+) exist and are pre-funded. Per-wallet early-buy caps WILL be bypassed via wallet rotation; guard blunts single-tx sweeps only (already acknowledged in §6.5 — now confirmed empirically) |
| Programmatic flow share | >50% of sampled swaps have `sender ≠ recipient` (contract-mediated execution) | A majority of visible "trading" is programmatic. Chain-level activity metrics (DAW, volume, holder counts) are inflated and MUST NOT be used for revenue projections without an organic-flow discount |

**Consequences (binding planning assumptions):**
1. All market-sizing and revenue estimates in any planning doc carry an explicit **organic-flow discount** (default: assume **≤50% of headline DEX volume is organic** until own indexer data says otherwise).
2. **Day-1 users of the platform are assumed to be bots.** Product must be **correct and profitable under bot-dominant flow** (fee capture is actor-agnostic — the pull-payment curve fee §12.25 accrues regardless of who trades) while protecting human UX (creator atomic buy §5.3, early-buy caps §6.5).
3. Gate 6 (economic red-team, §10) simulations are parameterized against the observed patterns above: multi-wallet sniping from a shared funder, same-second multi-pool exits, wash-loop volume.

## 3. Competitive Landscape

| Player | Model | Threat |
|---|---|---|
| **hood.fun** (**mainnet 2026-07-09**) | Curve → Uniswap V3 1% → permanent LP lock (per their PR) | **Incumbent, not "same-day peer" (v1.2).** Same venue mechanics we adopt. Differentiation = UX speed, trust transparency (incl. organic-flow metrics §5.2/§8.5), later creator fees. **Standing task:** weekly snapshot of hood.fun traction (tokens created/day, graduation count, visible volume) via own indexer or Dune — feeds Gate G-A (§14) |
| **Pump.fun cross-chain** | Routes SOL trades into existing Robinhood Chain tokens; no native creation | Captures flow, validates demand |
| **Bot operators** (v1.2) | Shared-infra sniper/arb executors + gas-funder farms (§2.2) | Not a competitor for creators, but the dominant **trader** cohort near-term. They generate real curve fee revenue **and** degrade human trader experience. Neutral-to-positive for revenue, negative for retention; addressed via Trust panel (§5.2) — organic-flow transparency, not futile suppression |
| **Uniswap v2/v3/v4** | Venue, not competitor | — |

## 4. Template Analysis

Template repositories:
- Gnad.fun (primary): https://github.com/HarrierOnChain/Gnad.fun-SmartContract
- IDOFactory / TokenLocker reference: https://github.com/VitaliyShulik/launchpad
- Minimal-surface reference: https://github.com/DePayFi/depay-evm-launchpad
- (Scaffolding CLI, ignored): https://github.com/0xPolygon/dapp-launchpad

### 4.1 Gnad.fun-SmartContract — primary architecture template
**Take:** Factory → Curve → Token pattern; router as single entrypoint with slippage/deadline; virtual-reserve constant product (`k = virtualEth × virtualToken`); locked LP token tranche; custom errors; event taxonomy; Foundry test structure.
**Drop / change:**
- `WMon` → canonical WETH `0x0Bd7…AD73`
- Caller-supplied fee validated via `checkFee` → **fee computed in-contract** (griefing/footgun surface)
- V2 graduation → **V3 migrator + fee vault** (§6.3)
- Bespoke multisig FeeVault → **Safe** (§6.6)
- `UNLICENSED`, `^0.8.13` → MIT, exact pin (§6.7)
- Add OZ v5 throughout: SafeERC20, ReentrancyGuard, Ownable2Step, granular pause flags

### 4.2 DePayFi/depay-evm-launchpad — benchmark for minimal audit surface only.
### 4.3 VitaliyShulik/launchpad — `TokenLocker` pattern is reference material for the LP fee vault's "principal can never leave" property.
### 4.4 0xPolygon/dapp-launchpad — scaffolding CLI; ignore.

## 5. Product Spec — Three Pages

> **Amended by §12.50 (2026-07-11):** v1 ships **four** pages — the redesign adds a read-only **Portfolio `/portfolio`** (promoted from §5.4) and renames `/launch` → **`/create`**. No new transaction types; all §5.1–5.3 feature content below is unchanged.

Dark, dense, fast. Optimistic UI reconciled by WebSocket events; every trade renders as soft-confirmed and reconciles on indexed event.

### 5.1 Discover (`/`)
- King of the Hill hero (closest to graduation, volume-weighted)
- Live launch ticker (WebSocket)
- Token grid: sorts = trending / newest / mcap / 24h volume / progress; filters = pre-grad / graduated / all
- Search: name, ticker, contract, creator (Postgres `pg_trgm`)
- Card: image, name, ticker, mcap, progress bar, 24h Δ%, creator, age

### 5.2 Token Detail (`/t/[address]`)
- Live candles (`lightweight-charts`, 1s→1h) from indexed trades; **venue-continuous** across graduation (curve trades + V3 `Swap` events in one series)
- Buy/Sell widget: curve quote pre-grad; post-grad routes to Uniswap V3 (invisible venue switch); slippage default 2%, deadline
- **Trust panel (new, first-class):** ownerless token ✓ · fixed 1B supply ✓ · live curve reserves (ETH/token, read on-chain) · graduation threshold + progress · LP destination: **"LP principal permanently locked; trading fees claimable by treasury."** (the canonical sentence, §12.14 — explanatory sub-copy may mention the immutable vault, but the quoted string is the one constant) · fee policy (1% curve fee → treasury) · metadata content hash vs on-chain commitment (§8.3)
- **Organic-flow metrics (new, v1.2 — sourced entirely from own indexer §8.5, no new on-chain surface):**
  - **Organic holder estimate** — % of holders NOT flagged by bot heuristics (§8.5), with a methodology tooltip; displayed as a **range**, never false precision
  - **Flow quality** — share of curve volume from flagged clusters over trailing 24h
  - **Funding-cluster flag on the holder list** — top-20 holders sharing a gas-funding source are visually grouped
  Rationale: on a chain where >50% of flow is programmatic (§2.2), "how many holders are real" is a question no competitor (pump.fun, hood.fun) answers — cheapest available differentiation, reusing infrastructure (Ponder → Postgres) that exists anyway
- Live trade feed (soft-confirmed badge), holder distribution (top 20; creator/curve/vault flagged)
- Token info, Blockscout links, creator profile
- SSR + per-token OG image (chart snapshot + mcap + progress) — the viral share unit

### 5.3 Launch Flow (`/launch`) *(route renamed `/create`, §12.50)*
- Form: name, ticker ≤10, description ≤500, image required (≤4MB, re-encoded), optional links
- Image uploaded **through the API** (MIME sniff + re-encode before storage, §8.4), stored to R2 — presign lives on the API→R2 leg only (§12.19); metadata JSON server-canonicalized, client re-verifies the hash before signing → `keccak256` hash emitted on-chain in `TokenCreated` (§8.3)
- Optional atomic initial creator buy (anti-self-snipe)
- One tx: `Router.createToken{value: deployFee + initialBuy}`; tradeable in <1s (soft-confirmed)
- Economics displayed plainly, including exact LP copy: **"LP principal permanently locked; trading fees claimable by treasury."** Never "burned."

### 5.4 Phase 2 (out of v1)
~~Portfolio page (schema ready day 1)~~ **(moved into v1 as read-only `/portfolio`, §12.50)** · creator fee share (§7) · comments · ERC-4337 gasless · livestreams.

## 6. Smart Contract Architecture

Foundry workspace, immutable contracts (no proxies), upgrade = new factory version.

```
contracts/
├── LaunchToken.sol     // OZ ERC20 + ERC20Permit, fixed 1B, ownerless
├── CurveFactory.sol    // deploys token+curve, global config, hard caps
├── BondingCurve.sol    // virtual-reserve constant product
├── Router.sol          // create/buy/sell entrypoint, fees, guards
├── V3Migrator.sol      // graduation: pool init/verify, mint LP, send NFT to vault
├── LPFeeVault.sol      // immutable: holds LP NFTs forever, collect()-only to treasury
└── interfaces/, errors/, libs/
```

### 6.1 LaunchToken.sol
OZ ERC20 + ERC20Permit, 18 decimals, 1,000,000,000 minted once to curve in constructor. No mint/burn/owner/hooks/taxes/blacklist. Constructor stores `metadataHash` (bytes32) — immutable on-chain commitment to the canonical metadata JSON.

### 6.2 BondingCurve.sol
- Virtual-reserve constant product (Gnad math, hardened): buy `tokensOut = virtualToken − k/(virtualEth + ethIn)`; sell inverse; 1% ETH-leg fee **accrued in-contract** (never pushed to treasury on any trade path — withdrawn by the permissionless pull-payment `sweepFees()`, §12.25) before curve math
- Only Router may call
- Graduation trigger: **net-of-fee** `realEthReserves` reaches `GRADUATION_ETH` (§12.11 — the final buy is clamped to land exactly on the threshold, excess refunded) → curve locks **both buys and sells** (a deterministic, permissionlessly-exitable protocol state, not a pause — §12.12); permissionless `graduate()` (small caller reward) invokes V3Migrator

### 6.3 Graduation — Option B: Uniswap V3 (1% tier) + locked fee vault
1. Deduct flat graduation fee → treasury (Safe)
2. **Pool lifecycle & pre-seed defense:** the token/WETH **V3 pool (1% tier) is created and initialized at token-creation time** at the deterministic graduation price. At graduation, migrator reads `slot0`; if price was polluted (donations/swaps into the near-empty pool), the migrator **arbs price back to target using curve inventory before minting** (bounded loop, reverts if unachievable within tolerance) — never mints into a hostile ratio. Fuzz/invariant tests must cover donation, sync-style, and swap griefing on the pre-graduation pool.
3. Mint **full-range V3 position** with `LP_TOKEN_TRANCHE` + raised ETH (as WETH), amount-mins enforced
4. LP NFT transferred to **LPFeeVault**: immutable, no owner, no `withdraw`; sole external function `collect(tokenId)` sends accrued fees to a fixed treasury address set at deploy. Principal mathematically cannot leave.
5. Residual **token** dust → `0x…dEaD` ("burned"; LaunchToken has no burn fn); residual **WETH** dust → treasury (§12.13); `Graduated` emitted
**Fallback (documented, not default):** V2 + LP burn (original Option A) if V3 migrator complexity threatens timeline — with copy switched back to "LP burned forever," since burn and fee capture are mutually exclusive claims.

### 6.4 Economics (pump.fun parity, ETH-denominated)
Factory constants, tuned at deploy via parameter notebook (M0); existing curves immutable.

| Parameter | Target | Note |
|---|---|---|
| Total supply | 1,000,000,000 | fixed |
| Sold on curve | ~793.1M (79.31%) | pump.fun ratio |
| LP tranche | ~206.9M (20.69%) | minted to V3 at graduation |
| Graduation mcap | ≈ $69k equivalent | ETH figure computed at deploy — no hardcoded ETH/USD in spec |
| Trade fee | 1% ETH leg, both directions → treasury via pull-payment `sweepFees` (§12.25) | hard cap in code: ≤2% |
| Creation fee | ~$1–2 equivalent flat → treasury | spam resistance |
| Graduation fee | **small flat, cost-based** (≈ V3-migration gas + thin margin) → treasury | exact number computed at M1 vs real testnet gas; carried as formula/placeholder in M0 constants, never a hardcoded USD figure — NOT a %-of-raise (§12.26) |
| Post-graduation revenue | **V3 1% pool fees on our LP, claimable via LPFeeVault → treasury** | new in v1.1 |
| Creator reward | 0 in v1 | `creatorFeeBps` slot exists, no code path (§7) |

### 6.5 Router.sol
- `createToken(meta, metadataHash, minTokensOut) payable` (atomic create+buy), `buy`, `sell` (+permit variant); slippage + deadline on all; fees computed in-contract; `nonReentrant`, CEI
- **Anti-sniper guard (§12.18):** early-window per-tx buy cap using a **`block.timestamp` window** (deployment-constant seconds; e.g. first 5–10s post-create) — **never `block.number`** (L1 estimate on Orbit) and, per the §12.18 ruling, not `arbBlockNumber` either. Cap = `MAX_EARLY_BUY` ETH; blunts single-tx sweeps under FCFS, acknowledged bypassable via multi-wallet. A decaying + size-based early-buy-fee redesign is deferred to pre-caps-lift (§12.27); v1 ships this fixed mechanism
- **Granular pause flags (replaces single Pausable):**
  - `pauseCreates` — stop new launches
  - `pauseBuys` — stop curve buys
  - **sells always open** — no flag exists that can block curve sells (exit is a right, not a permission). This is a **no-pause-authority** guarantee: the deterministic `ReadyToGraduate` lock pending `graduate()` is not a pause (§12.12); and because fees accrue in-contract and are swept by `sweepFees()` (no trade path pushes ETH to the treasury), a hostile/reverting treasury cannot freeze sells either (§12.25)
  - **no pause authority of any kind post-graduation** — Uniswap is out of our control by design
- Hard-capped admin params (fee ceilings in code)

### 6.6 Treasury — Safe, not bespoke
- **Primary:** Gnosis Safe as treasury. Verify official Safe deployment on chain 4663 at implementation time; if absent, **deploy the canonical Safe contracts ourselves** (permissionlessly deployable, audited) rather than writing a custom multisig.
- Bespoke Gnad-style FeeVault: **dropped.** LPFeeVault (§6.3) remains custom but is ~50 lines with one function and no privileged paths — smallest possible novel surface.
- Admin (`Ownable2Step` on Factory/Router) = the Safe. Owner cannot touch live curves, existing token economics, or LPFeeVault.

### 6.7 Compiler & verification
- **Exact pin, single version, no ranges.** Candidate at time of writing: `0.8.35` — reviewer reports Blockscout verification failures on 0.8.36 for this chain. Confirm the pin against Robinhood Blockscout verification support at implementation start; whole workspace compiles on that one version.
- **EVM target = `cancun` (§12.44).** `evm_version = "cancun"` is the compile/deploy target: Robinhood ArbOS ≥ 32 supports it (ArbOS 20 "Atlas" added `MCOPY`), the Blockscout verifier lists `cancun`, and OZ v5.5's mandatory `ERC20Permit → ECDSA → EIP712 → Strings → Bytes` chain emits `MCOPY` so **pre-Cancun cannot compile**. Only `mcopy` (EIP-5656) is relied upon; **transient storage (`TSTORE`/`TLOAD`) remains deliberately unused** (storage-based `ReentrancyGuard`). Supersedes the earlier "do not assume Cancun features" wording; final `cancun`-target verification is folded into the O-5 pin check (§13).
- All contracts verified on Blockscout at deploy; repo public.

## 7. Creator Rewards (designed-in, disabled)
Treasury-only v1. `creatorFeeBps` field exists in fee config, hardcoded 0, no branching path. Phase 2 = new Router + pull-payment `CreatorVault`. Indexer/UI track `creator` per token from day 1.

## 8. Off-Chain Architecture

```
┌─ Next.js (App Router, Bun runtime) ── SSR + OG images (satori)
├─ Ponder (Node container) ── Factory/Curve/Router + V3 Pool events → Postgres
├─ Hono on Bun ── R2 uploads (API-mediated, §12.19), moderation queue, search API
├─ Postgres (+pg_trgm) · Redis (pub/sub → WS fanout)
└─ Cloudflare R2 + CDN ── images + canonical metadata JSON
```

- **Ponder** indexes **six event families** (§12.15–16): `TokenCreated(…, metadataHash, metadataUri)`, `Trade` (carries post-trade reserves + fee), `Graduated`, LaunchToken `Transfer` (sole source of holder-balance truth), plus **V3 `Swap`/`Collect`** on graduated pools (venue-continuous candles; treasury fee accrual dashboard). Candle rollups 1s·15s·1m·5m·15m·1h (§12.17).
- **Confirmation-state labels:** indexer records soft-confirmed → posted-to-L1 → finalized per event (§2.1); UI badges where relevant.
- WebSocket: Redis pub/sub → Bun WS; per-token + global channels; target <500ms event-to-browser via Alchemy WS RPC.

### 8.3 Metadata integrity (new)
R2 URLs are mutable; the chain commitment is not. Canonical metadata JSON is canonicalized (stable key order) → `keccak256` → emitted in `TokenCreated` and stored in the token. Indexer verifies fetched JSON against hash; Trust panel shows match/mismatch. Image integrity: image hash included inside the metadata JSON.

### 8.4 Moderation
Upload-time MIME sniff, size caps, re-encode. Auto-moderation (CSAM hash-matching vendor + NSFW/violence classifier) gates *listing*, never chain state. Impersonation flags for top-asset and Stock Token tickers. Admin can hide listings only.

### 8.5 Bot/farm detection heuristics (new, v1.2)

Implemented in **M2** as SQL views / scheduled jobs over the existing trade + transfer tables (no new event families). Heuristics (v1, tune with data):

1. **Funder clustering:** wallets whose first inbound tx is a micro-transfer (< 0.001 ETH) from an address that funded ≥ N (default 20) other wallets in a 24h window → cluster by funder; cluster inherits a `farm` flag.
2. **Wallet age vs. action:** address executes its first-ever buy < 60s after a token's `TokenCreated` and was funded < 1h prior → `sniper` flag.
3. **Contract-mediated execution:** trade where the `tx.origin`-equivalent ≠ token recipient (Router-external executors) → `programmatic` flag. **Whitelist our own Router/contracts** (they legitimately mediate).
4. **Wash-loop detection:** address pairs/clusters with round-trip buy-sell of similar size within short windows, netting ≈ fees only → `wash` flag on volume, **excluded from "organic volume" metrics**.
5. **Same-second multi-pool exits:** recipient receiving WETH from ≥3 pools in one block → `arb/exit` flag.

Outputs: per-address flag set; per-token organic-holder % and organic-volume %; internal flow-quality dashboard + Trust-panel feed (§5.2). **All heuristics are advisory (labeling only) — never gate chain interactions**, consistent with §8.4 (moderation gates listing, never chain state). Cluster-alert thresholds for Gate 7 are set in the M0 notebook (§13).

## 9. Frontend Stack
Next.js 16 + React 19 (exact majors, no ranges — §12.37) App Router on Bun · wagmi v2 + viem + RainbowKit (custom chain 4663, injected/WalletConnect/Robinhood Wallet) · TanStack Query + WS · `lightweight-charts` · Tailwind dark-first · satori OG · Playwright e2e on fork, Vitest units.

## 10. Security Program (hardened per review)

AI-assisted auditing alone is insufficient assurance for a public-funds launchpad. v1.1 posture: AI pipeline **plus** a hard-capped beta **plus** public bounty before meaningful volume, with an external checkpoint decision gate.

**Gates (all required):**
1. Static: Slither (zero unexplained), Aderyn, solhint, CI-enforced fmt
2. Foundry unit + fuzz + **invariants**: `k` non-decreasing from trades; curve solvency at any fill sequence (`balance ≥ realEthReserves`; any circulating sell payable); exact fee accounting; graduation single-fire and reachable; post-grad curve holds zero value; **pre-seeded/donated/swapped V3 pool cannot cause hostile-ratio mint**; no fuzzed actor sequence extracts ETH beyond fair curve value
3. Fork tests on live chain: full lifecycle vs real V3 factory/NPM, real WETH `0x0Bd7…AD73`
4. Mutation testing on curve + migrator math (suite must kill mutants)
5. Multi-model LLM audit (≥3 frontier models, adversarial prompts, per-contract + system pass; written findings register with dispositions)
6. Economic red-team on fork: sniper/sandwich/wash sims under FCFS; guard behavior verified via `arbBlockNumber`/timestamp. **(v1.2) Scenarios parameterized against the observed §2.2 patterns:** multi-wallet sniping from a shared gas-funder, same-second multi-pool exits, wash-loop volume — the multi-wallet-bypass acknowledgment (§6.5) is now empirically confirmed, so the sim quantifies its cost rather than treating it as hypothetical
7. **Capped beta (mandatory):** mainnet launch with global TVL cap and per-token caps enforced in Factory config, monitoring + alerting on invariant metrics, kill-switch = pause creates/buys only. **(v1.2) Add cluster monitoring:** alert when a single funding cluster (§8.5) exceeds X% of a token's curve volume or Y% of platform-wide volume (thresholds set in the M0 notebook, §13) — early-warning for metric distortion and coordinated dumps
8. **Public bug bounty live before caps lift** (e.g. 10% of at-risk funds, cap TBD); repo public from day 1
9. **Decision gate before caps lift:** if traction justifies it, commission a Sherlock/Code4rena-style contest or firm review at that point — budget line reserved, decision explicit, not deferred by default
10. Published known-risks doc: no-firm-audit disclosure, single-sequencer dependency, soft-confirmation semantics, centralized listing moderation. **(v1.2) Add:** "platform activity metrics may be materially influenced by automated trading; displayed organic estimates are heuristic (§8.5)."

## 11. Milestones

| # | Milestone | Contents |
|---|---|---|
| 0 | Parameter notebook | curve constants from live ETH/USD; price/mcap plots; V3 tick math for graduation price. **(v1.2)** organic-volume floor + cluster-alert thresholds (§13) |
| 1 | Contracts + gates 1–4 | incl. V3Migrator + LPFeeVault; Robinhood Chain testnet deploy. **(v1.2) Hard timebox — see below** |
| 2 | Indexer + API | Ponder, V3 events, candles, WS, confirmation states, search. **(v1.2)** §8.5 bot/farm heuristics + internal flow-quality dashboard |
| 3 | Frontend | 3 pages + Trust panel vs testnet. **(v1.2)** Trust-panel organic metrics (§5.2) |
| 4 | Gates 5–8 | LLM register, red-team, capped beta, bounty |
| 5 | Caps lift | gate-9 decision executed; OG loop tuned |
| P2 | Portfolio · creator fees · 4337 | separate specs |

**M1 hard timebox (new, v1.2).** If **V3Migrator** (incl. the pre-seed defense) does **not** pass fuzz/invariant gates by **end of week 1 of M1**, switch to the documented fallback (**V2 + LP burn**, §6.3) **the same day**. Rationale: hood.fun incumbency makes calendar time the scarcest resource; the fallback trades post-graduation fee revenue for ~1 week of schedule. **Decision is pre-made here to avoid sunk-cost drift.**

> **Reconciliation with §12 (explicit, do not re-litigate):** the fallback trigger is **migrator gate-failure within week 1**, *not* V3 availability — **§12.28 confirms Uniswap V3 is deployed on 4663**, so "V3 unavailable" is no longer a possible cause. Accepting the fallback has a stated, one-way cost: it **flips the LP copy** from the canonical "LP principal permanently locked; trading fees claimable by treasury" (§12.14) to **"LP burned forever"** (§6.3 / CLAUDE.md hard rule — the only sanctioned flip), and it **eliminates post-graduation fee revenue** (no LPFeeVault.collect stream, §6.4). The **pull-payment curve-fee escrow (§12.25) is unaffected** — it is curve-side and independent of graduation venue, so day-1 fee capture under bot-dominant flow (§2.2) survives either path.

**Pre-caps-lift roadmap (M4/M5, before caps lift):** anti-sniper redesign to a decaying + size-based early-buy fee (§12.27); §10 gates 5–10 execution; **operations runbooks** — RPC failover, Ponder schema-migration re-index strategy, Redis seq-reset heal stampede, lost-publish gap signal + poll cadence, graduate/collect cron + stuck-graduation alert, incident runbook (findings Bucket 6; sources exist today, must gain items before M4).

## 12. Resolved Decisions

1. Graduation: **Uniswap V3 1% full-range, LP NFT in immutable LPFeeVault, treasury collects fees** (Option B — supersedes interview's V2 choice; V2+burn documented as fallback)
2. Wallets: classic wagmi/RainbowKit; 4337 phase 2
3. Fees: treasury (Safe) only; creator rewards designed-in, disabled
4. Graduation threshold: ~$69k mcap parity, constants fixed at deploy
5. Indexer: Ponder + Postgres + Redis
6. Frontend: Next.js on Bun, SSR + OG
7. Storage: R2 + CDN, on-chain metadata hash commitment
8. Security: AI pipeline + capped beta + public bounty + explicit external-review decision gate
9. Compiler: exact single pin validated against Blockscout (candidate 0.8.35)
10. Treasury: Safe (verify/deploy canonical), custom multisig dropped
11. (2026-07-09) `GRADUATION_ETH` is **net-of-fee real curve reserves** (what actually funds the LP); M0 sizes the ~$69k-parity target from net reserves + LP tranche at the graduation price (contracts O-1)
12. (2026-07-09) `ReadyToGraduate` locks **both** buys and sells; §6.5 "sells always open" means *no pause authority* — the lock is a deterministic, permissionlessly-exitable protocol state; UI shows a "Graduating…" interstitial for both directions (contracts O-2, web-5)
13. (2026-07-09) Graduation dust: **token leg → `0x…dEaD`; WETH leg → treasury** — real ETH value is never burned (contracts O-3)
14. (2026-07-09) Canonical LP sentence, single string constant everywhere incl. the §5.2 Trust panel: **"LP principal permanently locked; trading fees claimable by treasury."** (web-8)
15. (2026-07-09) Canonical event shapes (indexer needs zero hot-path RPC reads): `TokenCreated(token, curve, creator, name, symbol, metadataHash, metadataUri, pool)` on the factory; `Trade(trader, isBuy, ethAmount gross, tokenAmount, fee, virtualEthReserves, virtualTokenReserves, realEthReserves — all post-trade)` on the curve; creator's initial buy is **not** in `TokenCreated` — derived from the first `Trade` in the same tx (indexer OI-1/OI-2, contracts O-11)
16. (2026-07-09) LaunchToken `Transfer` is the **sixth indexed event family** and the sole source of holder-balance truth; V3-leg cost basis is best-effort until Phase-2 portfolio; pre-graduation V3 pool activity is **not** indexed into the price series (curve is the sole venue until `Graduated`; gate-7 alerting covers pool griefing) (indexer OI-3/4/5)
17. (2026-07-09) Candle interval set: **1s · 15s · 1m · 5m · 15m · 1h** (indexer OI-7)
18. (2026-07-09) Anti-sniper mechanism = `block.timestamp` window (both mechanisms were spec-sanctioned; seconds are deployment-constant, block cadence is marketing); window/cap **values** from M0 (contracts O-7)
19. (2026-07-09) Image uploads are **API-mediated** (magic-byte sniff + re-encode before any byte reaches public storage; presign exists only on the API→R2 leg); metadata JSON is server-canonicalized and the client **must** re-verify the hash with the shared canonicalizer before signing (api OI-A1)
20. (2026-07-09) Confirmation upgrades propagate as O(1) **watermark broadcasts** on `global:confirmations` (clients upgrade held events locally); REST serves the materialized per-row state; no per-row fanout (indexer OI-9)
21. (2026-07-09) Moderation listing defaults: `pending_review` stays **listed**; the WS launch ticker is unmoderated in v1 (hide propagates via REST); direct `/tokens/:address` fetch of a hidden token returns it **with a hidden flag**, never a 404 — hiding is listing-only (api OI-A5/A6)
22. (2026-07-09) Discovery ranking defaults (tunable config, not consensus values): King of the Hill = `progress × ln(1+vol24h)`; trending = `vol24h × e^(−age/24h)`; search = trgm similarity with ticker boost ×1.2, volume tiebreak, floor 0.25 (api OI-A2/A3)
23. (2026-07-09) v1 scope defaults: no WS replay buffer (REST-heal on reconnect); server-side image-hash verification deferred (hash carried in metadata JSON, client-verifiable); UI is dark-**only**; impersonation watchlist = curated, source-cited, dated data file refreshed ≥ monthly (indexer OI-10/12, api OI-A9, web-9)
24. (2026-07-09) Frontend component library = **shadcn/ui** (design not ready; library must be trivially re-themeable): Tailwind-native (already the §9 stack), components are **vendored into `apps/web/components/ui/` via the CLI** (owned code, no runtime component dependency), Radix primitives under the hood, RSC/Next-15-compatible, dark-first. Theming is **exclusively via CSS custom-property design tokens** (`globals.css` `@theme`); the future design lands by swapping token values + selective component restyles. Rule: **no component may carry bespoke styling that bypasses the token system** (no raw color values outside the token file; lint-enforced, web.md §7/§8.3). Alternatives rejected: raw Radix (same result, more assembly — shadcn *is* Radix wrapped), HeroUI (runtime dep, less ownable), Mantine (parallel styling system alongside Tailwind). (web-12)
25. (2026-07-09) **Curve fee escrow = pull-payment.** `BondingCurve` never transfers ETH to the treasury on any trade path; the 1% ETH-leg fee **accrues in-contract** (`accruedFees`) and is withdrawn by a separate **permissionless `sweepFees()`** (`nonReentrant`, **not** phase-gated, never reaches curve reserves), mirroring `LPFeeVault.collect()`. This restores "sells always open" *by construction* — a hostile/reverting treasury can no longer freeze buys or sells (resolves threat-model UM-1). Solvency invariant becomes `balance ≥ realEthReserves + accruedFees`; exact-fee invariant becomes `treasuryReceipts (via sweepFees) + accruedFees == Σ computed fees`. `graduate()` transfers `balance − accruedFees` to the migrator, so accrued fees remain sweepable after graduation and the post-grad "zero value" invariant means zero LP/reserve value (unswept fees excluded, drained to 0 by `sweepFees`). The migrator's flat graduation-fee push to treasury is unchanged (graduate() is not a sell path; a reverting treasury there is the UM-2 grief surface, not a sell freeze). (findings C-1 / threat-model UM-1)
26. (2026-07-09) **Graduation fee = small flat, cost-based.** Sized to ≈ V3-migration gas + a thin margin — **not** 1.5%-of-raise. Exact number computed at M1 against real testnet gas; M0/constants carry it as a documented formula/placeholder, never a hardcoded USD figure (no "$219"). §6.4 amended. (findings U-3, graduation-fee leg)
27. (2026-07-09) **Anti-sniper: v1 mechanism frozen; redesign deferred.** v1 ships the ratified fixed `block.timestamp` window + per-tx `MAX_EARLY_BUY` cap (§12.18 mechanism; values from M0). A redesign to a **decaying + size-based early-buy fee** is a roadmap item deferred to **before caps-lift** (§11 pre-caps-lift roadmap); the M1 mechanism is unchanged. (findings U-3, anti-sniper leg)
28. (2026-07-09) **Uniswap V3 confirmed on chain 4663.** Verified addresses recorded as source of truth (also CLAUDE.md chain facts + M0 constants `external.*`): Factory `0x1f7d7550B1b028f7571E69A784071F0205FD2EfA`, NonfungiblePositionManager `0x73991a25C818Bf1f1128dEAaB1492D45638DE0D3`, SwapRouter02 `0xcaf681a66d020601342297493863e78c959e5cb2`, QuoterV2 `0x33e885ed0ec9bf04ecfb19341582aadcb4c8a9e7`. Closes O-4 / OI-13 / web-11 (V3 leg) / E-1. **Deploy-time runtime assertions remain mandatory** (`factory.feeAmountTickSpacing(10000)==200`, `NPM.factory()`/`NPM.WETH9()` checks — contracts.md §7.2). Trade fee stays **1%** (kept). Safe deployment/signer set (O-6) remains open. (findings U-4 / O-4 / E-1)
29. (2026-07-09) **Workspace manager = pnpm.** The monorepo uses pnpm workspaces (strict non-flat `node_modules`, `workspace:*` internal deps, single-version via pnpm catalogs, one `pnpm-lock.yaml`); **Bun remains the runtime and test runner** (§8/§9). The Bun→pnpm conversion of `packages/*`, root `package.json`, and lockfiles is executed by robbed-shared. (findings U-2)
30. (2026-07-09) **On-chain name/symbol byte limits = cross-service source of truth.** `createToken` validates `bytes(name).length ∈ [1,32]` and `bytes(symbol).length ∈ [1,10]` (contracts.md §2.2). The API, `packages/shared` zod schemas, and OpenAPI MUST validate the same **byte** limits (name ≤ 32 bytes, ticker ≤ 10 bytes — bytes, not characters), so no name/ticker that passes the API can revert at `createToken`. robbed-shared updates the frozen schemas (name was `≤ 64 chars`). (findings X-1)
31. (2026-07-09) **Metadata JSON `version` frozen at `1` inside the hash preimage.** The canonical metadata object carries `version: 1` for v1; it is part of the canonicalized bytes that produce `metadataHash`, so it is committed on-chain. Bumping `version` is a future, explicit decision (new preimage ⇒ new hash). (findings X-13, metadata-version leg)
32. (2026-07-10) **Anti-sniper values set (M0 default, tunable in the capped beta).** The §12.18/§12.27 fixed `block.timestamp` guard is parameterized in `tools/m0/out/constants.json`: `antiSniper.windowSeconds = 8` (inside the §12.18-sanctioned 5–10s band) and `antiSniper.maxEarlyBuyWei = 201922000000000000` (~0.202 ETH = **exactly 2.5% of `GRADUATION_ETH`**, §12.11). Values live in constants, never inlined. These are **M0 engineering defaults** — shown to and not overridden by the user ("ship simple now", §12.27) — **retunable during the capped beta (gate 7), not immutable-at-deploy product commitments**. Multi-wallet bypass remains acknowledged (§2.2/§6.5); the decaying + size-based redesign stays deferred pre-caps-lift (§12.27). Closes the O-7 values leg (§13). (contracts O-7)
33. (2026-07-10) **Arb-back parameter values set (M0 default, tunable in the capped beta).** In `tools/m0/out/constants.json`: `v3.toleranceTicks = 100`, `v3.maxArbIterations = 8`, `v3.migrationSlippageBps = 100`, under the §13-ratified token-leg budget rule (arb spend consumes only inventory **above** the target-price mint requirement). **Scope limit — records the param *values* only.** The **WETH-leg budget *definition*** (what the "mint requirement" means on the WETH side, undefined by the token-leg rule since the mint consumes all WETH) remains a robbed-contracts **M1 implementation obligation** per threat-model §8.1 — it must be defined **and proven in gate-2 invariant 6**, NOT here. These values feed the gate-2 fuzz bounds and are retunable in the capped beta. Closes the O-8 param-values leg (§13); the WETH-leg budget definition is tracked to M1. (contracts O-8) *Update 2026-07-11 — WETH-leg budget definition CLOSED (architect ratification of the M1-10 rule).* The M1 obligation is discharged: robbed-contracts defined the WETH-leg budget as `wethArbBudget = wethForMint × MIGRATION_SLIPPAGE_BPS / 10_000` — the arb may spend at most `MIGRATION_SLIPPAGE_BPS` of the WETH the target-price mint requires — and the M1-10 Part-1 fix (M-10-A) made the token leg **symmetric** to it (draw down to `LP_TOKEN_TRANCHE × (1 − bps)`), with the mint's `amount0Min`/`amount1Min` (each = pre-arb requirement × `(1 − bps)`) independently re-enforcing the parity floor on both sides. This is threat-model §8.1's candidate (a) (slippage-bounded value-skew cap; the dedicated `maxWethArbBps` immutable was deliberately folded into the single knob), it changes no stated guarantee (§12.11 parity holds to within `MIGRATION_SLIPPAGE_BPS` either side), and it is **proven in gate-2 invariant 6** as demanded (M1-10 re-gate PASS: 384 griefed lifecycles vs real V3, 0 hostile mints; token-leg liveness invariant + directed `test_M10A_tokenLegGrief_*` regressions). Rule recorded in `contracts.md` §3.4 step 5 + §8 O-8 row; code `contracts/src/V3Migrator.sol` (decision #2, `_arbStep`). **Still open, tracked separately:** the gate-6 economic quantification (griefing-cost proof) and the UM-2 Part-2 residual (mispricing beyond the slippage-recoverable range → retriable revert) remain **caps-lift (M4) items** — this closure covers the *definition* leg only. Evidence: the decision ledger (retired 2026-07-12; history: git).
34. (2026-07-10) **Graduation caller reward set (M0 default, tunable in the capped beta).** `fees.callerRewardWei = 2822000000000000` (~0.002822 ETH ≈ $5 at the recorded ETH/USD snapshot — source + timestamp in constants, **never a hardcoded USD figure**, §2), with immutable hard ceiling `fees.maxCallerRewardWei = 14110000000000000` (5×). **M1 re-validation obligation:** re-confirm the reward is **≥10× actual `graduate()` gas** against real testnet gas at M1 (mirrors the §12.26 graduation-fee finalization). Retunable within `[0, maxCallerReward]` during the capped beta. Closes O-9 (§13). (contracts O-9)
35. (2026-07-10) **Large-value disclosure threshold — M0 input furnished; the threshold value remains M3-10.** M0 furnishes the sourced ETH/USD basis for the §2.1 large-value confirmation-disclosure threshold: `ethUsdSnapshot` in `tools/m0/out/constants.json` (source: coingecko, timestamped `2026-07-10T06:43:28.947Z`, value `1771.50888854` — live-queried, never hardcoded, §2). The **threshold config value itself is NOT decided here** — it remains the architect's decision **before M3 exit** (plan item M3-10), a config value in web + api, never a literal. This entry records only that the M0 ETH/USD input exists; **web-10 stays OPEN in §13** pending M3-10. (web-10)
36. (2026-07-10) **M0-4 governance values computed and recorded — organic-volume floor (Gate G-A.1) + funding-cluster alert thresholds (gate 7).** The M0 notebook (robbed-contracts) computed and emitted both into `tools/m0/out/constants.json.governance`; this entry records them and their disposition. **The two halves have DIFFERENT status — read the split carefully:**
    - **`governance.organicVolumeFloor` — Gate G-A.1 market floor (§14). This is a PRODUCT/POLICY default REQUIRING architect + effectively user ratification before Gate G-A — it is NOT settled here.** Values: `floorWei = 40384344110704909120` (= **exactly 5 × `GRADUATION_ETH`**, §12.11, ≈ 8.08 ETH), `windowDays = 7`, `floorGraduationsEquiv = 5`, `organicFlowDiscountMaxPct = 50`. Deliberately expressed as a **self-referential multiple of the curve's own `GRADUATION_ETH`** — NO headline / chain-wide / Dune / USD figure is baked in (§2). It is the Gate G-A.1 "memecoin activity has not collapsed" threshold, measured against the **own indexer's** organic-volume estimate (§8.5, wash-flagged volume excluded), under the ≤50% organic-flow discount (§2.2) when only headline data is available. **The magnitude N (= 5 graduations-equiv per 7d, "what counts as not-collapsed") is a genuine go/no-go policy judgement that defines the real-money mainnet decision, not a pure engineering default.** Disposition: **M0 default, ratification REQUIRED before Gate G-A** (architect + effectively the user weighs in), and **recalibrate at M2** once the indexer emits real organic-volume series (§8.5). It stays a **live Gate-G-A input**, not a closed decision — do not treat it as settled. web-10-style "furnished, not decided" discipline applies.
    - **`governance.clusterAlertThresholds` — gate-7 funding-cluster alert (§10 gate 7 amend, §8.5). Tunable ENGINEERING defaults.** Values: `perTokenPct = 25` (X — one §8.5 funder cluster exceeding 25% of a single token's curve volume), `platformPct = 10` (Y — one cluster exceeding 10% of platform-wide curve volume), `windowHours = 24`, with `platformPct ≤ perTokenPct` asserted in the notebook. Advisory ALERT only — **never gates chain state** (§8.4/§8.5 advisory-only). Disposition: **M0 default, tunable; final threshold tuning routed to robbed-security before the capped-beta deploy (gate 7)**; hooks emitted in M2-12. Not a Gate-G-A input.

    This closes the **compute+record** leg of the §13 "organic-volume floor + funding-cluster alert thresholds" item, but the organic-floor magnitude remains an **open Gate-G-A ratification input** in §13 (see below); the cluster thresholds pass to robbed-security. Values live in `constants.json.governance`, never inlined (§2). (M0-4)
37. (2026-07-10) **Frontend pins Next.js 16 + React 19 (exact majors, no ranges) — supersedes the §9 "Next.js 15" reference.** Per user directive, the frontend stack targets **Next.js 16** with **React 19** *only* — no version ranges anywhere, consistent with the whole-repo no-ranges discipline (mirrors the compiler-pin rule §6.7). **Docs-first basis (2026-07-10, context7 `/vercel/next.js` + `/react/react`, cross-checked against the official upgrade guide `nextjs.org/docs/app/guides/upgrading/version-16`, doc header `version: 16.2.10`, lastUpdated 2026-05-13):** Next.js 16 is released (context7 indexes v16.0.3 → v16.2.9; upgrade guide reports 16.2.10 current); its App Router **uses/requires React 19.2** (`react@19` / `react-dom@19`); toolchain floors **Node.js ≥ 20.9** and **TypeScript ≥ 5.1** (both already satisfied — repo is on Node 22). Pins to record at M3 scaffold: `next@16` (latest 16.2.x), `react@19` + `react-dom@19` (latest 19.2.x), matching `@types/react@19` / `@types/react-dom@19`. **This §12 entry records the decision + doc text only; the concrete version pins land in `apps/web/package.json` when `apps/web` is scaffolded in M3 — owned by robbed-shared (pnpm catalogs / single-version policy §12.29), never here.**
    - **Peer-library React-19 compatibility:** context7 was consulted for Next.js and React (the two the directive mandated). No React-19-blocking incompatibility was surfaced for the rest of the §9 stack (wagmi v2, viem, RainbowKit, TanStack Query v5, lightweight-charts, satori). `lightweight-charts` and `satori` are React-version-agnostic (no React peer). wagmi v2 / viem / TanStack Query v5 already support React 19; RainbowKit must be on a React-19-compatible line. **No peer bump is mandated by this entry.** Authoritative peer-version confirmation (exact compatible versions + any RainbowKit floor) is a robbed-frontend obligation at **M3-start** under the existing web-6/web-7 runtime checks (§13); if any peer needs a bump, hoodpad-frontend records it in web.md and hoodpad-shared sets the catalog version — not the architect here.
    - Breaking-change awareness for M3 (from the upgrade guide, informational — not decisions): Turbopack is default; async Request APIs (`cookies`/`headers`/`params`/`searchParams`) are fully async; `middleware` → `proxy` rename; `next lint` removed (use ESLint/Biome directly); `next/image` default changes. hoodpad-frontend absorbs these when scaffolding M3.

### Integration-seam reconciliation sweep (2026-07-10, §12.38–§12.43)

38. (2026-07-10) **Full read-function contract ABIs are a compilation-time codegen artifact in `packages/shared`.** `packages/shared` carried only *event* ABIs (`events.json`, §12.15). The frontend M3-5 Trust-panel live reads (`reserves()`, `phase()`, `quoteBuy/quoteSell`, per-token `TRADE_FEE_BPS`, `totalSupply()`, factory `config()`) **and** the indexer's startup curve-constants read (currently env-interim) both require the **full** contract ABIs. Ratified: a **compilation-time codegen step** (needs only `forge build` — **no deploy**) emits the complete ABIs for all six contracts (LaunchToken, CurveFactory, BondingCurve, Router, V3Migrator, LPFeeVault) from `contracts/out/*.json` into `packages/shared/src/abi/*.json` (+ a typed index), hosted under `packages/shared` per the anti-drift rule (single source; apps import, never hand-write — architecture.md §4). Distinct from (a) `events.json` (event fragments only, §12.15) and (b) the **deploy-time** `addresses` codegen (M1-14, which needs a broadcast). The read ABIs land at M1 (`forge build`) so M2 (indexer) and M3 (frontend) consume them independent of any deploy. Owner: **hoodpad-contracts** extends the M1-3 codegen (new plan sub-item **M1-3b**). Consumers: **hoodpad-indexer** replaces the `virtual_eth`/`virtual_token`/`graduation_eth` env-interim startup read with a `config()` read via the shared ABI (indexer.md §2/§3.1); **hoodpad-frontend** imports the shared ABIs for all M3-5 live reads (web.md §3.2 — "No ABI duplicated or hand-written in `apps/web`"). (integration-seam #1)
39. (2026-07-10) **`ICurveFactory.FactoryConfig` shape ratified = the §2.2 storage field set, returned whole by `config()`.** The M1-8 gate flagged that `config()`'s return struct was never itemized. Ratified: `FactoryConfig` is the single current-config snapshot struct consumed by Router, UI/Trust-panel, and the indexer startup read (§12.38), mirroring the contracts.md §2.2 storage field set — **no invented fields**. It carries: operational owner-settable fields (`treasury`, `tradeFeeBps`, `creatorFeeBps`≡0, `creationFee`, `graduationFee`, `callerReward`, `earlyWindowSeconds`, `maxEarlyBuyWei`, `perTokenEthCap`, `globalEthCap`, `pauseCreates`, `pauseBuys`, `router`, `migrator`), the immutable ceilings (`maxCreationFee`, `maxGraduationFee`, `maxCallerReward`), and the immutable curve-shape defaults the indexer caches for new curves (`virtualEth0`, `virtualToken0`, `curveSupply`, `lpTranche`, `graduationEth`). **Per-token vs factory-config caveat (see §12.40d):** `FactoryConfig` fields are *factory-current* and govern future curves only for the snapshotted economics; per-token live values (e.g. an older curve's `TRADE_FEE_BPS`) are read from that curve, never from `config()`. `CurveParameters` (the staged per-curve deploy struct, valid only mid-`createToken`) is unchanged and separate. Itemized in contracts.md §2.2. Owner: **hoodpad-contracts** aligns the M1 implementation to this itemization (report any divergence, never silently differ). (integration-seam #2 / F-5)
    - **Amendment (2026-07-12) — SPLIT read surface re-ratified as canonical (supersedes "returned whole by `config()`").** hoodpad-contracts reported the divergence per this entry's own rule; the architect re-ratifies the as-built ABI rather than scheduling a breaking struct expansion. Canonical surface: **`config()` returns only the 12 operational owner-settable fields** (`treasury`, `tradeFeeBps`, `creatorFeeBps`≡0, `creationFee`, `graduationFee`, `callerReward`, `earlyWindowSeconds`, `maxEarlyBuyWei`, `pauseCreates`, `pauseBuys`, `perTokenEthCap`, `globalEthCap`); the five immutable **curve-shape defaults** (`virtualEth0`/`virtualToken0`/`curveSupply`/`lpTranche`/`graduationEth`) are returned by the ADDITIVE **`curveDefaults()`** view (`CurveDefaults` struct, `ICurveFactory` — landed 2026-07-12 for LAUNCH-2; the canonical pre-create economics read for the Create-page preview and the indexer startup cache, safe before any curve exists); `router()`/`migrator()`/`weth()`/`treasury()` have dedicated getters; the three setter ceilings (`maxCreationFee`/`maxGraduationFee`/`maxCallerReward`) are `public immutable` on the factory (on-chain readable) and constructor-asserted (incl. `maxCallerReward + maxGraduationFee < graduationEth`, `GraduationUnfundable`). Rationale: the frozen 12-field `config()` ABI is already consumed mid-flight by indexer/web via the §12.38 shared codegen ABIs, so a breaking expansion buys nothing functional — **no field was dropped from the read surface, only its packaging changed** — and the split keeps the mutability model legible (`config()` = owner-settable operational snapshot; `curveDefaults()` = immutable shape), reinforcing the §12.40d per-token-vs-factory caveat. Consequence for §12.38: the indexer startup curve-constants read targets `curveDefaults()`, not `config()` (indexer.md §3.1 updated). contracts.md §2.2 itemization updated to the as-built split.
40. (2026-07-10) **API interface-seam refinements (six-part).** Ratifies the cross-service API seams surfaced by the API track; consumer-visible pieces recorded here, implementation routed to owners.
    - **(a) Shared response-type aliases.** `packages/shared` `api-types.ts` gains `export type` aliases `ConfirmationsResponse` (`GET /v1/confirmations`), `EthUsdResponse` (`GET /v1/eth-usd`), and `ModerationQueueItem` (admin queue row, api.md §3.6), for naming consistency with the other DTOs. Owner: **hoodpad-shared**.
    - **(b) `conflict`/409 retired; imageHash-reference mismatch is 400 `invalid_request`.** v1 has **no genuine resource-conflict path** — image upload and `metadata/{hash}.json` are both content-addressed (idempotent; identical bytes ⇒ identical hash ⇒ no conflict). The `POST /v1/metadata` imageHash-not-produced-by-us case is a **client input error → HTTP 400, `error.code = 'invalid_request'`** (matches the API implementation). The shared error-code union **drops `conflict`/409** so the union lists only codes the API actually returns (the frontend needs no 409 branch). Owner: **hoodpad-shared** (error-code union + enum-doc example); api.md §3.2 documents the 400.
    - **(c) Moderation vendor interfaces are API-only, NOT in `packages/shared`.** `CsamHashMatcher` / `ContentClassifier` (api.md §4.3) are consumed by exactly one service (the API moderation worker) — they **fail the anti-drift ≥2-consumers test** and stay in `apps/api/src/moderation/vendors/`. api.md §4.3 corrected (they do **not** live in `packages/shared`). Owner: **hoodpad-shared** confirms exclusion; api owns the interface files.
    - **(d) Per-token `trade_fee_bps` is indexed and is the Trust-panel source, not factory config.** `setTradeFeeBps` applies to future curves only (contracts.md §2.2), so live factory config misreports the fee of any curve created under a prior fee. The indexer stores `tokens.trade_fee_bps` as a **per-token immutable snapshot**, read from the curve's public `TRADE_FEE_BPS` immutable at `TokenCreated` (via the §12.38 shared read-ABI; creation is low-frequency, not hot-path). The API `trust.feePolicy.tradeFeeBps` (api.md §3.4) sources this stored per-token value; the frontend may additionally live-read the curve's `TRADE_FEE_BPS` per-token for the "read from chain just now" treatment — never the factory config. Owner: **hoodpad-indexer** (store + serve).
    - **(e) `change24hPct` price anchor defined.** `change24hPct = (lastPrice − anchorPrice) / anchorPrice`, where `anchorPrice` = the close of the most-recent 1h candle at or before `now − 24h`; for tokens younger than 24h, `anchorPrice` = first-trade price (creation-anchored); no trades ⇒ `0`. Tunable like the §12.22 ranking formulas (indexer/API-owned; frontend renders only). Owner: **hoodpad-indexer**.
    - **(f) `moderation_audit_log` is an API-local table, not shared.** Written/read only by the API admin path (fields: actor, action, target, reason, ts — api.md §6.2); it fails the ≥2-consumers test, so its row type stays in `apps/api` (not `packages/shared` `db-rows.ts`, not indexer.md). DDL documented in api.md §6.2. Owner: **hoodpad-indexer** (owns api.md) documents it; **hoodpad-shared** confirms exclusion. (integration-seam #3)
41. (2026-07-10) **M2-0b Ponder same-tx factory-child spike runs at Phase-I/M2 start; receipt-derived initial-buy is the pre-sanctioned fallback.** The spike (does Ponder index a child curve's `Trade` emitted in the *same tx* as its `TokenCreated`, i.e. the atomic initial buy, §12.15) is BLOCKING before M2-5 handler work relies on native same-tx child capture. It runs once the local stack is up (needs anvil+Ponder+Postgres — the I-2 local-chain bring-up supplies the same-tx create+buy fixture). **Pre-sanctioned fallback if Ponder cannot capture the same-tx child event:** derive the creator's initial buy by **parsing the first `Trade` log from the `createToken` transaction receipt** (via the §12.15 event ABI) inside the `TokenCreated` handler, instead of relying on Ponder child-source firing for the same-tx `Trade`. Because this fallback is pre-sanctioned, a failing spike does **not** require re-escalation to the architect — hoodpad-indexer implements the receipt-derived path and records the Ponder-version finding in indexer.md §7.4. Owner: **hoodpad-indexer**. ***Spike outcome 2026-07-11: PASSED — NATIVE same-tx child capture confirmed on ponder 0.16.8*** (both legs: source-level mechanism — block-granular child registration with `<=` matching, address resolution before log filtering on both realtime and historical paths — plus a runnable anvil+PGlite spike capturing same-tx `ChildCreated`+child `Trade` on both sync paths, zero handler errors; indexer.md §7.4). **The receipt-derived fallback is RETIRED to dormant** (pre-sanctioned, never implemented); M2-5 handlers rely on native capture. **Load-bearing invariant recorded (§12.15 emission order):** the factory emits `TokenCreated` **before** the initial-buy `Trade` in the same tx — same-block handler ordering is by `logIndex`, so this **contracts-side invariant must stay true** (owner: robbed-contracts; inverting it would fire the `Trade` handler before the token row exists — an event-shape/ordering divergence to escalate, never work around; M2-5 keeps a cheap token-existence guard in the `Trade` handler). (integration-seam #4 / E-3)
42. (2026-07-10) **`apps/indexer` `typecheck` script = `ponder codegen && tsc --noEmit`.** Ponder's virtual-module types (`ponder:registry`, generated schema types) do not exist until `ponder codegen` runs; a bare `tsc --noEmit` in CI fails wholesale on the absent virtual modules (flagged in M2-4 evidence). The workspace `typecheck` script for `apps/indexer` must run `ponder codegen` first. Owner: **hoodpad-shared** (owns CI + workspace scripts, §12.29). (integration-seam #5)
43. (2026-07-10) **The M1 `IBondingCurve` interface's reconciliation to the §12.25 pull-payment shape was legitimate, not a freeze violation.** The "frozen" `IBondingCurve` was updated during M1-8/M1-9 to add `trader` (§12.15/X-3), `sweepFees()`, `accruedFees`, the `FeesSwept` event, and corrected NatSpec — matching spec §12.25 (pull-payment fee escrow, 2026-07-09) and contracts.md §2.3 exactly. Confirmed ratified: reconciling a frozen interface to a **landed §12 decision** is the sanctioned path (the freeze prevents *silent* drift, not decision-driven updates). The freeze baseline for `IBondingCurve` is now the §12.25-reconciled shape in contracts.md §2.3. No further implementation change. (integration-seam #6 / F-1)

44. (2026-07-10) **Cancun is the compile/deploy EVM target; only `mcopy` (EIP-5656) is relied upon, transient storage stays deliberately unused.** hoodpad-contracts pinned `evm_version = "cancun"` in `foundry.toml` after discovering that **OpenZeppelin v5.5's `Bytes.sol` emits `MCOPY`** (Cancun / EIP-5656) and is reached **transitively through the mandatory `ERC20Permit → ECDSA → EIP712 → Strings → Bytes` chain** — so a pre-Cancun `evm_version` fails to compile the token, independent of any code we write. **Confirmed safe on chain 4663:** ArbOS 20 "Atlas" added `MCOPY` (together with `TSTORE`/`TLOAD`); Robinhood Chain runs **ArbOS ≥ 32**; and the Robinhood Blockscout verifier lists `cancun` as a supported compilation target. **Only `mcopy` is relied upon** — the codebase uses **no transient storage**: the storage-based OZ `ReentrancyGuard` stays (contracts.md §5.4), and the §3.4-step-3 `CurveParameters` staging remains plain storage (never `TSTORE`). This **supersedes** the prior contracts.md §7.1 / spec §6.7 "do not assume Cancun features" guidance (both amended). **Final Blockscout verification of the `cancun` target is folded into the O-5 pin check** (verify `0.8.35` + `cancun` together with a throwaway contract at **M1-2 / O-5**, before first deploy); if either is unsupported, the architect records the corrected pin/target in §12, never silently. Amends contracts.md §7.1 + spec §6.7. Owner: **hoodpad-contracts**. (contracts §7.1)

45. (2026-07-10) **Hosting split ratified (user-directed): backend on self-hosted Komodo, Next.js frontend on Cloudflare Workers via OpenNext.** Resolves the architecture.md §6 "Hosting" deferral ("concrete host/orchestration decided at M4") ahead of schedule by user direction. **Two targets:**
    - **Backend → Komodo** (self-hosted container orchestration, github.com/moghtech/komodo). Postgres (+`pg_trgm`), Redis, the **Ponder indexer** (Node container, spec §8), and the **Hono/Bun API + Bun WS fanout** (`apps/api`, incl. `apps/api/src/ws.ts`) deploy together as a Komodo **Stack** (a docker-compose-based, git-synced resource) managed by a Komodo **core + periphery agent**. **Reasoning:** the backend is long-running Bun/Node processes with stateful attachments (Postgres, Redis) and a persistent WebSocket fanout — not a request/response edge workload; the **WS fanout stays co-located on the container host** so the Redis→socket hop keeps the <500ms event-to-browser budget (§8) without an edge round-trip. Env/secrets are Komodo-managed; images either built on Komodo from the repo or prebuilt in CI (runbook decides per service).
    - **Frontend → Cloudflare Workers** (NOT Pages-edge) via the **OpenNext Cloudflare adapter** (`@opennextjs/cloudflare`). SSR (the three App-Router pages, §9) runs on `workerd` with `compatibility_flags = ["nodejs_compat"]` + a recent `compatibility_date`; deploy pipeline is `opennextjs-cloudflare build && … deploy` behind a `wrangler.jsonc` + `open-next.config.ts`. **Docs-first basis (2026-07-10, opennext.js.org):** OpenNext Cloudflare explicitly supports "all minor/patch versions of Next.js 16" — so it does not lag the §12.37 Next.js 16 pin. **Standing docs-first caveat for M3/deploy:** re-verify Next.js 16 support against the adapter's live compatibility matrix at implementation time; if a future adapter/Next combination regresses, that is a deploy blocker to surface, not to work around.
    - **Required frontend consequence — OG rendering moves off the native N-API path to WASM.** `workerd` cannot load native N-API addons, so `@resvg/resvg-js` (the native rasteriser chosen at plan item **M3-8**) is unrunnable on the Workers target. The OG route must use a **WASM path**: `@resvg/resvg-wasm` + `satori`, or Next's `ImageResponse`/`next/og` (which ships a WASM resvg). **This supersedes the M3-8 native-`@resvg/resvg-js` choice *for the deploy target*** (web.md §6's "raw `satori` + `@resvg/resvg-js`" and `serverExternalPackages` native-dep note): the native dep is removed and replaced with the WASM rasteriser. Layout/`satori` code is unchanged; only the raster backend swaps. Owner: **hoodpad-frontend** (task spec below), executed after the concurrent `apps/web` redesign.
    - **Cloudflare = R2 + CDN for images + canonical metadata** (unchanged role from architecture.md §6): bucket `robbed-assets` (pre-created), account `0b1b0b8753489a11d35ee922961f6b72`. R2 stays the content-addressed store the API writes to (§12.19); the on-chain hash remains the commitment.

    **Scope note:** this is an infra/deployment-target decision. It changes no contract, indexer, or API *logic* — only where processes run and which OG raster backend ships. Bun remains the local runtime/test runner everywhere (§8/§9); the frontend still *builds* under the §12.37 Next.js 16 + React 19 pins. Amends architecture.md §6; supersedes plan item M3-8's native-resvg raster backend for the Workers deploy target. Records the P-3 hosting split (indexer/api → Komodo Dockerfiles + Stack; web → Workers, no web Dockerfile). (hosting decision)

46. (2026-07-10) **Project renamed `hoodpad` → `ROBBED_` (user-directed), with an explicit brand-vs-identifier split.** The user directed: "rename all project name mentions from hoodpad to robbed_ in the repo." Ratified naming convention, binding on all copy, code, assets, and future agents:
    - **Brand / display / copy / logo / OG / page titles → `ROBBED_`** (or lowercase `robbed_` where the trailing-underscore blinking-cursor motif is intended). **The trailing `_` is a display motif only** — it carries no semantic meaning and never appears in an identifier. This also resolves the *name* portion of the §13 "Name/domain/brand" open item (domain + final logo/brand-mark treatment stay open).
    - **Identifiers → `robbed` (NO underscore).** Applies to: the npm scope (`@hoodpad/*` → `@robbed/*`), all package `name` fields (root `hoodpad` → `robbed`; `@hoodpad/{shared,web,api,indexer,m0}` → `@robbed/…`), Claude Code agent names + filenames (`.claude/agents/hoodpad-*.md` → `robbed-*.md`, and every `Agent(subagent_type: "hoodpad-*")` call-site + cross-reference), directory/service names (docker-compose project + service + volume names, Postgres db/user/password-var, Redis, MinIO/R2 identifiers), env-var prefixes, metric/label prefixes, and Solidity generated identifiers (`HoodpadConstants` → `RobbedConstants`, `HOODPAD_CONSTANTS` → `ROBBED_CONSTANTS`, produced by `tools/m0/derive.ts`). **Rationale for dropping the `_`:** `@robbed_` is not a valid npm scope and a trailing `_` is illegal or awkward in most identifier grammars (npm, Postgres roles, docker service names, Solidity). Solidity *type* names (LaunchToken, BondingCurve, CurveFactory, Router, V3Migrator, LPFeeVault, CurveMath) are product-neutral and are **not** renamed — only literal "hoodpad" strings in NatSpec/comments/titles change.
    - **Already-final external names (do NOT revert to hoodpad):** the R2 bucket is already `robbed-assets` and the Cloudflare account id is fixed (§12.45) — both stay. The Cloudflare **Worker** name in the P-3 runbook is still `hoodpad-web` (a *plan* value, nothing deployed) and moves to `robbed-web` as part of this rename, before any Workers deploy.
    - **Execution is deferred and gated, not performed at ratification.** A concurrent `apps/web` redesign is live; renaming the `@hoodpad/*` scope or the `.claude/agents/hoodpad-*.md` files mid-flight would break that build and the running agent identities. The rename executes as one atomic pass **after** the redesign foundation lands and **before** the Workers/OpenNext adaptation (so the new Workers files are authored against final `@robbed` names). Two bootstrapping constraints: (1) the npm-scope change + **every** consumer import must land in a single commit so no `@hoodpad/…` specifier dangles; (2) the six agent-file renames + **every** reference (CLAUDE.md Agents section, `.claude/skills/goal`, `.claude/commands/*`, cross-agent mentions, and orchestrator `subagent_type` call-sites) must land in a single commit so no `robbed-*` route dangles. Full inventory, atomic per-category execution order, and verify commands: the executed rename plan (2026-07-10, commits `f83967a` + `3d6cbec`; archive retired 2026-07-12 — history: git). Owner: **hoodpad-shared** (owns `packages/*`, workspace config, package names) executes the scope + infra + agent rename; **hoodpad-architect** ratifies and updates spec/CLAUDE.md/docs prose. (naming decision)

47. (2026-07-10) **Large-value confirmation-disclosure threshold DECIDED — `1.0 ETH` (M3-10, closes web-10).** The §2.1 requirement that large-value trades surface the stronger confirmation tiers (posted-to-L1 / finalized) more prominently is parameterized by a single ETH-notional threshold: **`largeValueEthThreshold = 1000000000000000000` wei (1.0 ETH)**. A trade whose ETH notional (gross buy ETH in, or gross sell ETH out) is **≥ 1.0 ETH** triggers the stronger disclosure UX; below it, the default soft-confirmed treatment applies. **§2 discipline:** the value is a **config**, never a code literal — the on-chain event/curve carries no such number and no USD figure is baked in. Recorded basis (for reasoning only, not stored in code): at the M0 `ethUsdSnapshot` (`1771.50888854`, coingecko, `2026-07-10T06:43:28.947Z`, §12.35) 1.0 ETH ≈ $1,771.51; and 1.0 ETH ≈ **12.38% of `GRADUATION_ETH`** (`8.076868822140981824` ETH, §12.11/§12.32) — a self-referential cross-check so the threshold tracks the curve's own scale, not a headline metric. **Disposition: M0/config default, retunable in the capped beta** (like §12.32–35). **Config keys (wiring is a follow-up for hoodpad-frontend + hoodpad-indexer, NOT done in this entry):** web reads `NEXT_PUBLIC_LARGE_VALUE_ETH_THRESHOLD` (already present as a placeholder in `.env.example`; populate with `1.0`); the API mirrors the same value where it surfaces confirmation state. Neither service inlines the literal. Closes web-10 (§13); the §12.35 M0 input is now consumed. (web-10 / M3-10)

### M2-start §13 checks (2026-07-10, §12.48) — indexer implementation-time dispositions

48. (2026-07-10) **M2-start §13 checks recorded (OI-6 / OI-8 / OI-11).** hoodpad-indexer's M2-start checks; the architect records dispositions. **Two are env-gated on a live Robinhood RPC and are recorded CONDITIONALLY — no chain fact is invented.**
    - **(a) OI-6 — ETH/USD source on 4663. Config-driven; Chainlink-if-present, else DefiLlama/Coinbase. Verification ENV-GATED (needs live 4663 RPC).** The `eth_usd_snapshots` poller sources price from `ETH_USD_SOURCE_URL` (indexer.md §3.9): **if** a Chainlink ETH/USD aggregator is deployed on 4663, use the on-chain feed; **else** DefiLlama or Coinbase HTTP with source+timestamp (§2 — displays go "dated" via `eth_usd_snapshot_age_seconds`, never stale-silent). The **Chainlink-existence check requires querying chain 4663** and cannot be confirmed without a live RPC — recorded conditionally; the feed-address-vs-HTTP selection is finalized at M2 start against the real RPC and written to `ETH_USD_SOURCE_URL`. Either branch satisfies §2. Shared with api OI-A10 (single producer = indexer poller, single reader = API). Marked **env-gated (needs RPC)**; DefiLlama/Coinbase is the always-available default until the on-chain feed is confirmed. Closes OI-6's *disposition* (§13); the concrete source selection is the env-gated implementation step. ***Verified 2026-07-11:* the env-gated leg is CLOSED — Chainlink ETH/USD feed CONFIRMED on 4663; Chainlink branch selected. Addresses + mandatory startup assertions recorded in §12.51** (evidence: the decision ledger (retired 2026-07-12; history: git)).
    - **(b) OI-8 — `safe`/`finalized` RPC block-tag support. Use tags if supported; pre-sanctioned L1-watermark fallback. Verification ENV-GATED (needs live 4663 RPC).** The confirmation tracker (indexer.md §5.1) polls `eth_getBlockByNumber("safe")`/`("finalized")` **if** the Robinhood RPC exposes those Orbit tags; **else** the **pre-sanctioned fallback** reads the L1 Rollup/SequencerInbox watermarks (`SequencerBatchDelivered` = posted, node-confirmed finality) via an L1 RPC (M2-3b, env vars `L1_RPC_URL` + rollup/inbox addresses). The M2-6 confirmation tracker already **no-ops gracefully** when tags are unsupported, so the fallback is a funded, non-escalating path. **Tag support cannot be confirmed without a live RPC** — recorded conditionally; the day-1-of-M2 check decides which branch runs. Marked **env-gated (needs RPC)**. Closes OI-8's *disposition* (§13); the branch selection is the env-gated implementation step (§12.20 confirmation semantics unchanged either way). ***Verified 2026-07-11:* BOTH TAGS SUPPORTED on the official 4663 RPC** — `eth_getBlockByNumber ["safe"/"finalized", false]` each returned full Arbitrum block objects (incl. `l1BlockNumber`; finalized < safe < latest as expected; `eth_chainId` = 4663 on the same endpoint; verbatim probe evidence in the decision ledger (retired 2026-07-12; history: git)). The M2-6 tracker's tag path runs; **M2-3b (L1-watermark reader) stays a dormant pre-sanctioned fallback, NOT funded** — the tracker's graceful no-op remains as resilience only. Closes §13 OI-8 in full.
    - **(c) OI-11 — external `confirmation_state` UPDATE vs sidecar table. Sidecar `event_confirmations` is the sanctioned default. VERSION-GATED (needs pinned Ponder), NOT RPC-gated.** Whether Ponder tolerates an external `UPDATE` of a `confirmation_state` column on Ponder-managed tables depends on the **pinned Ponder version**, not the chain. Disposition: the **sidecar `event_confirmations` join table is the sanctioned default** (it satisfies §2.1 regardless of Ponder version and keeps writes out of Ponder-owned rows); **if** the pinned Ponder version demonstrably tolerates external UPDATEs, direct in-column materialization is permitted as an optimization. This is decidable at M2 against the installed version (M2-6 recorded it OPEN pending the pin) — **not env-gated on an RPC**. Either satisfies §12.20's O(1) watermark broadcast + materialized-column REST read. Closes OI-11 (§13); default = sidecar, optimization = version-gated. ***Version gate resolved 2026-07-11 (ponder 0.16.8): direct in-column UPDATE is NOT tolerated for hot rows — the sidecar is MANDATORY, not merely default.*** Ponder's indexing-store cache retains rows in memory across realtime blocks (cached keys are not re-read from the DB) and flushes **all** columns from the cached copy, so an external `confirmation_state` upgrade on `tokens` (mutated on every Trade) is silently reverted by that token's next flush — states flap backwards, violating §5.1/§12.20 monotonicity. The direct-UPDATE optimization is off the table for this pin. **Recorded mismatch:** the current direct-UPDATE materialization in `apps/indexer` (`confirmation.ts` / `confirmationStore.ts`) is being reworked to the sidecar `event_confirmations` join by robbed-indexer (in flight). Evidence: the decision ledger (retired 2026-07-12; history: git); indexer.md §5.1/§7.3/§10. (M2-3 / OI-6 / OI-8 / OI-11)

49. (2026-07-10) **Three-environment model ratified (user-directed): LOCAL / TESTNET / MAINNET, mapped onto Phase I / Phase T / Phase B.** The stack runs against exactly three environments, each a distinct chain + endpoint set + domain. Chain facts below are the **official Robinhood values** (docs-first, 2026-07-10: `docs.robinhood.com/chain/connecting`, cross-checked against CLAUDE.md chain facts for mainnet 4663) — **nothing is invented**; per §2 these live in **env/config, never in source** (chain id stays a compile-time constant in `@robbed/shared` asserted against the RPC, env-inventory.md §Conventions).
    - **LOCAL** (dev / test / e2e) — **anvil** + optional **mainnet-fork**; **zero credentials**. Contracts are **fresh-deployed locally**; V3/WETH are **mock or forked** (not the 4663 addresses). Maps to **Phase I** (the local stack: anvil + Ponder + Postgres + Redis + API/WS + `next dev`/`workerd` preview). No domain (`localhost`). This is the only env any agent touches without secrets.
    - **TESTNET** — Robinhood **testnet, chain id `46630`**. Public RPC `https://rpc.testnet.chain.robinhood.com`, WS `wss://feed.testnet.chain.robinhood.com` (Alchemy recommended for production-grade load: `https://robinhood-testnet.g.alchemy.com/v2/{KEY}` + `wss://robinhood-testnet.g.alchemy.com/v2/{KEY}`; public endpoints are rate-limited, not for production per Robinhood docs). Explorer `explorer.testnet.chain.robinhood.com`. Domain **`testnet.robbed.fun`**. Maps to **Phase T** (the committed portfolio-grade build, §14 Phase A — the full M0→M3 stack on testnet; no legal work, no fees collected). This **resolves the §13 "Robinhood Chain testnet parameters" open item** (chain id / RPC / explorer now furnished from official docs); the **faucet URL remains to be pulled from official docs at Phase-T deploy start** (owner: robbed-contracts) and deploy fails if unset. ***Resolved 2026-07-11:* faucet URL + drip mechanics confirmed and recorded in §12.52** (official `faucet.testnet.chain.robinhood.com`; 0.05 ETH + 5 of each stock token per 24h; Chainlink/QuickNode fallbacks); the testnet external-contract set (WETH/Safe/Multicall3/V3) is likewise §12.52.
    - **MAINNET** — Robinhood **mainnet, chain id `4663`**. Public RPC `https://rpc.mainnet.chain.robinhood.com`, WS `wss://feed.mainnet.chain.robinhood.com` (Alchemy recommended: `https://robinhood-mainnet.g.alchemy.com/v2/{KEY}` + `wss://robinhood-mainnet.g.alchemy.com/v2/{KEY}`). Explorer `robinhoodchain.blockscout.com`. Domain **`robbed.fun`**. Maps to **Phase B**, entered **only after Gate G-A passes** (§14) — mainnet is Gate-G-A-gated, not a Phase-A deliverable; the legal-wrapper open item (§13) is **blocking at G-A**.
    - **Contract-address sourcing per env (no invented addresses):** LOCAL = **fresh-deploy codegen** (the M1 broadcast writes `packages/shared/src/addresses.ts` / the indexer's `CURVE_FACTORY_ADDRESS` etc. from the local deploy artifact); TESTNET/MAINNET = versioned deploy artifacts checked into `tools/deployments/{testnet,mainnet}.json`, consumed by the same codegen. The Uniswap V3/WETH constants (§12.28, CLAUDE.md) apply to **4663 mainnet only** — on testnet they come from the testnet deploy artifact, on LOCAL from mock/fork; all three still assert at startup (fail-closed if unset), per env-inventory.md §1.
    - **DNS prerequisite (BLOCKING for the mainnet/testnet Worker custom domains):** `robbed.fun` is **registered but not yet on Cloudflare DNS** — its nameservers must be pointed to this Cloudflare account (account `0b1b0b8753489a11d35ee922961f6b72`, §12.45) before any Cloudflare Worker **custom domain** (`robbed.fun`, `testnet.robbed.fun` — a subdomain of the same zone) can be attached. Until then, Workers are reachable on their `*.workers.dev` names. This is a §13 open item (the "domain" leg of the Name/domain/brand item) with a concrete blocker recorded; it does not block LOCAL or `*.workers.dev` testing.
    - **Per-env Cloudflare Workers strategy (docs-first, 2026-07-10, `developers.cloudflare.com/workers/wrangler/environments`):** use **Wrangler named environments** (`env.testnet` / `env.production` in `apps/web/wrangler.jsonc`) over one Worker codebase, deployed with `wrangler deploy --env <name>` (via the OpenNext pipeline). Cloudflare names the deployed Worker `<top-level-name>-<env>` (e.g. `robbed-testnet`); `vars`/bindings/`routes` are **non-inheritable** and must be set per env (the `robbed-assets` R2 binding + the per-env public endpoints repeat under each). LOCAL uses `wrangler dev`/OpenNext preview, no deploy. Custom domains (`robbed.fun` on `production`, `testnet.robbed.fun` on `testnet`) attach once DNS is on Cloudflare; `*.workers.dev` in the interim.
    - **Records the model + the sourced values only; wiring is a follow-up routed to owners** (per-env `.env.*` files + `wrangler.jsonc` environments → robbed-frontend/robbed-shared; per-env addresses codegen → robbed-contracts/robbed-shared; per-env indexer/API config → robbed-indexer). Full config matrix: `docs/runbooks/environments.md`. Cross-refs `env-inventory.md` (per-var dev/testnet/prod columns already anticipate this) + `deploy-komodo-cloudflare.md`. (3-environment model)

50. (2026-07-11) **Four-page redesign recorded (user-directed, authoritative — `docs/design/robbed-redesign-plan.md`); amends §5 by reference.** The `apps/web` rebuild to the `docs/Robbed.html` mockup carries five spec deviations, hereby recorded per the plan's "Spec deviations to record" routing:
    - **(a) Four pages, not three.** **Portfolio `/portfolio` moves from §5.4 Phase-2 into v1 scope** as a read-only page (address header, TOTAL VALUE / LOOT ALL-TIME / WALLET ETH stat cells, HOLDINGS / ACTIVITY / CREATED tabs). It introduces **no new transaction types** — the 9 tx types of the M3-11 catalog are unchanged — and **no `collect()` UI surface** (LPFeeVault remains treasury-facing, no owner, sole external fn; §6.3/§6.6). §5's "Three Pages" heading and §5.4's Portfolio line are amended by this entry. Phase-2 keeps creator fee share, comments, 4337, livestreams.
    - **(b) Route rename `/launch` → `/create`** (page label "Create"). §5.3's route is amended by reference. Catalog flow IDs `LAUNCH-*` are **retained** (M3-11 stable-ID rule: IDs never encode routes; a rename would churn `@flow:` tags for zero information).
    - **(c) Brand `ROBBED_`** per §12.46 (already ratified; the redesign is the visual execution).
    - **(d) Terminal-mono skin supersedes the §12.24 shadcn look** (shadcn primitives may remain under the hood, restyled to the token set).
    - **(e) Mobile-first is the primary layout target** (§9 dark-first stands — consistent).
    - **Unchanged and non-negotiable:** all data/protocol rules — §2 no-hardcoded-metrics (the mockup's `$610K` etc. are MOCK, wire live), §6.5 sells-always-open, the §12.14 LP sentence, §12.19 upload/hash flow, §12.20 confirmation tiers, `@robbed/shared` as sole type/ABI/math source.
    - **M3-11 catalog consequence:** the ratified user-flow catalog (36 flows) was authored against the three-page baseline and remains valid; **`PORT-*` Portfolio flows are a REQUIRED catalog addendum — owner: robbed-frontend (author) + architect (ratify) — landing before the I-5a `e2e:coverage` baseline is frozen** (recorded in `apps/web/e2e/user-flows.md` §8). *Update 2026-07-11: addendum LANDED and RATIFIED — `PORT-1..8` authored (robbed-frontend) + ratified (architect) in `apps/web/e2e/user-flows.md` §3b with 8 P-7 waiver rows; 44 flows are the I-5a coverage baseline. Portfolio reads ratified as **advisory** (no WS channel, ~15s staleTime + refetch per `api.md` §3.4a — consistent with this entry's read-only scope; no live-patch obligation exists or is routed); the decision ledger (retired 2026-07-12; history: git).* (redesign / M3-11)

51. (2026-07-11) **Chainlink ETH/USD feed on Robinhood Chain 4663 CONFIRMED — addresses recorded as source of truth (closes the §12.48a env-gated leg / §13 OI-6).** Following the §12.28 precedent for externally-confirmed addresses: **proxy `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9`**, aggregator `0x6091E64eb7138EEF066a80FD3A0d7427B91f2721` (Chainlink official registry: `smartcontractkit/documentation` `chains.ts` + RDD `feeds-robinhood-mainnet.json`, ETH/USD among 66 listed feeds). **Live-verified 2026-07-11** by robbed-indexer via `eth_call` on `rpc.mainnet.chain.robinhood.com`: `description() == "ETH / USD"`, `decimals() == 8`, and a fresh `latestRoundData()` round (observation source+timestamped per §2, recorded in the decision ledger (retired 2026-07-12; history: git) — never stored or hardcoded). Per §12.48a the Chainlink branch is selected: the indexer's §3.9 `eth_usd_snapshots` poller reads this feed (source label `chainlink:4663`) with a staleness check. **Startup assertions are MANDATORY when the §3.9 poller lands: assert `description() == "ETH / USD"` and `decimals() == 8` against the configured proxy, fail-closed** — mirrors the §12.28 deploy-time V3 assertions. **Scope: 4663 MAINNET ONLY** — LOCAL and TESTNET use the documented HTTP fallback (DefiLlama/Coinbase via `ETH_USD_SOURCE_URL`, env-inventory.md §3), which also remains the configured resilience fallback on mainnet. Evidence: the decision ledger (retired 2026-07-12; history: git); indexer.md §3.9/§10. Owner: **robbed-indexer** wires the feed + assertions at M2. Closes §13 OI-6. (M2-3 / OI-6)

52. (2026-07-11) **Testnet (46630) external-contract set ratified — WETH / Safe / Multicall3 confirmed canonical; Uniswap V3 = ADOPTED verified-but-unofficial community deployment, TESTNET-ONLY.** Resolves the Phase-T inventory + the T-1 escalation ("documented substitute if Uniswap is mainnet-only there"). All addresses **live-re-verified by the architect on 2026-07-11** against `rpc.testnet.chain.robinhood.com` (`eth_chainId == 46630`); per §12.49 they are consumed from the testnet deploy artifact / `tools/m0/external.testnet.json` / env — never inlined in source.
    - **Confirmed facts (official/canonical sources + live RPC):**
      - **WETH `0x7943e237c7F95DA44E0301572D358911207852Fa`** — official (`docs.robinhood.com/chain/protocol-contracts`); TransparentUpgradeableProxy bridge pattern matching mainnet; `symbol() == "WETH"` verified live.
      - **Safe v1.4.1 canonical set** — `safe-deployments` lists 46630 as **canonical**; all six addresses carry matching bytecode (SafeProxyFactory `0x4e1DCf7AD4e460CfD30791CCC4F9c8a4f820ec67` and SafeL2 singleton `0x29fcB43b46531BcA003ddC8FCB67FFE91900C762` re-verified live); Safe{Wallet} UI supports the chain. Satisfies §6.6 (canonical Safe, never bespoke) for the T-2 testnet treasury with dev signers; the **mainnet** Safe deployment + signer set stays OPEN (O-6, §13).
      - **Multicall3** at the canonical address `0xcA11bde05977b3631167028862bE2a173976CA11` (code verified live).
      - **Blockscout v2 verifier on the testnet explorer:** no API key required; `solc v0.8.35+commit.47b9dedd` in its supported list — feeds the M1-2/O-5 pin+`cancun` check when the testnet deploy runs (the check itself stays O-5).
      - **Faucet mechanics:** official `faucet.testnet.chain.robinhood.com` drips **0.05 ETH + 5 of each stock token per 24h after verification** (Robinhood support article); verified fallbacks targeting 46630: Chainlink `faucets.chain.link/robinhood-testnet`, QuickNode `faucet.quicknode.com/robinhood/testnet`. Closes the §13 testnet-faucet leg (§12.49).
    - **THE V3 DECISION — option (i) adopted.** The official Uniswap deployments registry covers **mainnet 4663 only** (re-checked 2026-07-11: `developers.uniswap.org` Robinhood Chain page lists chainId 4663 with exactly the §12.28 addresses; no testnet page), and the §12.28 addresses carry **zero code** on 46630 (verified live). Ratified: adopt the **Blockscout-VERIFIED community V3 deployment** for TESTNET-ONLY use — **Factory `0xdf9e3D6ffaC4513dD7b053212bbECcbCD15ec932`, NonfungiblePositionManager `0xFFe6CFc4f759b65f9B62c9D05A9E21a78cE93e12`, SwapRouter02 `0xb79cB26e90EBBD9bC02c75267c9a86dBa1AFedB7`, QuoterV2 `0xDDcBe4989C8171F721c5e683C9C6339B59718213`**. The set is internally coherent and the §12.28 assertions **pass live**: `feeAmountTickSpacing(10000) == 200`, `NPM.factory()` == that factory, `NPM.WETH9()` == the official WETH. At least two OTHER **unverified** V3 clones exist on 46630 — **rejected outright**; only this verified set is sanctioned.
    - **Rationale (evidence-weighed, options ii/iii rejected):** the testnet's purpose is exercising OUR contracts' lifecycle (G-7/G-8) at zero real value; graduation correctness is **independent of who deployed the pool infra** — the migrator's mandatory §12.28 deploy-time assertions (which pass) plus the pre-seeded/polluted-pool defense proven in gate-2 invariant 6 (§10) make a hostile-ratio mint impossible by construction. Option (ii) (deploy our own V3 core/periphery from pinned bytecode) buys marginal provenance for real work and carries the same "not Uniswap-official" character; option (iii) (block Phase T on an official testnet deployment) makes Phase A (§14) hostage to a third party.
    - **Explicit trust caveat + scope limit:** the single deployer EOA `0xe7A35BD49997E10c51B5e1dea4de366dA7F831F3` (differs from the mainnet official deployer) is also the **factory owner** — it can `enableFeeAmount` / `setFeeProtocol` (i.e. skim up to the protocol-fee share of pool trading fees) but **cannot** upgrade, pause, or break existing pools, nor touch LP principal. Acceptable only because the testnet holds zero real value. **These addresses are TESTNET-ONLY — forbidden in any mainnet or LOCAL path; mainnet stays §12.28** (LOCAL stays mock/fork, §12.49).
    - **Mandatory deploy-time assertions unchanged and fail-closed on testnet too** (§12.28, contracts.md §7.2): `factory.feeAmountTickSpacing(10000) == 200`, `NPM.factory() == FACTORY`, `NPM.WETH9() == WETH` — the deploy reverts if any fails.
    - **Wiring:** values land in `tools/m0/external.testnet.json` (fixture authored by robbed-contracts; filled with these values once present) and the T-1 `constants.testnet.json` derive; runbook `docs/runbooks/testnet.md` §3/§6 updated. Owner: robbed-contracts consumes at T-3 deploy. (Phase-T inventory / T-1)

## 13. Open Items
- Domain + final logo/brand-mark treatment (project **name** RESOLVED — `ROBBED_` brand / `robbed` identifiers, §12.46; domains DECIDED — `robbed.fun` (mainnet) / `testnet.robbed.fun` (testnet), §12.49; **still open — DNS prerequisite:** `robbed.fun` registered but not yet on Cloudflare DNS, nameservers must point to account `0b1b0b8753489a11d35ee922961f6b72` before Worker custom domains attach (`*.workers.dev` until then); OG brand mark; header wordmark styling)
- Uniswap V3 addresses on 4663 **RESOLVED (§12.28)** — Factory/NPM/SwapRouter02/QuoterV2 recorded; deploy-time runtime assertions remain mandatory (contracts O-4 closed, indexer OI-13 closed, web-11 V3 leg closed). **Still open:** Safe deployment + signer set on 4663 (O-6) — pull canonical Safe from official registry or deploy; never invented; startup/deploy must fail if unset (testnet 46630: canonical Safe v1.4.1 CONFIRMED, §12.52 — dev signers only)
- Confirm compiler pin (candidate 0.8.35) **and the `cancun` EVM target (§12.44)** against Blockscout verifier before first M1 deploy — one throwaway-contract check covers both (contracts O-5)
- M0 notebook outputs (curve constants + V3 graduation tick/sqrtPrice recorded in `constants.json`, still under the §13 M0-review gate): **anti-sniper window+cap values (O-7) RESOLVED (§12.32)**; **arb-back `TOLERANCE_TICKS`/`MAX_ARB_ITERATIONS`/`MIGRATION_SLIPPAGE_BPS` param values (O-8) RESOLVED (§12.33)** — the token-leg budget rule was ratified and the numbers are now set; **the WETH-leg budget *definition* is now also CLOSED (§12.33 update, 2026-07-11):** `wethArbBudget = wethForMint × MIGRATION_SLIPPAGE_BPS / 10_000`, symmetric per-leg budgets per the M1-10/M-10-A fix, proven in gate-2 invariant 6 (the gate-6 griefing-cost quantification + UM-2 Part-2 residual stay caps-lift M4 items); **graduation caller reward (O-9) RESOLVED (§12.34)** (re-validate ≥10× `graduate()` gas at M1). All three (O-7/O-8/O-9) are **M0 defaults, tunable in the capped beta**
- Beta cap values `perTokenEthCap`/`globalEthCap` — set with hoodpad-security before mainnet beta deploy (O-10, gate 7)
- Large-value confirmation-disclosure threshold (ETH notional, config value) — **RESOLVED (§12.47, 2026-07-10):** `largeValueEthThreshold = 1.0 ETH` (M0/config default, retunable in the capped beta; ≈ 12.38% of `GRADUATION_ETH`, ≈ $1,771.51 at the §12.35 snapshot). Config keys `NEXT_PUBLIC_LARGE_VALUE_ETH_THRESHOLD` (web) + API mirror; **wiring is a hoodpad-frontend + hoodpad-indexer follow-up**, never a literal (web-10 / M3-10 closed)
- Moderation vendor (CSAM hash-match + NSFW classifier, incl. mandated-reporting legal flow, api OI-A7); bounty terms; Safe signer set (M-of-N, who — O-6); admin SIWE allowlist (follows signer set, api OI-A8)
- ETH/USD snapshot source on 4663 — **RESOLVED (§12.48a disposition + §12.51 verification, 2026-07-11):** Chainlink ETH/USD feed CONFIRMED on 4663 — proxy `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9` (aggregator `0x6091E64eb7138EEF066a80FD3A0d7427B91f2721`), live-verified via `eth_call` (`description()=="ETH / USD"`, `decimals()==8`, fresh `latestRoundData`; the decision ledger (retired 2026-07-12; history: git)). Chainlink branch selected for the §3.9 poller with **mandatory startup assertions** (description + decimals, fail-closed); 4663-mainnet-only — LOCAL/TESTNET use the documented DefiLlama/Coinbase HTTP fallback (indexer OI-6 closed)
- Robinhood RPC `safe`/`finalized` block-tag support — **RESOLVED (§12.48b disposition, verified 2026-07-11):** both tags SUPPORTED on the official 4663 RPC (`eth_getBlockByNumber` returned full Arbitrum block objects for both tags, finalized < safe < latest; verbatim probe evidence in the decision ledger (retired 2026-07-12; history: git)). The M2-6 tracker's tag path runs; **M2-3b (L1-watermark reader) stays a dormant pre-sanctioned fallback, NOT funded** (indexer OI-8 closed)
- Robinhood Chain **testnet parameters** — chain id `46630`, RPC `https://rpc.testnet.chain.robinhood.com`, WS `wss://feed.testnet.chain.robinhood.com`, explorer `explorer.testnet.chain.robinhood.com` **RESOLVED (§12.49)** from official docs (`docs.robinhood.com/chain/connecting`). **Faucet leg RESOLVED (§12.52, 2026-07-11):** official `faucet.testnet.chain.robinhood.com`, 0.05 ETH + 5 of each stock token per 24h after verification (Robinhood support article; Chainlink `faucets.chain.link/robinhood-testnet` + QuickNode `faucet.quicknode.com/robinhood/testnet` verified fallbacks). **Testnet external-contract set RESOLVED (§12.52):** WETH/Safe v1.4.1/Multicall3 confirmed canonical on 46630; Uniswap V3 = adopted Blockscout-verified community deployment, TESTNET-ONLY, mandatory deploy-time assertions + trust caveat recorded — mainnet stays §12.28
- Ponder external-write tolerance for `confirmation_state` materialization vs sidecar table — **RESOLVED (§12.48c; version gate resolved 2026-07-11):** direct in-column UPDATE is **NOT tolerated by ponder 0.16.8** for hot rows (indexing-store cache re-flushes all columns from cached copies, silently reverting external upgrades on `tokens`) — the **sidecar `event_confirmations` join table is MANDATORY**, not merely default. The current direct-UPDATE materialization in `apps/indexer` is a recorded mismatch under rework by robbed-indexer (in flight). Evidence: the decision ledger (retired 2026-07-12; history: git); indexer.md §7.3/§10 (indexer OI-11)
- M3-start runtime checks: WalletConnect projectId + Robinhood Wallet connector on 4663; `next/og`/satori under Bun self-hosting; Multicall3 presence on 4663 (web-6/7)
- **Legal wrapper / ToS jurisdiction for the frontend — (v1.2) becomes BLOCKING at Gate G-A (§14)** (MiCA/JDG consultation with a workable path required before Phase B); until G-A it is not blocking (Phase A is testnet-only, no fees collected)
- **(v1.2) Organic-volume floor + funding-cluster alert thresholds** (§2.2, §8.5, §10 gate 7) — **M0 COMPUTED + RECORDED (§12.36, 2026-07-10)** in `constants.json.governance`. Split disposition:
  - **Organic-volume floor (Gate G-A.1) STAYS OPEN as a Gate-G-A ratification input.** M0 default `N = 5` graduations-equiv / 7d (`floorWei = 5 × GRADUATION_ETH`). This magnitude is a product/policy go/no-go judgement — **requires architect + effectively user ratification before Gate G-A (§14)** and **recalibration at M2** against the indexer's real organic-volume series (§8.5). Not settled; the user weighs in at G-A.
  - **Funding-cluster alert thresholds (gate 7) → hoodpad-security.** M0 defaults `perTokenPct = 25` / `platformPct = 10` / `windowHours = 24` (advisory-only, never gates chain state); final tuning owned by hoodpad-security before the capped-beta deploy (gate 7, O-10-adjacent).
- **(v1.2) Weekly hood.fun traction snapshot** (tokens created/day, graduation count, visible volume) — feeds Gate G-A (§14); owner: indexer job once M2 lands, manual/Dune until then

## 14. Project framing & go/no-go gates (new, v1.2)

Explicit framing to resolve the business-vs-portfolio ambiguity. The project runs in **two phases with a decision gate between them.**

**Phase A — Portfolio-grade testnet build (committed, timeboxed).**
Scope: **M0 → M3 fully on Robinhood Chain testnet**, public repo from day 1, plus one technical write-up (candidate topic: bot-fleet analysis on a week-old L2 — data already in hand, extended by §8.5 output). Hard timebox: **3 weeks of part-time effort**; primary occupation (contract search) retains priority. **No legal work required in Phase A** (no mainnet, no fees collected).
Exit artifacts regardless of the Phase B decision: verified contracts + invariant suite, live indexer with bot-detection, 3-page frontend, write-up. **These stand alone as portfolio value.**

**Gate G-A — mainnet go/no-go (end of Phase A, ~3 weeks out).** Proceed to Phase B only if **ALL** hold:
1. **Market:** Robinhood Chain memecoin activity has not collapsed (organic-volume estimate from **own indexer**, not headline metrics; define floor in M0 notebook, §13).
2. **Competition:** hood.fun has not consolidated the niche (e.g., still < order-of-magnitude ahead on daily token creations, or visibly failing).
3. **Personal:** B2B contract situation resolved or on a clear path — mainnet operation does not compete with income security.
4. **Legal:** MiCA/JDG consultation completed with a workable path (legal wrapper / ToS jurisdiction, §13) — this item moves from "open" to **blocking** at this gate.

**Phase B — capped mainnet launch (conditional).** Only after G-A passes: M4 (gates 5–8), capped beta, bounty, then M5 per v1.1. **If G-A fails:** the repository is archived as a portfolio artifact, the write-up is published, loss = zero beyond the timeboxed effort.

**Relationship to the build-plan Goal** (execution tracker retired 2026-07-12; history: git)**:** the plan's Goal ("production-ready, not production-launched" — the full stack on testnet, mainnet explicitly out of goal) **is exactly Phase A**. §14 does not expand the Goal; it names the Phase-A/Gate-G-A/Phase-B structure the Goal already implied and makes the mainnet decision explicit and conditional. Nothing before G-A depends on a G-A input.
