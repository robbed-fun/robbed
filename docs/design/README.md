# docs/design — Product / Visual Design Artifacts

**Genre: DESIGN** ("what the system is", product/visual slice) — design directives and their plans, distinct from the per-service *technical* designs in `docs/services/`. Currently: [`robbed-redesign-plan.md`](robbed-redesign-plan.md) (the four-page terminal redesign, spec §12.50), whose source mockup is `docs/Robbed.html` — the mockup stays at `docs/Robbed.html` because 40+ code comments across `apps/web` + `apps/api` cite that exact path. **Owner:** robbed-architect (ratified from user directives); robbed-frontend implements. **Authority relation:** below spec/CLAUDE.md; where a design artifact and `docs/services/web.md` disagree on implementable behavior, the service doc is corrected first via ratification, never bypassed. Does **not** belong here: execution sequencing (→ `docs/plans/web-plan.md`) or copy/interaction rules already owned by web.md.
