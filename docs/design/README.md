# Design — Product / Visual Design Rationale

Design directives for the product's look and interaction model, distinct from the technical designs in [`../how-it-works/`](../how-it-works). Currently: [`robbed-redesign-plan.md`](robbed-redesign-plan.md) — the four-page terminal redesign directive (spec §12.50), whose source mockup is `docs/Robbed.html`. The mockup stays at exactly `docs/Robbed.html` — 40+ code comments across `apps/web` + `apps/api` cite that path (frozen; see [`../README.md`](../README.md)).

Where a design artifact and [`../how-it-works/web.md`](../how-it-works/web.md) disagree on implementable behavior, web.md is corrected first via ratification, never bypassed.
