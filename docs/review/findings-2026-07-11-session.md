# Review findings — 2026-07-11 adversarial commit review

Source: multi-agent adversarial review of commits `b7d8e96` / `053fd35` / `da3a060` (the 1:1 redesign + CI fixes). Each finding below survived a 3-lens refutation pass with **0 successful refutations**. Severity is the reviewer's; all are follow-ups (none block the committed work). Owner unless noted: **robbed-frontend**.

| # | File:line | Sev | Defect | Fix direction |
|---|---|---|---|---|
| 1 | `apps/web/src/widgets/trade-feed/ui/TradeFeed.tsx:235` | medium | `row.priceEth.toPrecision(2)` emits exponential notation for values < 1e-6; per-token curve prices start ~1e-9, so every early trade's PRICE column shows e.g. `9.3e-10` instead of a decimal. The same commit's `format-receive.ts` documents and avoids this exact pitfall. | route the price through the shared decimal formatter (fixed/2-sig), not raw `toPrecision`. |
| 2 | `apps/web/src/shared/lib/format.ts:57` | low | Tiny-value significant-places computed from the **pre-rounding** magnitude, so values that round up across a power of ten render 3 sig digits: `0.00009999`→`0.000100` (want `0.00010`), `signedEth(0.0999)`→`+0.100` (want `+0.10`). Misaligns tabular columns; contradicts the module's 2-sig contract. | recompute places from the rounded value (or clamp when `toFixed` rounds into the next decade). |
| 3 | `apps/web/src/shared/lib/format.ts:53` | low | `Number.prototype.toFixed` returns exponential for `|eth| >= 1e21`, so a corrupted/absurd wei value (≥1e39 wei) renders `1e+21 ETH`. Only reachable with bad indexer input; formatter contract silently broken with no guard. | guard the huge-magnitude branch (BigInt-side formatting or explicit clamp). |
| 4 | `apps/web/src/shared/lib/format.ts:84` | low | `formatPercent` renders signed zero for sub-0.05% deltas: `-0.04`→`−0.0%` (painted red by `Delta`), `+0.04`→`+0.0%` (green). Sign/tone branch keys off unrounded `pct`. | branch on the rounded value; a displayed zero should be unsigned/neutral. |
| 5 | `apps/web/src/widgets/trade-widget/ui/TradeWidget.tsx:409` | medium | The merged "Max slippage" details row (label + 3 preset chips + current % + "· deadline 10m") exceeds the 320px right rail (≈280px content width in Plex Mono); wraps/cramps at lg+ and overflows below ~355px viewports. The mockup row it copied had no chips. | give the slippage chips their own row (as the pre-merge `SlippageControl` was), or shrink/wrap deliberately. |
| 6 | `apps/web/e2e/flows/td-2.spec.ts:34` | low | `getByText(/curve fee/i)` no longer tests the trade widget — the commit changed the widget Fee row to plain `1%`; the assertion now passes only via unrelated TrustPanel copy. §5.2 quote-line fee-disclosure e2e guard is silently dead. | re-point the assertion at the widget's actual fee disclosure (`Fee 1%`), or restore explicit widget copy. |

**Already fixed (this session, commit `adb3c5d`):** the CI/`validate.sh` bun-test discovery bug (`ls` multi-glob skipped every package → ~465 tests unrun in CI) — replaced with `find`-based discovery + a ≥3-package vacuity guard. The `**`-without-globstar concern the reviewer also raised is moot under the `find` rewrite.
