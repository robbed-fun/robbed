# ROBBED_ — Consolidated Verification Findings (2026-07-09)

Source: 10 Fable verification tracks — 3 clean-room probes (web/api/indexer), adversarial multi-agent doc review (67 agents, 13 reviewers + skeptic panels), plan pre-mortem, plan DAG audit, traceability matrix, question storm (50 Q + answer-from-docs), mechanical doc-lint, plus artifacts from the interface-freeze (contracts interfaces, shared package, OpenAPI) and M0 notebook. This file is the disposition worklist for the hoodpad-architect ratification pass.

Legend: **C**ritical / **H**igh / **M**edium / **L**ow. Owner = who fixes. Decision = needs a ruling (architect) or a human call (USER).

---

## Bucket 0 — CRITICAL (block M1 freeze)

| ID | Finding | Source (convergence) | Owner / Decision |
|---|---|---|---|
| C-1 | **Treasury-pointer sell-freeze (UM-1).** Curve sell pushes the 1% fee to the owner-settable treasury via revert-on-failure `call` *before* paying the seller; a reverting treasury (compromised/buggy Safe) freezes ALL sells on ALL curves — a de-facto `pauseSells`, contradicting the "sells always open" guarantee (§6.5). | threat-model UM-1 **and** independent doc-review of contracts.md §5.7 — two independent methods, same finding | architect proposes fix (pull-payment fee escrow or failure-tolerant fee send); **USER confirms risk posture**. Reshapes every fee transfer in BondingCurve — must resolve before M1-8. |

## Bucket 1 — Self-inflicted drift (our own edits contradict later §12 decisions) — architect, no user decision

| ID | Finding | Owner |
|---|---|---|
| D-1 | CLAUDE.md hard rule "No code path may ever block curve sells" now contradicts §12.12 (ReadyToGraduate locks both directions). CLAUDE.md steers every agent → highest-priority fix. Add the §12.12 carve-out. | architect |
| D-2 | CLAUDE.md + README still say "R2 presigned uploads" — the pre-§12.19 wording whose browser-presign reading was rejected (unmoderated bytes on CDN). Change to "API-mediated R2 uploads (§12.19)". | architect |
| D-3 | spec §6.5 still presents two anti-sniper mechanisms though §12.18 resolved to the timestamp window. Amend §6.5 to the decided mechanism. | architect |

## Bucket 2 — Cross-doc contradictions causing wrong implementation / revert-after-publish — architect ruling (a few need USER)

| ID | Finding | Owner / Decision |
|---|---|---|
| X-1 | **Name limit 32 bytes on-chain (contracts.md §2.2 / ICurveFactory) vs 64 chars API+shared+OpenAPI.** 33–64-char name passes API, gets hashed+published to R2, then reverts at `createToken`. Ticker has the same char-vs-byte hazard. | architect: align API/shared to 32 **bytes** (recommended) |
| X-2 | **V3 price orientation stated backwards** in indexer.md §3.2 ("inverted if token is token0"; ratio is token1/token0 → invert when token is token1). Literal transcription = reciprocal prices for half of graduated tokens. | architect: correct the doc; fork test arbitrates |
| X-3 | **`Trade` event needs `trader` but curve buy/sell never receives trader identity** — signatures pass recipient only. Either add trader plumbing or change the event. | architect + contracts |
| X-4 | **`real_token_reserves` is NOT NULL with no maintenance path**; also balance-write double-count between Trade/Swap handlers and the "sole source of truth" Transfer handler (indexer.md §3.2/§3.4 vs §3.6). | architect + indexer |
| X-5 | **Transfer event family has no persisted per-event table** → breaks the doc's own idempotency claim (increments with no (tx,log) dedup key). | architect + indexer |
| X-6 | **`fee_collected` WS message promised in channel taxonomy but absent from the frozen message union** → fee-dashboard updates silently dropped. Add schema or drop the promise. | architect + shared |
| X-7 | **OG image fetches candles with `?limit=48`** but endpoint requires `from`/`to`, no `limit` → 400 on every OG render. | architect + web/api |
| X-8 | **architecture.md system diagram routes `graduate`/`collect` through Router**; they live on BondingCurve / LPFeeVault. Implementer wires nonexistent Router fns. | architect |
| X-9 | **Admin re-verify endpoint writes an indexer-owned table** the API is read-only on (api.md §3.6 vs §7). | architect + indexer/api |
| X-10 | **`TokenCreated` observation seam unowned**: moderation + impersonation "run at TokenCreated" but no component observes it (API has read-only DB, no subscription). | architect + indexer/api |
| X-11 | **Confirmation-tier upgrade in web.md §4 specified as per-event WS messages** vs the ratified O(1) watermark broadcast (§12.20). | architect + web |
| X-12 | **graduation recordEthDelta ordering** (contracts.md §3.4 vs §2.2): negative-delta registration vs the fully-ordered 10-step flow disagree. | architect + contracts |
| X-13 | LP-copy trailing period, `TokenDetail.creator` object-vs-address, metadata `version` frozen at `1` inside the hash (needs explicit ratification), threat-model refs citing contracts.md §§ as if spec §§. | architect |

## Bucket 3 — Plan not runnable as-is — architect (one USER item)

| ID | Finding | Owner / Decision |
|---|---|---|
| P-1 | **P0-6 gates every phase** (Phase-0 exit needs a CI run needs your push). Split: local commit (no consent) / push+CI (USER, moved to Phase I entry). | architect + **USER (push)** |
| P-2 | **M0 verify-commands don't match reality** (`generate --profile` / `constants.json` vs actual `derive` / `out/constants.json`) → `/goal` perpetually reopens a done item. | architect |
| P-3 | **M2↔I compose deadlock** (M2 db-tests need docker-compose that only Phase I builds) → hoist compose to M2 entry. | architect |
| P-4 | **M3↔I codegen circularity** (frontend needs `addresses.ts` only Phase I emits) → move codegen into M1 deploy item. | architect |
| P-5 | **hoodpad-shared roster drift**: plan + development-flow §4 omit the agent and assign `packages/shared` to indexer; still Bun-vs-pnpm unreconciled (see U-2). | architect |
| P-6 | **§5.1 search has no plan item with a verify contract** (only caught by M2 exit criterion). Add to M2-9. | architect |
| P-7 | **G-5b can livelock `/goal`**: error-path flows (slippage revert, anti-sniper cap) produce no indexer record, so the 3-layer assertion is impossible. Add per-flow assertable-layers + waivers. | architect |
| P-8 | Verify-line rot: `ROBINHOOD_RPC`→`ROBINHOOD_RPC_URL`, missing `[profile.fork]`, `grep '0.8'` matches comments, hardcoded port 3001, spec-check job skips-≠-passes. | architect |
| P-9 | Ownership: `docs/runbooks/**`, root `docker-compose.yml`, T-2 owner "ops", `docs/user-flows.md` (frontend writing architect-owned docs/) all need ratified owners. | architect |

## Bucket 4 — External-dependency risks with no fallback item — architect adds items, some USER

| ID | Finding | Owner / Decision |
|---|---|---|
| E-1 | **Uniswap V3 may not be deployed on chain 4663.** Fail-closed zero-addresses turn "registry empty" into a permanent stall at M1. Add M1-start decision: self-deploy V3 core+periphery vs descope fork gate 3. | architect + **USER** |
| E-2 | **Testnet may be unreachable / faucet can't fund $69k-parity graduation.** Add scaled testnet constants profile + deployer-key provisioning to Blocked-on-user. | architect + **USER** |
| E-3 | **Q18 (UNANSWERED): Ponder same-tx factory-child indexing.** If Ponder doesn't index a child curve's `Trade` emitted in the same tx as its `TokenCreated` registration, every atomic initial buy is silently lost (§12.15). Needs a spike before M2 is planned. | indexer spike |
| E-4 | **OI-8 negative branch (safe/finalized RPC tags) has no funded fallback item** (L1 rollup-contract watermark reader — new component, new env vars). | architect + indexer |

## Bucket 5 — Threat dispositions still open (§12 entries missing) — architect proposes, USER confirms posture

| ID | Finding |
|---|---|
| T-1 | **UM-2 grief-locked ReadyToGraduate**: revert-and-retry frozen but cost-bound-vs-timeout-fallback disposition unrecorded; near-empty pool offers no arb incentive → possible permanent freeze of curve ETH. |
| T-2 | **O-8 arb-back budget rule uncomputable on the WETH leg** (mint takes ALL remaining WETH, so "spend only inventory above mint requirement" has no defined value; $69k-parity impact unanalyzed). |
| T-3 | **Cross-entrypoint reentrancy** (refund callback → permissionless `graduate()` mid-buy) never analyzed; curve trade fns not `nonReentrant` per contracts.md §5.4. |
| T-4 | UM-9 CREATE2 curve-address squatting, UM-4 single-RPC integrity, UM-5 `javascript:`/`data:` link XSS on Token Detail — all undispositioned. |
| T-5 | threat-model §7 coverage map omits ~1/3 of §4 threats → gate-5/6/10 authors would silently skip them. |

## Bucket 6 — Operations documentation absent (out of goal, required before capped beta)

RPC failover runbook, Ponder schema-migration re-index strategy, Redis seq-reset heal stampede, lost-publish gap signal + poll cadence, graduate/collect cron + stuck-graduation alert, incident runbook. All correctly out-of-goal; must gain items before M4.

---

## Decisions requiring the USER (not architect-rulable)

1. **U-1 (C-1 risk posture):** UM-1 fix approach — pull-payment fee escrow (cleanest, more code) vs failure-tolerant fee send (smaller) vs documented trust (rejected for a public-funds launchpad).
2. **U-2 (workspace):** bun vs pnpm. You asked for pnpm; the scaffold built bun workspaces (`bun.lock`). Ratify pnpm + convert, or keep bun.
3. **U-3 (M0 economics):** ratify or adjust the proposed graduation fee (1.5% of raise), anti-sniper window (8s / 0.209 ETH), graduation caller reward (~$5).
4. **U-4 (E-1):** if V3 isn't on 4663 — self-deploy vs descope the fork gate.
5. **U-5 (git):** commit/push cadence (currently: user handles git; repo has zero commits).

## Disposition log — hoodpad-architect ratification pass (2026-07-09)

Every docket item is now dispositioned. **DECIDED** = spec §12 entry; **OBLIGATION** = delegated to owning agent (decide-it-yourself); **FIXED** = doc corrected; **DEFERRED** = roadmap; **NEEDS-USER** = surfaced. Full cross-ref in `docs/decisions.md` §5.

| ID | Disposition |
|---|---|
| C-1 / UM-1 | **DECIDED §12.25** — pull-payment fee escrow (`sweepFees`); sells un-freezable by construction. contracts.md §2.3/§3.2–3.4/§5.7/§6, threat-model §0/§4/§8/R5, CLAUDE.md updated. USER risk-posture (U-1): pull-payment approach adopted (the cleanest of the three candidates). |
| D-1 | **FIXED** — CLAUDE.md sells rule carries the §12.12 lock + §12.25 pull-payment carve-outs. |
| D-2 | **FIXED** — CLAUDE.md + README "R2 presigned uploads" → "API-mediated R2 uploads (§12.19)". |
| D-3 | **FIXED** — spec §6.5 anti-sniper collapsed to the §12.18 timestamp window. |
| X-1 | **DECIDED §12.30** — 32-byte name / 10-byte ticker source of truth; api.md/shared/OpenAPI aligned (hoodpad-shared executes schema change). |
| X-2 | **FIXED** — indexer.md §3.2 price orientation corrected (invert when token is token1). |
| X-3 | **FIXED** — `trader` plumbed into curve buy/sell (contracts.md). |
| X-4 | **FIXED** — `real_token_reserves` incremental maintenance + balance-write ownership split (indexer.md). |
| X-5 | **FIXED** — `transfers` per-event table added (indexer.md §3.6/§7.3). |
| X-6 | **FIXED** — `fee_collected` WS message defined; hoodpad-shared adds to ws-messages.ts. |
| X-7 | **FIXED** — OG uses `from`/`to`, not `limit` (web.md §6). |
| X-8 | **FIXED** — architecture diagram routes graduate→BondingCurve, collect→LPFeeVault. |
| X-9 | **FIXED** — admin re-verify via Redis `control:reverify`; indexer sole writer. |
| X-10 | **FIXED** — API moderation worker subscribes to `global:launches` for the TokenCreated seam. |
| X-11 | **FIXED** — web.md §4 uses watermark broadcasts (§12.20), not per-event messages. |
| X-12 | **FIXED** — graduate() registers `recordEthDelta(−realEth)` (contracts.md §3.4). |
| X-13 | **DECIDED §12.31** (metadata version) + **RATIFIED** (LP-copy trailing period, TokenDetail.creator object vs address). threat-model §-refs left as-is (they cite contracts.md §§, acceptable — noted). |
| P-1…P-9 | **FIXED** — plan runnable: P0-6 split (local commit orchestrator / push USER at Phase I; M0/M1 un-gated from CI), M0 `derive`/`out/constants.json`, compose→M2-0, addresses codegen→M1-14, search→M2-9, G-5b assertable-layers + `docs/user-flows-waivers.md`, verify-line rot (`ROBINHOOD_RPC_URL`, `[profile.fork]`, `solc=` grep, `${API_PORT}`, spec-check skip≠pass), owners ratified (P-9). |
| E-1 | **DECIDED §12.28** — V3 confirmed; self-deploy/descope decision moot. |
| E-2 | **NEEDS-USER** — testnet reachability + faucet funding still a Blocked-on-user item (Phase T). Unchanged. |
| E-3 | **PLANNED** — Ponder same-tx factory-child spike added as M2-0b (before M2-5). |
| E-4 | **PLANNED** — OI-8 L1-watermark fallback added as M2-3b (conditional, funded). |
| T-1 (UM-2) | **OBLIGATION** — bounded-retry + escape-hatch M1 design task (hoodpad-contracts) + gate-6 cost proof; escape hatch mandatory if griefing not proven uneconomic. threat-model §8/§8.1. |
| T-2 (O-8 WETH leg) | **OBLIGATION** — hoodpad-contracts defines + proves the WETH-side arb budget (M1); threat-model §8.1, contracts.md §3.4. |
| T-3 (reentrancy) | **OBLIGATION** — trade fns + graduate() + sweepFees() nonReentrant + CEI + cross-entrypoint unit (M1). |
| T-4 (UM-9/4/5) | **DISPOSITIONED** — UM-9 accept+document (low), UM-4 accept v1 + gate-10 disclosure (2nd RPC = roadmap), UM-5 M3 https-only allowlist + CSP + e2e obligation. threat-model §8.1. |
| T-5 | **FIXED** — threat-model §7.1 coverage-map completion addendum. |
| Bucket 6 | **DEFERRED (pre-M4)** — ops runbooks recorded in spec §11 roadmap + plan P-4. |
| U-1 | **RESOLVED** — folded into C-1/§12.25 (pull-payment). |
| U-2 | **DECIDED §12.29** — pnpm (conversion by hoodpad-shared). |
| U-3 | **DECIDED §12.26/§12.27** — graduation fee cost-based; anti-sniper v1 frozen, redesign deferred. Caller-reward number stays O-9 (M0). |
| U-4 | **DECIDED §12.28.** |
| U-5 (git) | **NEEDS-USER** — commit/push cadence; local commit is orchestrator (P0-6a), push is USER (P0-6b). |

## Meta-observation

No finding is architectural — the design is sound. Every finding is a consistency, completeness, executability, or drift gap. The largest *class* is drift introduced by our own iterative edits (CLAUDE.md/README/plan lagging the §12 decisions we made later), which is exactly what this verification pass existed to catch before code depended on it. Verdict per every probe: ~90% transcription-ready; the frozen artifacts (interfaces, shared package, OpenAPI) already resolved much of the standalone-doc ambiguity.
