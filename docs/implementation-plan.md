# ROBBED_ — Implementation Plan (master meta-plan)

**Status:** v1.0, 2026-07-09. Owned by hoodpad-architect; executed by the `/goal` command (`.claude/commands/goal.md`) as a goal-driven loop.
**Authority:** this plan is a *sequencing* of work already specified elsewhere — every item transcribes a section of `launchpad-spec.md`, `CLAUDE.md`, or a `docs/services/*.md`. Nothing here overrides spec §12; items blocked on spec §13 say so inline with the owner. If this plan and the spec/service docs disagree, they win and this plan gets fixed (development-flow.md §1).

---

## Goal (end state)

> **This Goal == spec §14 Phase A (v1.2).** The two-phase framing added in spec §14 maps exactly onto this plan: **Phase A** = this Goal (M0→M3 on testnet + verified contracts + bot-detecting indexer + 3-page frontend + write-up, "production-ready, not production-launched"); **Gate G-A** = the mainnet go/no-go at Phase-A exit (market/competition/personal/legal — §14, all NEEDS-USER); **Phase B** = M4/M5 (gates 5–8, capped beta, bounty, caps lift), which begins only if G-A passes. §14 does not expand this Goal — it names the structure the Goal already implied and makes the mainnet decision explicit and conditional. Nothing before Gate G-A depends on a G-A input.

ROBBED_ is **production-ready, not production-launched**: the entire stack (six contracts, indexer, API+WS, three-page frontend) is implemented as a transcription of the ratified service docs, runs locally with one command including seeded demo data, passes every automated test tier (Foundry unit/fuzz/invariant/fork = security gates 1–4, Vitest units, and a **Playwright e2e harness covering every user flow in the flow catalog** `docs/user-flows.md` — the goal is verified, not assumed: every transaction type and every §5 user flow demonstrably works locally end-to-end through the real stack, asserted at all three layers: on-chain state, indexed record, reconciled UI), is green in CI on all jobs, is deployed and Blockscout-verified on Robinhood Chain testnet with the full token lifecycle (create → trade → graduate → collect) exercised there, and has a prepared — not executed — production deployment: env-var inventory, deploy runbook (contracts + Safe handover + hosting), and buildable infra configs. **Mainnet launch, the capped beta (gate 7), the bug bounty (gate 8), and gates 5–10 generally are explicitly outside this goal** — they are the M4/M5 follow-up (spec §10, §11) and several are blocked on human decisions (§13: Safe signers, beta caps, moderation vendor, bounty terms).

### End-state checklist (every item objectively verifiable)

- [ ] **G-1 One-command local stack.** `bun run dev:stack` brings up Postgres+Redis(+minio) via docker compose, an anvil chain with all six contracts deployed, indexer, API, WS fanout, and web. Verify: `bun run dev:stack && bun run dev:health` exits 0 (health script checks: DB, Redis, chain RPC `eth_chainId`, indexer head advancing, `GET /v1/healthz` + `/v1/readyz`, WS handshake + sub/unsub, web `/` returns 200).
- [ ] **G-2 Seeded demo data.** `bun run dev:seed` creates ≥3 demo tokens — one fresh, one mid-curve with multi-actor trades, one graduated (with V3 swaps + a `collect()`) — so all three pages render with data. Verify: `curl -sf http://localhost:${API_PORT:-3001}/v1/tokens` (port from the stack's env, not hardcoded) returns ≥3 tokens with ≥1 `graduated: true`; Playwright smoke asserts `/`, `/t/[address]`, `/launch` render non-empty.
- [ ] **G-3 Contracts tests green (gates 1–4).** `forge test` (in `contracts/`) green (unit + fuzz + invariant); `FOUNDRY_PROFILE=fork forge test` green with **`ROBINHOOD_RPC_URL`** set (fork run requires a `[profile.fork]` block in `foundry.toml`); Slither triage has zero unexplained findings (`contracts/slither.triage.json` committed); mutation report at `contracts/reports/mutation/` shows all mutants killed or dispositioned.
- [ ] **G-4 TS tests green.** `bun test` green in `packages/shared`, `apps/indexer`, `apps/api`, `apps/web` (incl. shared golden canonicalization fixtures passing identically in all three consumers).
- [ ] **G-5 E2E green.** `bunx playwright test` (from `apps/web`) green against the G-1 stack — the full flow-catalog matrix (G-11), of which the web.md §8.2 seven scenarios are a subset.
- [ ] **G-5b Flow catalog complete & fully covered.** `docs/user-flows.md` enumerates with stable IDs every §5 user flow **and every transaction type** — `createToken` (with and without initial buy), `buy`, `sell`, `sellWithPermit`, `graduate()`, post-graduation V3 buy/sell via the widget, `LPFeeVault.collect()` — plus the error paths (slippage revert, deadline expiry, anti-sniper cap hit, sells-open-while-buys-paused, **sells-open-while-treasury-reverts (§12.25)**, metadata hash mismatch, graduating-window lock). **Each flow declares its `assertable-layers` (P-7):** happy-path transaction flows assert all three layers (anvil receipt/state, indexer record via API, UI reconciled to soft-confirmed); **error-path flows that produce no indexed record declare fewer layers** (e.g. a slippage-revert flow asserts on-chain revert + UI error state only — there is no indexer record to assert, so demanding three layers would livelock `/goal`). A **waivers file `docs/user-flows-waivers.md`** records, per flow ID, which layers are intentionally N/A and why. Every flow ID is claimed by ≥1 Playwright test (`@flow:<ID>` tag) asserting exactly its declared layers. Verify: `bun run e2e:coverage` exits 0 (fails listing uncovered flow IDs **or** flows asserting fewer layers than declared without a waiver entry).
- [ ] **G-6 CI green.** Latest run on `main` passes all jobs (changes, contracts, slither, apps, spec-check, e2e). Verify: `gh run list --branch main --limit 1` → success.
- [ ] **G-7 Testnet deployed + verified.** `tools/deployments/testnet.json` exists with all six contract addresses + Blockscout verification GUIDs; every address resolves as *verified* on the testnet Blockscout.
- [ ] **G-8 Testnet lifecycle exercised.** `docs/runbooks/testnet-lifecycle.md` records tx hashes for create → buys/sells → `graduate()` → `LPFeeVault.collect()`, each checkable on Blockscout; staging indexer indexed all of them with confirmation watermarks advancing (`GET /v1/confirmations` shows non-zero `safeBlock`).
- [ ] **G-9 Prod prepared.** `docs/runbooks/env-inventory.md` (every env var per service: purpose, secret?, source, owner) and `docs/runbooks/deploy.md` (contracts deploy order §7.2, Safe handover, indexer/API/WS/web hosting, rollback, monitoring bring-up) exist; Dockerfiles for indexer/api/web build (`docker build` exits 0 for each); prod compose/manifest files exist.
- [ ] **G-10 Spec conformance.** `/spec-check` on the final tree reports no High+ findings; no §12 decision contradicted; every remaining §13 item is either out-of-goal (M4/M5) or listed in the Blocked-on-user table below.

### Explicitly OUT of this goal (M4/M5 — do not start without user direction)

Mainnet deploy of any kind · capped beta (gate 7, incl. beta cap values O-10) · bug bounty (gate 8) · multi-model LLM audit (gate 5) · economic red-team (gate 6) · external-review decision gate (9) · known-risks doc publication (10, though its content sources exist) · caps lift (M5) · Phase 2 features (portfolio, creator fees, 4337).

---

## Conventions

- **Item format:** `- [ ] ID (owner) — what. Transcribes: <doc §>. Verify: <command / file check>.` The verification command is the contract: `/goal` re-runs it before trusting a `[x]`.
- **Marking done:** `/goal` flips `[ ]` → `[x]`, appending `— done YYYY-MM-DD; evidence: <one-line command result or commit>`. A failed re-verification re-opens the box (strike the evidence, note why).
- **Every item implicitly includes** the development-flow.md §5 loop: docs-first (context7/WebFetch for every library touched), service-doc coverage before code, tests per the doc's testing section, self-check greps, `/spec-check` on the diff, and — for any `contracts/` diff — the hoodpad-security adversarial gate. These are not repeated per item. **The mandatory `/spec-check` is the LOCAL command; CI's `spec-check` job skipping when `ANTHROPIC_API_KEY` is absent (e.g. on forks) is NOT a pass — a skipped CI spec-check never satisfies the gate (P-8).**
- **Path-ownership extension (ratified here by hoodpad-architect, process-level):** `tools/localstack/**` (compose, orchestration, health) → **hoodpad-indexer** (its services are the center of the stack); `tools/localstack/seed-chain*` + anvil/deploy glue → **hoodpad-contracts**. Root `package.json` scripts wiring these → whoever lands the item, architect sign-off. Everything else per development-flow.md §4.
- **Ownership ratifications (P-9, 2026-07-09):** root **`docker-compose.yml`** → **hoodpad-indexer** (M2-0). **`docs/runbooks/**`** → **authored by the owning item's agent** (env-inventory/deploy → hoodpad-architect; toolchain/testnet-lifecycle → hoodpad-contracts; monitoring/infra → hoodpad-indexer), architect ratifies — *not* exclusively architect. **T-2** (testnet Safe) → **hoodpad-architect + ops** (canonical Safe only, §6.6). **`docs/user-flows.md`** and **`docs/user-flows-waivers.md`** → **authored by hoodpad-frontend, ratified by hoodpad-architect** — a sanctioned exception to "docs/*.md root = architect-owned" (development-flow §4), because the frontend owns the flow surface; the catalog's authority position is recorded in development-flow.md §1. **`packages/*` + workspace config** → **hoodpad-shared** (development-flow §4).
- **Ambiguities:** never self-resolved (development-flow §3). Items marked **NEEDS-USER** stop the `/goal` loop and are surfaced.

### Blocked-on-user table (surfaced, never invented)

| Blocker | §13 item | Blocks | Workaround inside this goal |
|---|---|---|---|
| GitHub remote + push consent | — (process) | P0-6b, G-6 (CI runs on GitHub); NOT M0/M1/M2/M3 (local test tiers) | local commit (P0-6a) proceeds without consent; push is a Phase I entry item |
| WalletConnect projectId | web-6 | M3-1 full wallet matrix | injected-wallet-only dev mode; projectId env documented |
| Name / domain / brand | §13 name/brand | M3-8 OG brand mark, header | "ROBBED_" placeholder wordmark, flagged in code as `// §13 brand pending` |
| Safe signer set (M-of-N, who) | O-6 / OI-A8 | Mainnet ownership handover (P-2 documents it; M4 executes) | testnet uses canonical Safe contracts with dev signers (§6.6 satisfied: canonical, never bespoke) |
| Moderation vendor + legal flow | OI-A7 | Production moderation | stub vendors + boot guard (api.md §4.3) — sanctioned for M2 |
| Legal wrapper / ToS jurisdiction | §13 legal | Footer links | placeholder link, flagged |
| Bounty terms, beta caps | gate 8 / O-10 | M4/M5 only | out of goal |

---

## Phase 0 — Foundations

**Entry criteria:** none.
**Note on in-flight work:** P0-3 and P0-4 are being produced by concurrent agents. `/goal` must **verify, never recreate or edit** them; if incomplete when reached, wait/re-check rather than duplicating.

- [x] **P0-1** (hoodpad-architect) Claude Code hooks: `.claude/settings.json` + `.claude/hooks/` (hard-rule greps, solidity fmt). Transcribes: CLAUDE.md hard rules, development-flow §7. Verify: `test -f .claude/settings.json && test -x .claude/hooks/check-hard-rules.sh`. — done 2026-07-09; evidence: files present, PostToolUse wired.
- [x] **P0-2** (hoodpad-architect) CI pipeline `.github/workflows/ci.yml`: changes / contracts (fmt, build, test) / slither / apps (bun) / spec-check jobs. Transcribes: development-flow §5.6–5.7, contracts.md §6 gate 1. Verify: `test -f .github/workflows/ci.yml` + job names present. — done 2026-07-09; evidence: file present with all five jobs.
- [x] **P0-3** (bootstrap agent) Workspace scaffold: **pnpm workspaces (§12.29)** (`apps/*`, `packages/shared`), `contracts/` Foundry workspace (foundry.toml pinned solc 0.8.35, OZ v5 vendored, src/test/script skeleton), `tools/`. Transcribes: architecture.md §2, contracts.md §2 layout. Verify: `test -f pnpm-workspace.yaml && test -f contracts/foundry.toml && grep -q '0.8.35' contracts/foundry.toml`. — done 2026-07-10; evidence: foundry.toml (0.8.35) + pnpm-workspace.yaml + apps/{web,indexer,api}/packages/shared/tools present. *Plan text corrected bun→pnpm per §12.29 (spec wins over stale plan).*
- [x] **P0-4** (threat-model agent) `docs/threat-model.md`. Transcribes: spec §10, contracts.md §5.7, api.md §6.4. Verify: `test -f docs/threat-model.md`. — done 2026-07-10; evidence: docs/threat-model.md present (56 threats; UM-1 resolved via §12.25).
- [x] **P0-5** (hoodpad-contracts) Toolchain install + pin record: Foundry (`foundryup`), Slither, Aderyn, solhint; versions recorded in `docs/runbooks/toolchain.md`. Transcribes: contracts.md §6 gate 1, §7.1. Verify: `forge --version && slither --version && solhint --version && test -f docs/runbooks/toolchain.md`. — done 2026-07-10; evidence: VERIFY_EXIT=0 (forge 1.7.1 / slither 0.11.5 / solhint 6.2.3 / aderyn 0.6.8 / solc-select→0.8.35); `docs/runbooks/toolchain.md` + `contracts/.solhint.json` (pins 0.8.35) written. **CI-tooling note (for hoodpad-shared):** slither/solhint/aderyn are installed globally, not repo-pinned — CI gate-1 needs a setup step or catalog pin (raised, not decided).
- [x] **P0-6a** (orchestrator — no user consent needed) **Local commit** of the initial tree. Verify: `git log --oneline -1` non-empty. — done 2026-07-10; evidence: git `011a332 "init commit"` (committed by user; requirement satisfied).
- [ ] **P0-6b** (**USER**) **NEEDS-USER — moved to Phase I entry:** create the GitHub remote + push (spec §6.7/§10.8 require the repo public; CI runs on GitHub). This gates CI-dependent verification (G-6) only — it does **not** gate M0/M1/M2/M3 local work, which is verified by local `forge test`/`bun test`, never by a CI run. Verify: `git remote get-url origin` set and `gh run list --branch main --limit 1` non-empty.

**Exit criteria:** P0-3/P0-4 verified complete; `forge --version` works; tree committed locally (P0-6a). **CI is explicitly NOT a Phase-0/M0/M1 gate** — P0-6b (push + first CI run) is a Phase I entry item, so nothing before Phase I blocks on a CI run.

## Phase M0 — Parameter notebook (`tools/m0`)

**Entry criteria:** Phase 0 exit. **Driving doc:** contracts.md §4 (schema + consistency assertions), spec §11 M0, `/m0-notebook` command.

- [x] **M0-1** (hoodpad-contracts) Notebook implementation in `tools/m0/`: curve constants from **live** ETH/USD (source + timestamp recorded in output — spec §2, never hardcoded), price/mcap plots, V3 tick math for the graduation price in **both token orderings** (contracts.md §4 `v3.*`), targeting §12.11 net-of-fee `GRADUATION_ETH` sizing, and the §12.26 cost-based graduation-fee **formula/placeholder** (gas-derived, never a hardcoded USD figure). Verify: `bun run --cwd tools/m0 derive` writes `tools/m0/out/constants.json` matching the contracts.md §4 schema. — done 2026-07-10; evidence: derive exit 0, 14/14 validations pass; `graduationFeeWei` now cost-based (§12.26 formula, gas×margin), zero `%-of-raise`/hardcoded-USD residue. *Margin multiple (1.5×) is a labeled M0 placeholder — architect to ratify "thin margin" policy at M1 with real gas (§12.26).*
- [x] **M0-2** (hoodpad-contracts) `out/constants.json` validation (same assertions the deploy script runs: supply split sums to 1B, graduation reachability, sqrtPrice/tick alignment — contracts.md §4). The deriver validates inline (randomized) and supports a recompute-only mode. Verify: `bun run --cwd tools/m0 derive --reuse-snapshot` exit 0 and `test -f tools/m0/out/constants.json`. — done 2026-07-10; evidence: `derive --reuse-snapshot` exit 0, 14/14 validations (supply conservation, raise==G Δ0 wei, tick round-trips both orderings, zero dust, 25/25 fuzz graduations).
- [x] **M0-3** (hoodpad-contracts computes → hoodpad-architect records) Resolve M0-owned §13 numbers: anti-sniper window+cap (O-7 values), arb-back `TOLERANCE_TICKS`/`MAX_ARB_ITERATIONS`/`MIGRATION_SLIPPAGE_BPS` under the ratified budget rule (O-8, incl. the WETH-leg budget definition owned by hoodpad-contracts per threat-model §8.1), graduation caller reward (O-9), and the M0 input for the large-value disclosure threshold (web-10, decided by architect before M3 exit). Verify: spec §12 entries exist; corresponding `out/constants.json` fields non-zero. — done 2026-07-10; evidence: spec §12.32 (O-7 anti-sniper 8s/0.202 ETH), §12.33 (O-8 arb-back 100/8/100; WETH-leg budget kept as M1 obligation), §12.34 (O-9 caller reward ~$5 + 5× ceiling), §12.35 (web-10 M0 input) + decisions.md §5 rows + §13 closures; constants fields non-zero. *(Architect task reported "failed" on a mid-stream API stall, but edits had already landed — verified on disk.)*
- [x] **M0-4** (hoodpad-contracts computes → hoodpad-architect records) **(v1.2, spec §2.2/§8.5/§10/§14)** Define in the M0 notebook: the **organic-volume floor** for Gate G-A.1 (the memecoin-activity threshold below which mainnet is a no-go, expressed against the indexer's organic-volume estimate — never a headline metric) under the **binding ≤50% organic-flow discount assumption**; and the **funding-cluster alert thresholds** X% (per-token curve volume) / Y% (platform-wide volume) for gate-7 monitoring (§10). Verify: values recorded (notebook output + spec §13 dispositions in §12/decisions.md); referenced by M2-12 metric hooks and Gate G-A.1. — done 2026-07-10; evidence: `constants.json.governance` (organicVolumeFloor = 5×GRADUATION_ETH/7d, self-referential per §2; clusterAlertThresholds 25/10/24h), 16/16 validations; recorded spec §12.36 + decisions.md §5. **⚠ Organic-floor N=5 stays OPEN in §13 as a Gate-G-A ratification input (user weighs in before mainnet); recalibrated at M2 with real indexer series.**

**Exit criteria:** `tools/m0/out/constants.json` (testnet inputs) passes M0-2 validation; O-7/O-8/O-9 recorded in spec §12; **(v1.2)** organic-volume floor + cluster thresholds recorded (M0-4).

## Phase M1 — Contracts + gates 1–4

**Entry criteria:** M0 exit. **Driving doc:** contracts.md (all). Every item here additionally passes the hoodpad-security gate (development-flow §5.8). *Sequencing note:* spec §11 lists "testnet deploy" inside M1; this plan defers it to Phase T so the full off-chain stack can exercise the deployment — a sequencing interpretation, not a contradiction (the deliverable still lands before goal completion).

> **M1 HARD TIMEBOX (spec §11 v1.2 — pre-made decision, do not re-litigate).** If **M1-10** (`V3Migrator` incl. pre-seed defense) does **not** pass its fuzz/invariant gates (gate-2 invariant 6) **by end of week 1 of M1**, switch to the documented fallback — **V2 + LP burn (spec §6.3)** — **the same day**, then continue M1 on the V2 path. **Explicit cost of the fallback (so the pre-made decision is eyes-open):** (a) it **flips the LP copy** from the canonical §12.14 sentence to **"LP burned forever"** across CLAUDE.md/`packages/shared` `constants.ts`/web copy-lint (the only sanctioned flip — `/spec-check` rule 5); (b) it **removes post-graduation fee revenue** — `LPFeeVault` + `collect` + V3 `Collect` indexing + the `/fees` dashboard are descoped; (c) **§12.28 (V3 confirmed on 4663) means V3 availability is NOT a trigger** — only migrator gate-failure is; (d) the **pull-payment curve-fee escrow §12.25 is unaffected** (curve-side), so day-1 fee capture under bot-dominant flow (§2.2) survives either path. If the fallback fires, hoodpad-architect records it in §12 the same day and re-points the affected docs; hoodpad-contracts/-indexer/-frontend descope the V3-graduation legs. Verify (gate check on day 5 of M1): M1-10 gate-2 invariant 6 green, **or** the fallback decision is recorded in §12 + LP copy flipped.

- [x] **M1-1** (hoodpad-contracts) **O-4 already RESOLVED (spec §12.28):** wire the confirmed 4663 Uniswap V3 addresses (Factory/NPM/SwapRouter02/QuoterV2) into `out/constants.json.external.*` and implement the mandatory deploy-time **runtime assertions** (`factory.feeAmountTickSpacing(10000)==200`, `NPM.factory()`/`NPM.WETH9()` — contracts.md §7.2). Verify: `external.*` fields equal the §12.28 addresses; deploy script asserts them (M1-14 smoke fails on wrong address). — done 2026-07-10; evidence: `derive` emits §12.28 addrs (treasurySafe still 0 pending O-6); `contracts/script/lib/V3Assertions.sol` + test (6/6 pass); `forge build` green; **security gate PASS** (0 High+). Tracked informational: **F-1** (make `MockV3Factory.feeAmountTickSpacing` fee-arg-sensitive so a `V3_FEE_TIER` mutation can't survive) + **F-2** (M1-14 `Deploy.s.sol` MUST keep absolute `require(weth==0x0Bd7…AD73)` — V3Assertions binds WETH only relatively) → hoodpad-contracts.
- [ ] **M1-2** (hoodpad-contracts → architect records) **[ENV-GATED: needs funded deployer + Blockscout]** Resolve **O-5 (§13)**: verify **both the `0.8.35` solc pin AND the `cancun` evm_version (§12.44)** against robinhoodchain.blockscout.com with one throwaway contract; architect records final in §12. Verify: verification GUID in `docs/runbooks/toolchain.md`; `grep -E '^\s*solc\s*=\s*"0\.8\.[0-9]+"' contracts/foundry.toml` = 1 line; `grep -E '^\s*evm_version\s*=\s*"cancun"' contracts/foundry.toml`.
- [x] **M1-3** (hoodpad-contracts) **Interface artifacts first:** `contracts/src/interfaces/` + `errors/Errors.sol` + the codegen step emitting `packages/shared/events.json` (canonical ABI shapes, byte-for-byte per spec §12.15 / contracts.md §2 — generated artifact, owned by the contracts pipeline per architecture.md §4). Transcribes: contracts.md §2, spec §12.15. Verify: `forge build` green; `grep -q 'metadataUri' packages/shared/events.json`. — done 2026-07-11; evidence: forge build exit 0, metadataUri grep PASS; `events.json` extracted from forge artifacts (never hand-written) inside the M1-3b codegen step, fail-closed byte-for-byte cross-check vs `src/abi/events.ts` + freeze test (`packages/shared/test/events-json.test.ts`); shared 141/0, indexer 126/0, forge 130/0. Upstream-verbatim `Swap`/`Collect` events added to the vendored V3 interfaces so all fragments are artifact-extractable; barrel generator now owns the `external` re-export (idempotent regen). Note for hoodpad-shared: `fee_collected` WS reconciliation unblocked — the on-chain event is `FeesCollected` and carries no `recipient` field.
- [x] **M1-3b** (hoodpad-contracts) **(§12.38, seam sweep)** Full read-function contract ABI codegen. — done 2026-07-10; evidence: `contracts/script/codegen-abi.ts` emits 6 ABI JSON (LaunchToken 15 / CurveFactory 44 / BondingCurve 24 / Router 8 / V3Migrator 18 / LPFeeVault 4 fns) + a const-asserted `packages/shared/src/abi/index.ts` barrel (viem literal types); `forge build` green, `reserves`/`config`/`collect` greps PASS; Trust-panel + indexer reads confirmed present. **Follow-ups:** (1) hoodpad-shared repoint `"./abi"` export → `src/abi/index.ts` (done in main session); (2) **contract-vs-spec divergence** → contracts item: `config()` omits the 5 curve-shape defaults §12.38/§12.39 named (they're `internal immutable` on the factory) — **per-curve fallback via `BondingCurve` public immutables works today (§12.40d), so M3-5/M2-4b unblocked**; factory-snapshot getters are a convenience add.
- [x] **M1-4** (hoodpad-contracts) **Tests-as-spec: gate-2 skeletons BEFORE implementation.** `test/invariant/` + `test/fuzz/` files encoding all 7 gate-2 invariant rows (contracts.md §6 table) against interfaces + harness (MockArbSys etched at `address(100)`, MockWETH9, real V3 bytecode deployed locally); compiling, initially skipped/failing. Transcribes: contracts.md §6 gate 2; development-flow docs-precede-code. Verify: `forge build` green; one test file per invariant row exists (`ls contracts/test/invariant contracts/test/fuzz`). — done 2026-07-11 (verified retroactively; the skeletons matured into the live gate-2 suite); evidence: 7 invariant files present (CurveSolvency/FeeExactness/GraduationSingleFire/KNonDecreasing/NoValueExtraction/PoolGriefingNoHostileMint/PostGraduationZeroValue) + `test/fuzz/CurveMath.t.sol`; `forge test` 130/0/2 green (M1-15 run).
- [x] **M1-5** (hoodpad-contracts) `LaunchToken.sol` + unit tests (fixed 1B, ownerless, `metadataHash` immutable, no burn fn). Transcribes: contracts.md §2.1. Verify: `forge test --match-path 'test/unit/LaunchToken*'`. — done 2026-07-10; evidence: 15/15 tests; ownerless by non-inheritance, no mint/burn selectors, supply constant 1e27, permit intact; **security gate PASS** (0 High+). Tracked info: **F4** — constructor doesn't guard `metadataHash!=0` (relies on factory `ZeroMetadataHash` guard → that guard is LOAD-BEARING for M1-7, §8.3).
- [x] **M1-6** (hoodpad-contracts) `libs/CurveMath.sol` (pure, rounding-direction proofs) + direct fuzz (k non-decreasing). Transcribes: contracts.md §2.3 math. Verify: `forge test --match-path '*CurveMath*'`. — done 2026-07-10; evidence: 7/7 fuzz; **security gate PASS** — all 4 value-logic attacks (rounding extraction / k-decrease / overflow-DoS / rounding-sign inversion) refuted by hand-proof; Ceil-on-retained ≡ floor-on-payout, error accrues to curve. **⚠ M1-13 (gate-4) obligations before mutation sign-off — the suite is NOT yet mutation-adequate:** F1 (a zero-output mutant survives — add positivity/monotonicity assertions to testFuzz_Buy/Sell), F2 (Ceil→Floor is outside Gambit's reach — add a differential/hand mutant for rounding direction), F3 (raise fuzz runs + independent reference-equality check). Route to hoodpad-contracts at M1-13.
- [x] **M1-7** (hoodpad-contracts) `CurveFactory.sol` (CREATE2 staging, config snapshot model, hard caps, one-time setters, granular pauses — **no `pauseSells` exists**). Transcribes: contracts.md §2.2. Verify: `forge test --match-path '*Factory*'`. — done 2026-07-10; evidence: **security gate PASS** — CREATE2 squat-resistant (deployer-bound preimage + monotonic salt, UM-9 holds), F4 metadataHash guard enforced, one-time setters, owner reaches no live-curve economics (§6.6). Tracked: F-3 (add constructor assert `maxCallerReward+maxGraduationFee < graduationEth`), F-4 (wrong error name on metadataUri check), F-5 (FactoryConfig struct → architect ratification).
- [x] **M1-8** ✅ done 2026-07-10 — **security gate PASS; "sells always open by construction" HOLDS** (treasury read in exactly 1 place = `sweepFees()`; reverting-treasury sell proof faithful; 128k-call solvency+drain, 0 reverts; gate-2 inv 1–5,7 green). Tracked (non-blocking): **F-1** (frozen `IBondingCurve` NatSpec still says "fee→treasury" — stale push-model docs, fix before audit freeze), F-2 (pin `evm_version`), F-3 constructor assert. Original: (hoodpad-contracts) `BondingCurve.sol` (**§12.25 pull-payment fees: `accruedFees` + permissionless non-phase-gated `sweepFees()`, no treasury call on any trade path**; `trader` plumbed into `buy`/`sell` for the `Trade.trader` field, §12.15/X-3; §12.11 net-of-fee graduation clamp; §12.12 `ReadyToGraduate` two-way lock; graduate() withholds `accruedFees` + registers `recordEthDelta(−realEth)`, X-12; §12.18 timestamp anti-sniper; donation sweep) + enable gate-2 invariants 1–5 and 7 with the updated solvency (`balance ≥ realEthReserves + accruedFees`) and exact-fee (`treasuryReceipts + accruedFees == Σ fees`) forms; all trade fns + `graduate()` + `sweepFees()` `nonReentrant` (cross-entrypoint reentrancy, threat-model §8.1). Transcribes: contracts.md §2.3, §3.2–3.4. Verify: `forge test --match-path '*Curve*' --match-path 'test/invariant/*'` incl. reverting-treasury solvency-drain.
- [x] **M1-9** (hoodpad-contracts) `Router.sol` (create/buy/sell/sellWithPermit, deadline+slippage everywhere, forwards `msg.sender` as `trader`, provably pause-free sell path). Transcribes: contracts.md §2.4, §5.3. Verify: `forge test --match-path '*Router*'` incl. pause-matrix test (sells succeed under both pauses **and** with `treasury` pointed at a reverting contract — §12.25 / UM-1). — done 2026-07-10; evidence: 107/0/3 suite, Router 21/21, pause-matrix green end-to-end; **security gate PASS** (0 High+) — sell-path unfreezability holds through the Router; `sellWithPermit` deadline enforced independently of the permit. Also applied fixups **F-1** (IBondingCurve NatSpec → pull-payment), **F-3** (constructor `GraduationUnfundable` guard), **F-4** (`InvalidMetadataUri`). Tracked info: I-1 (optional — validate curve before `permit()` call).
- [x] **M1-10** (hoodpad-contracts) `V3Migrator.sol` + `LPFeeVault.sol` (pool init at create, arb-back defense, full-range mint to vault, §12.13 dust split; vault ~50 lines collect-only) + enable gate-2 invariant 6 (pool-griefing fuzz campaign: donations, sync-style, swaps, hostile concentrated liquidity). Transcribes: contracts.md §2.5–2.6, §3.4–3.5. Verify: `forge test --match-path '*Migrator*' --match-path '*Vault*' --match-path '*griefing*'`. — **done 2026-07-10; security gate + re-gate PASS (M-10-A CLOSED).** 126/0/2, invariant 6 PASS (384 griefed lifecycles vs real V3, 0 hostile mints); migrator + vault security gate PASS on their own guarantees** (no hostile mint, LP principal locked by construction, TM-T1 reverting-treasury-safe, TM-T2 parity floor). **⛔ BUT gate found HIGH M-10-A (UM-2 realized, PoC-confirmed): token-leg arb budget ≈ dust (asymmetric vs WETH leg) → token-side mispricing freezes `graduate()` cheaply/sustainably → curve locked (§12.12). Invariant 6 proves no-hostile-mint, NOT liveness.** **Part 1 FIXED + verified 2026-07-10** (symmetric token-leg slippage budget mirroring the WETH leg; 126/0/2, PoC now graduates, reverting the floor reproduces the freeze = genuine regression; liveness invariant added, 198 directed token-leg cycles) — **re-gate PASS.** **Part 2 residual** (extreme grief beyond the ~1% recoverable range still reverts→retriable) recorded as a **UM-2 caps-lift (M4) item** — 3 dispositions: (a) gate-6 economic proof [attacker locks ≳0.08 ETH to freeze 8.08 ETH, non-permanent, third-party-correctable, zero attacker profit], (b) **non-§12.12-touching hatch** [timeout → permissionlessly widen arb tolerance/iterations or corrector-assisted retry — leaves the two-way lock intact, likely-preferred], (c) §12.12-touching hatch [reopens sells → USER decision]. **Deferred to caps-lift; NOT an M1/Phase-A blocker.** Mark `[x]` when re-gate passes.
- [x] **M1-11** (hoodpad-contracts) Gate 1 complete: Slither triage file (zero unexplained), Aderyn, solhint config, `forge fmt --check`, CI greps (`block.number`, `\^0\.8`, `checkFee`, `Pausable`). Transcribes: contracts.md §6 gate 1, §5.1, §5.6. Verify: `slither contracts/ --triage-database contracts/slither.triage.json` exit 0; CI contracts+slither jobs green. — **done 2026-07-10; gate 1 PASS.** Slither 0.11.5 `0 results` (40 triaged in `slither.db.json`, detectors live), Aderyn/solhint dispositioned, fmt clean, greps clean; `slither.config.json`/`slither.triage.json`/`aderyn.toml` + CI pinned tool versions. Follow-ups: **F-2 RESOLVED 2026-07-10** — `evm_version` pinned **`cancun`** (was default `osaka`). Discovery: OZ v5.5 `Bytes.sol` uses `mcopy` (Cancun) via the mandatory `ERC20Permit→ECDSA→Strings→Bytes` chain, so pre-Cancun **fails to compile** — Cancun is unavoidable, and confirmed safe (ArbOS 20 "Atlas" added MCOPY; Robinhood ArbOS ≥32; Blockscout lists `cancun`). Build green 126/0/2 on cancun. **§7.1 "do not assume Cancun" amendment + §12 entry → hoodpad-architect (text ready).** Final Blockscout round-trip ties to M1-2/O-5. (Redundant `IUniswapV3SwapCallback` import kept — `@inheritdoc` needs the direct import.)
- [ ] **M1-12** (hoodpad-contracts) Gate 3 fork tests vs live chain: full lifecycle against real V3 Factory/NPM + real WETH `0x0Bd7…AD73`, incl. real-ArbSys smoke. Requires a **`[profile.fork]`** block in `foundry.toml` (fork url from env). Transcribes: contracts.md §6 gate 3. Verify: `FOUNDRY_PROFILE=fork forge test` green with **`ROBINHOOD_RPC_URL`** set.
- [x] **M1-13** (hoodpad-contracts) Gate 4 mutation testing (Gambit or universalmutator) on `CurveMath` + migrator arb-back; survivors dispositioned in the findings register. Transcribes: contracts.md §6 gate 4. Verify: `contracts/reports/mutation/` report present, zero undispositioned survivors. — done 2026-07-10 (verify met: `reports/mutation/` present, 0 undispositioned). **CurveMath gate-4 PASS** (universalmutator 1.14.1: 58/58 killable killed, 6 provably-equivalent; **F1/F2/F3 closed** — positivity/monotonicity + independent ceil-reference tests, injected zero-output & Ceil→Floor both killed; suite 130/0/2 @ 20k runs). **V3Migrator arb-back: local half CLOSED 2026-07-11** — the 5 enumerated kill-tests landed (`test/unit/MigratorArbBackKill.t.sol` + both-orderings harness overloads + M-10-A `slippageBps=0` freeze-regression); full 83-survivor rerun: 43 empirically killed, **adequacy 0.585 → 0.800**, remaining 40 all dispositioned (16 E / 5 DID / 5 UG / 14 fork-gated); suite 149/0/2; evidence `contracts/reports/mutation/README.md` + `scores.tsv` + rerun logs. **Sole remaining obligation:** the 14 amount-min-floor survivors ride the env-gated gate-3 fork run (M1-12, `ROBINHOOD_RPC_URL`).
- [x] **M1-14** ✅ done 2026-07-10 — `Deploy.s.sol` (constants.json loader, §7.2 order, V3 assertions + F-2 WETH require, canary create+buy, pool pre-init verified at graduation sqrtPriceX96) + `codegen-addresses.ts` → `packages/shared/src/addresses.ts` + `apps/web/src/shared/config/addresses.ts` (derived, requireAddress guard). **Local anvil smoke exit 0** (real local V3 + MockWETH9). Caught+fixed a real broadcast deadline bug. §13 gaps: O-6 treasury Safe (fails-closed `TreasurySafeUnset` until filled), testnet constants, `contracts/deployments/` gitignore (local 31337 smoke entry in the committed shared file — minor). Original: (hoodpad-contracts) `script/Deploy.s.sol` — `out/constants.json` loader, consistency + V3 runtime assertions (§12.28), deploy order steps 1–6 (canary create), verification step; smoke on local anvil; **emit deploy artifacts → shared codegen (generated `addresses` + ABIs into `packages/shared`, never hand-edited)** — moved here from Phase I (P-4) so M2/M3 have the codegen at milestone handoff (architecture.md §4/§7). Transcribes: contracts.md §7.2, §4. Verify: `anvil` + `forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast` exits 0 with canary assertions passing; generated addresses file present and consumable by indexer config + web `lib/addresses.ts`.
- [x] **M1-15** (hoodpad-security) Full-suite adversarial review of `contracts/src/`: findings register with dispositions, no open High+. Transcribes: development-flow §5.8, spec §10. Verify: `docs/security/findings-m1.md` exists; register shows zero open High+. — done 2026-07-11; evidence: independent adversarial re-review PASS, **OPEN HIGH+: 0**; register re-verification stamp appended to `docs/security/findings-m1.md`; this run: slither exit 0 (40 triaged), solhint 0 errors, `forge test` 130/0/2 (all 7 gate-2 invariants green; 2 skips = env-gated M1-12 fork tests). Caps-lift residuals recorded: M1-12 fork run, M1-13 migrator mutation adequacy 0.585, UM-2 Part-2 hatch decision (M4).

**Exit criteria:** gates 1–4 evidence green (G-3 locally); deploy artifacts + `events.json` + generated `addresses` codegen consumable by M2; M1-15 clean.

## Phase M2 — Indexer + API + WS

**Entry criteria:** M1 exit (ABIs/artifacts in `packages/shared`); **`docker-compose.yml` (Postgres+pg_trgm, Redis, minio) built and healthy** — hoisted here from Phase I (P-3) because M2 DB/Redis tests need it (see M2-0). **Driving docs:** indexer.md, api.md. Owner throughout: **hoodpad-indexer**.

- [x] **M2-0** (hoodpad-indexer) **[hoisted from I-1, P-3; brought forward to M0 for "dockerize everything"]** Root `docker-compose.yml`: Postgres (+pg_trgm), Redis, minio (R2-compatible per api.md §8) with healthchecks — needed by every M2 DB/Redis test, not just Phase I. Verify: `docker compose up -d && docker compose ps --format json` all healthy. *(I-1 in Phase I reuses this; it is not rebuilt.)* — done 2026-07-10; evidence: `docker-compose.yml` (postgres:17+pg_trgm init SQL / redis:7 / minio + mc bucket bootstrap) + `docs/runbooks/docker.md`; `docker compose config` exit 0. **SKIP (named): live `docker compose up` + healthy check deferred — Docker daemon not running in this job (env limitation, not a work gap); must be run where a daemon exists (re-checked at I-3/G-1).**
- [x] **M2-0b** (hoodpad-indexer) **Ponder same-tx factory-child indexing SPIKE (E-3, Q18 — must run before the rest of M2 is planned):** verify that Ponder indexes a child curve's `Trade` emitted **in the same tx** as its `TokenCreated` factory-registration event (the atomic initial-buy case, §12.15). If Ponder cannot, every atomic initial buy is silently lost — escalate to hoodpad-architect with the workaround (e.g. derive the initial buy from the receipt, or a same-tx ordering shim) before M2-5. Verify: a spike test (or documented Ponder-version finding in indexer.md §7.4) proving same-tx child events are captured, or an escalation recorded. — done 2026-07-11; evidence: spike PASSED, both legs — source-level (ponder 0.16.8: `isAddressMatched` `<=` registration-block matching, child resolution before log filtering on realtime + historical paths) + runnable anvil+PGlite spike (same-tx `ChildCreated`+child `Trade` captured on both sync paths, zero handler errors); recorded indexer.md §7.4; native capture, §12.41 receipt fallback retired/dormant (§12.41 annotation).
- [x] **M2-1** (**hoodpad-shared** owns the package + pnpm workspace config, §12.29; module content authored by each owner, architect ratifies `constants.ts`/`confirmation.ts`) `packages/shared` interface layer: `metadata.ts` (canonicalizer + **golden fixtures**; **name ≤ 32 bytes / ticker ≤ 10 bytes** byte-length refinements + `version:1` literal — §12.30/§12.31), `constants.ts` (chain 4663, WETH, LP sentence incl. trailing period, intervals, size caps, V3 addresses §12.28), `confirmation.ts`, `channels.ts` (**incl. `control:reverify`, X-9**), `ws-messages.ts` (**incl. the `fee_collected` message, X-6**), `api-types.ts` (`TokenCard.creator` = address, `TokenDetail.creator` = object — X-13; **`trust.organic` + holder `botFlags`/`clusterId` — DATA-GAP-1; closed `error.code` enum**), `db-rows.ts` (incl. the `transfers` row X-5 **+ §8.5 `AddressFlagsRow`/`TokenFlowStatsRow`/`CompetitorSnapshotRow`**) — module ownership per api.md §5 / development-flow §4. Verify: `bun test packages/shared` green (fixture hashes stable; byte-length refinements reject a 33-byte name). — done 2026-07-10; evidence: **88/88 tests** pass, tsc clean, openapi.yaml validates (31 schemas); all 9 gaps closed (X-1/X-5/X-6/X-9/X-13/§8.5/DATA-GAP-1/error-enum/§12.28) via a shared `text.ts` byte-length helper (byte-boundary + multibyte tests). Migration is shape-add except two tightenings: `apiError.code` now closed enum, `trust.organic` required-nullable. Consumers (api/indexer/web) must re-import — noted in their tracks.
- [x] **M2-2** (hoodpad-indexer) OpenAPI document `apps/api/openapi.yaml` transcribing api.md §3 endpoint-for-endpoint (generated from the zod schemas or authored with a CI sync check). Transcribes: api.md §3. Verify: `bunx @redocly/cli lint apps/api/openapi.yaml` exit 0 + sync test in `bun test`. — done 2026-07-11; evidence: redocly lint "valid" (4 warnings, 0 errors); lockstep sync asserted in `packages/shared/test/api-types.test.ts` (shared 130/0, api 138/0).
- [x] **M2-3** (hoodpad-indexer → architect records) **M2-start §13 checks:** OI-6 (Chainlink ETH/USD feed on 4663 — else DefiLlama/Coinbase config), OI-8 (`safe`/`finalized` RPC tag support), OI-11 (Ponder external-write tolerance vs sidecar table, against the pinned Ponder version). Verify: spec §12 entries / decisions.md dispositions recorded. — done 2026-07-11; evidence: all three checks verified + recorded — OI-6 Chainlink ETH/USD feed CONFIRMED on 4663 (proxy `0x78F3…d3A9`, spec §12.51, §13 closed), OI-8 `safe`/`finalized` tags SUPPORTED on the official RPC (§12.48b annotated, §13 closed), OI-11 sidecar MANDATORY on ponder 0.16.8 (direct UPDATE not tolerated for hot rows, §12.48c annotated); evidence base decisions.md §11 + indexer.md §7.4/§10.
- [x] **M2-3b** (hoodpad-indexer) **OI-8 negative-branch fallback (E-4), conditional:** if M2-3 finds `safe`/`finalized` tags unsupported on the Robinhood RPC, build the **L1 rollup-contract watermark reader** (batch-posted from `SequencerBatchDelivered`, finalized from node confirmations, via an L1 RPC — indexer.md §5.1) as a funded item with its own env vars (`L1_RPC_URL`, rollup/inbox addresses). Verify: if tags supported → item is N/A (recorded in M2-3); else confirmation watermarks advance from the L1 reader in the M2 confirmation suite. — done 2026-07-11 (N/A per the item's own conditional); evidence: M2-3 verified both tags SUPPORTED (decisions.md §11 probe record) — the L1-watermark reader is NOT funded and stays a dormant pre-sanctioned fallback (§12.48b).
- [x] **M2-4** (hoodpad-indexer) Ponder config + `ponder.schema.ts` (all §3 tables), offchain migrations (watermarks, eth_usd, metadata_verifications, moderation_status), startup assertions (pg_trgm, V3 addresses non-zero, WETH constant, chainId 4663). Transcribes: indexer.md §2, §3, §7.3. Verify: `bun test apps/indexer` schema tests; indexer boots against compose Postgres. — done 2026-07-10; evidence: Ponder 0.16.6 config + schema (all tables incl. transfers X-5 + §8.5 flow tables) + offchain migrations + startup asserts; tsc clean (after `ponder codegen`), `bun test` 38/0. **SKIP (named):** "boots against compose Postgres" deferred — Docker daemon not running in this job (re-checked at I-3/G-1). **CI note:** `typecheck` must run `ponder codegen && tsc` (→ hoodpad-shared). Tracked: curve-constants/read-ABI source (env interim; needs factory view ABI in shared — M1-14 codegen), M2-0b same-tx spike pending, `moderation_status` DDL shared with API.
- [x] **M2-5** (hoodpad-indexer) Handlers for all six event families + unified `trades` table + candle pipeline (6 intervals, high-water idempotency) + `rebuild` script. Transcribes: indexer.md §3–4, §7. Verify: `bun test apps/indexer` — incl. venue-continuity-across-graduation fixture and rebuild-equals-incremental test. — done 2026-07-10; evidence: 6 event families, unified `trades` (venue discriminator), 6-interval candles, rebuild script; X-2 (v3 price invert when token1) + X-4/X-5 (Transfer sole balance-truth) baked in; `bun test` 38/0 incl. continuity + rebuild==incremental + dedup no-op. Same integration flags as M2-4 (read-ABI, M2-0b spike).
- [x] **M2-6** (hoodpad-indexer) Confirmation tracker: watermark singleton, ranged materialization, `global:confirmations` broadcast, reorg notice. Transcribes: indexer.md §5, spec §12.20. Verify: `bun test apps/indexer` confirmation transition suite (monotonicity, boundary, reorg). — done 2026-07-10; evidence: indexer 72/0 incl. monotonicity/boundary/reorg/OI-8-null; O(1) watermark broadcast, `safe`/`finalized` viem fetch (no-ops gracefully if tags unsupported → M2-3b fallback). OI-11 (external UPDATE vs sidecar) recorded OPEN pending pinned Ponder version.
- [x] **M2-7** (hoodpad-indexer) Metadata verification pipeline (fetch, shared canonicalize, keccak compare, backoff, re-verify schedule; never `match` without byte comparison). Transcribes: indexer.md §6, spec §8.3. Verify: `bun test apps/indexer` match/mismatch/unfetched suite. — done 2026-07-10; evidence: match/mismatch/unfetched + byte-compare mutation-guard + backoff/cadence + `control:reverify` subscribe (X-9 sole-writer via verifier LEFT-JOIN discovery); in indexer 72/0.
- [x] **M2-8** (hoodpad-indexer) Redis publish (zero hot-path DB/RPC reads) + Bun WS fanout (`apps/api/src/ws.ts`) with hardening + no-DB-import structural test. Transcribes: indexer.md §8, api.md §6.5, §8. Verify: `bun test apps/api` incl. import-graph assertion. — done 2026-07-10; evidence: `publish.ts` (envelope + per-channel INCR seq, backfill-suppression latch, `fee_collected` X-6 + recipient≠treasury alert) + `ws.ts` WsHub (Bun native-topic fanout, sub cap, no-DB-import scan); api 113/0 + indexer publish tests. **⚠ FLAG (→ architect): Ponder-in-Node (§8) vs `Bun.RedisClient` — publish no-ops under pure Node; decide run-indexer-under-Bun vs add Node redis dep (Phase-I infra decision; hot-path logic is transport-injected).**
- [x] **M2-9** ✅ done 2026-07-10 — Hono/Bun read endpoints (search §5.1 addr+similarity modes, lists/KotH/detail w/ Trust incl. organic, trades/`:txHash`/candles/holders/fees/stats/confirmations/eth-usd); shapes imported from `@robbed/shared`; `bun test apps/api` 91/0 + tsc clean; search-builder suite (4 field classes) green. (Integration "boots against Postgres" deferred — no docker daemon.) Original: (hoodpad-indexer) API read endpoints: **`GET /v1/search` (§5.1 — name/ticker/contract/creator via `pg_trgm`, address-mode + similarity-mode, ticker boost/volume tiebreak/floor per §12.22 — was orphan A-1, now explicitly in scope, P-6),** lists (5 sorts §12.22 formulas as tunable config, 3 filters), KotH, detail with full Trust-panel payload (LP sentence from `constants.ts`), trades + `GET /v1/trades/:txHash`, candles, holders, fees, stats, confirmations, eth-usd, healthz/readyz. Transcribes: api.md §3.3–3.5. Verify: `bun test apps/api` integration snapshots against shared types, **including the search-builder suite for all four field classes + address-mode detection (api.md §8)**.
- [x] **M2-10** ✅ done 2026-07-10 — API-mediated upload (magic-byte sniff, size cap, re-encode/EXIF-strip/decode-bomb guard, content-addressed R2/minio via keccak — §12.19) + `POST /v1/metadata` (shared canonicalizer + keccak); hostile-fixture tests green in the 91/0 suite. Original: (hoodpad-indexer) Upload + metadata endpoints: API-mediated image pipeline (sniff, re-encode, content-addressed R2/minio write — §12.19), `POST /v1/metadata` (shared canonicalizer, keccak, `metadata/{hash}.json`), hostile-fixture tests. Transcribes: api.md §3.1–3.2. Verify: `bun test apps/api` upload suite (oversized, wrong magic bytes, EXIF, decode-bomb).
- [x] **M2-11** ✅ done 2026-07-10 — moderation (pluggable vendor iface + stub + prod boot guard §13 OI-A7; impersonation watchlist; visibility state machine §12.21), SIWE admin (stateless HMAC cookie + Redis nonce replay-defense + CSRF), rate limits (trusted-proxy IP), `global:launches` subscribe (X-10 TokenCreated seam), `control:reverify` publish (X-9). **Route-inventory test proves no chain-write primitive is importable** (§8.4). `bun test apps/api` 91/0. Original: (hoodpad-indexer) Moderation pipeline: vendor interfaces + stub vendors + prod boot guard (**vendor itself OPEN — §13 OI-A7, stubs sanctioned**), impersonation watchlist as dated data file (§12.23), visibility state machine (§12.21), SIWE admin + audit log + rate limits (allowlist config awaits O-6 — dev allowlist meanwhile). Transcribes: api.md §4, §6. Verify: `bun test apps/api` moderation + SIWE suites; route-inventory test proves no chain-write capability.
- [x] **M2-12** ✅ done 2026-07-10 — Prometheus registry + gate-7 hooks (head lag, publish latency, confirmation lag, fee-recipient/graduation-double-fire/2%-ceiling invariant counters, ETH/USD age, **v1.2 `funding_cluster_vol_share` vs M0 governance thresholds 25%/10%**); `/metrics` on a standalone `node:http` server; `bun test apps/indexer` incl. metrics suite. **⚠ FLAG (→architect): served on `METRICS_PORT 9464` (in-process series), not `API_PORT` — unify-metrics-surface is a cross-service decision.** Original: (hoodpad-indexer) Gate-7 metric hooks emitting (head lag, publish latency, confirmation lag, invariant alerts, **(v1.2) funding-cluster share vs the M0-4 X%/Y% thresholds**) — hooks only; alert delivery is M4. Transcribes: indexer.md §9.4, spec §10 gate 7. Verify: `curl localhost:${API_PORT:-3001}/metrics` exposes the named series (incl. cluster-share) in the integration harness.
- [x] **M2-13** ✅ done 2026-07-10 — §8.5 heuristics as SQL views + scheduled recompute-from-raw job → `address_flags`/`token_flow_stats`: funder-clustering (N=20/micro-0.001ETH), sniper (60s/1h), contract-mediated (**own Router/factory/migrator/NPM/swapRouter whitelisted**), wash (excluded from organic volume), multi-pool exits; organic-holder % + organic-volume % as ranges; advisory-only, never gates chain state (§8.4). `bun test apps/indexer` 35 new/0 (funder→farm, own-Router not flagged, wash excluded, sniper boundary). Original: (hoodpad-indexer) **(v1.2, spec §8.5)** Bot/farm detection: SQL views / scheduled jobs over the existing `trades`+`transfers` tables — funder clustering, wallet-age-vs-action (sniper), contract-mediated exec with **own-Router/contracts whitelist**, wash-loop (excluded from organic volume), same-second multi-pool exits; outputs per-address flag set + per-token **organic-holder %** / **organic-volume %**; internal flow-quality dashboard + Trust-panel feed. **Advisory labeling only — never gates chain state (§8.4).** Transcribes: indexer.md §8.5. Verify: `bun test apps/indexer` bot-heuristics suite (fixture funder-cluster → `farm` flag; own-Router trade NOT flagged programmatic; wash-loop excluded from organic volume); dashboard endpoint returns organic % for a seeded token.
- [x] **M2-14** ✅ done 2026-07-10 — weekly hood.fun snapshot job → `competitor_snapshots`, source+timestamped, no-ops when source unconfigured (never fabricates a metric §2); feeds Gate G-A.2. `bun test apps/indexer` competitor suite green. Original: (hoodpad-indexer) **(v1.2, spec §3/§13/§14)** Weekly **hood.fun traction snapshot** job (tokens created/day, graduation count, visible volume via own indexer or Dune), stored source+timestamped (never a hardcoded metric, §2) — feeds Gate G-A.2. Transcribes: spec §3 standing task, §13. Verify: job produces a dated snapshot row; API exposes it for the internal dashboard.

- [x] **M2-4b** (hoodpad-indexer) **(§12.38/§12.40d/e, seam sweep, unblocked by M1-3b)** ✅ done 2026-07-10 — replaced env-interim curve constants with **per-curve on-chain read** (`bondingCurveAbi` immutables `VIRTUAL_ETH_0`/…/`GRADUATION_ETH`/`TRADE_FEE_BPS` at `TokenCreated`, after the dedup guard = 0 RPC on reorg; `Promise.all` not multicall — Multicall3 unconfirmed §13); added `tokens.trade_fee_bps` (per-token, §12.40d) + `change24hPct` close-based anchor (§12.40e). `bun test apps/indexer` 122/0, tsc clean. **Follow-ups (→shared/api):** `TokenRow.trade_fee_bps: number` in db-rows + API reads it instead of config; host `change24h` resolver in `packages/shared` so `api/projections/card` computes Δ% (returns null today).

**Exit criteria:** indexer.md §11 and api.md §10 DoD checklists green; `bun test` green in shared/indexer/api (G-4 partial); **(v1.2)** bot-heuristics (M2-13) + hood.fun snapshot (M2-14) live.

## Phase M3 — Frontend

**Entry criteria:** M2 exit (ratified REST/WS contracts in `packages/shared`). **Driving doc:** web.md. Owner throughout: **hoodpad-frontend**. During M3, services run manually (compose + `bun dev` per app); the one-command stack lands in Phase I.

- [x] **M3-1** ✅ done 2026-07-10 — §13 checks recorded in web.md §9: **web-7 RESOLVED** (next/og moot — M3-8 uses raw satori+resvg; Multicall3 unconfirmed → parallel `readContract` fallback, no dep); **web-6 NEEDS-USER** (WalletConnect projectId is a user secret; RainbowKit has no `robinhoodWallet` export → custom WC wrapper, unverified on-device on 4663). Verify: web.md §9 updated.
- [x] **M3-2** (hoodpad-frontend) **Design system per §12.24:** shadcn/ui init (components vendored into `apps/web/components/ui/`), dark-only (`html.dark`, §12.23), semantic CSS custom-property tokens in `globals.css` `@theme` (web.md §7); token-bypass lint (no raw hex/arbitrary color values outside `globals.css`, no bespoke styling that bypasses tokens). Transcribes: web.md §7 (as amended), spec §12.24. Verify: `bun run build` green; `rg '#[0-9a-fA-F]{3,8}' apps/web --glob '!app/globals.css' --glob '!components/ui/**'` clean per the lint's allowlist. — done 2026-07-10; evidence: `next build` (16.2.10) compiles clean; shadcn vendored, dark-only, `@theme` tokens; token-lint grep clean.
- [x] **M3-3** (hoodpad-frontend) Chain/wallet/data plumbing: `lib/chain.ts` (WETH the only inline literal), generated `lib/addresses.ts`, wagmi/RainbowKit config, providers, WS client with ref-counted channels + reconnect invalidation, typed REST client. Transcribes: web.md §2.3–2.6. Verify: `bun test apps/web` + build green; address-literal grep (web.md §8.3) clean. — done 2026-07-10; evidence: vitest 26/26 + `next build` green + tsc clean; chain 4663, wagmi/RainbowKit (RainbowKit 2.2.11 React-19-compat confirmed), WS client with §12.20 watermark tiers + reconnect-invalidate-all, typed REST from `@robbed/shared`; zero inline addresses. **NEEDS-USER (web-6):** RainbowKit has no `robinhoodWallet` export — custom WC wrapper built, unverified on-device; needs WalletConnect projectId + brand icon (injected dev works).
- [x] **M3-4** (hoodpad-frontend) Discover `/`: hero (KotH), WS ticker, grid with 5 sorts / 3 filters / search, URL-state, all states. Transcribes: web.md §3.1, spec §5.1. Verify: `bun test apps/web`; build green. — done 2026-07-10; evidence: tsc clean, vitest 40/40, `next build` green (`/` dynamic); URL-state via shared zod enums, WS patches `priceEth`-only (never fabricates aggregates §2), KotH renders API ranking verbatim. **Tracked (→ shared/indexer): WS `trade` payload lacks recomputed mcap/progress/Δ% — true live aggregates need them on the trade msg (or a token-summary WS msg); grid refetches on staleTime/reconnect meanwhile.**
- [x] **M3-5** ✅ done 2026-07-10 — 112/0 tests, tsc clean, build green; FSD (views/token-detail + widgets/{trust-panel,trade-widget,price-chart,trade-feed,holder-table} + entities/{curve,holder,trade}). **Trust panel: 4 rows LIVE on-chain** (supply/reserves/threshold/per-token-fee via wagmi `useReadContracts` + shared ABIs, degrade-not-cache), 2 indexer (metadata verdict, organic), LP copy from the single constant. **Sell-never-gated proven structurally.** Venue-continuous chart (one series + graduation marker). **Follow-up (→shared): add Uniswap `QuoterV2`/`SwapRouter02` ABIs to `packages/shared/abi` — the invisible venue switch's post-grad inline V3 routing needs them (switch structure done, engine-selected + pool deep-link meanwhile); addrs confirmed §12.28.** Original: (hoodpad-frontend) Token Detail `/t/[address]`: venue-continuous chart (6 intervals, one series), TradeWidget (invisible venue switch, §12.12 two-sided graduating interstitial, sell-never-gated, anti-sniper cap surfaced), **TrustPanel all 7 rows with exact sourcing** (live reads live, LP sentence from the single constant), **(v1.2) Trust-panel organic metrics (spec §5.2): organic-holder estimate as a RANGE (never false precision) + methodology tooltip, flow-quality (flagged-cluster share of 24h curve volume), funding-cluster grouping on the top-20 holder list — all sourced from the indexer §8.5 feed, no new on-chain surface**, TradeFeed with ConfirmationBadge, HolderTable. Transcribes: web.md §3.2, spec §5.2. Verify: `bun test apps/web` (sell-gating, badge, quotes vs shared vectors; organic-estimate renders a range not a point value; funding-cluster grouping renders when the API returns cluster data).
- [x] **M3-6** ✅ done 2026-07-10 — FSD `features/launch-token` + `views/launch`; form (byte-limit zod from shared §12.30), API-mediated upload (§12.19, no browser presign), metadata pin, **§12.19 client-side hash re-verify PROVEN** (recomputes with shared canonicalizer, blocks signing on canonical-bytes OR hash mismatch, signs its OWN hash not the server's — 4 tests), atomic `createToken{deployFee+initialBuy}` via `routerAbi` → optimistic reducer → index-grace redirect (no 404 on the <1s flow), live economics + LP sentence verbatim, stepper. **139/0 tests, tsc clean, build green.** Transcribes: web.md §3.3, spec §5.3. **Follow-up (→shared): curve-quote math (TS, byte-identical to CurveMath) for the pre-deploy initial-buy preview + non-zero slippage floor — meanwhile shows ETH-spent plainly + `minTokensOut=0` (safe on the atomic FCFS leg).**
- [x] **M3-7** (hoodpad-frontend) Optimistic trade lifecycle (`lib/trades.ts` reducer): §4 state machine — reconcile-never-drop, never-final-while-soft-confirmed, WS-silence handling via `GET /v1/trades/:txHash`. Transcribes: web.md §4. Verify: `bun test apps/web` trade-reducer suite. — done 2026-07-10; evidence: pure reducer, 63/63 tests (all §4 invariants incl. fuzz proving no sequence yields posted/finalized from optimistic), tsc clean + build green; tier derived only from indexed block via §12.20 watermark, txHash sole reconcile key. Tracked (→ architect/indexer): nonce absent on trade shapes (txHash-only, fine on 4663); `/v1/trades/:txHash` 404-vs-empty could fail dropped txs faster.
- [x] **M3-8** (hoodpad-frontend) OG images: satori route per web.md §6 (chart sparkline, mcap ETH-first, progress; brand mark placeholder — **§13 name/brand pending, NEEDS-USER for final art**). Transcribes: web.md §6. Verify: OG route returns `image/png` 1200×630 in a unit/integration test. — done 2026-07-10; evidence: raw satori+resvg (not next/og — Bun-self-host safe, web-7); 7 OG tests prove PNG 1200×630 via magic-byte+IHDR parse; FSD `widgets/token-og`+`shared/lib/og`; `serverExternalPackages` for the native/wasm deps; 70/70 vitest, build green. Tracked: suggest `mcapEth` on `tokenCardSchema` (→shared/indexer; OG derives usd/ethUsd meanwhile); brand mark NEEDS-USER; og:image `<meta>` wiring is verified at M3-5 (page exists).
- [x] **M3-9** ✅ done 2026-07-10 — one consolidated CI-blocking copy-lint suite (7 checks over app/+src/: LP-sentence-single-constant, no forbidden LP verb, no exchange/instant framing, no USD literals, no inline addresses, no raw color outside globals/kit, **+ FSD import-boundary**), each proven failing-if-violated (injected + caught + removed); `token-lint.test.ts` folded in. `bun run test` 140/0 green.
- [x] **M3-10** (hoodpad-architect) Decide + record **web-10** (large-value confirmation-disclosure threshold, config value) — required before M3 exit (§13). Verify: spec §12 entry; config key present in web + api. — done 2026-07-11; evidence: spec §12.47 (1.0 ETH, recorded 2026-07-10) + §13/decisions.md rows; web key `NEXT_PUBLIC_LARGE_VALUE_ETH_THRESHOLD` consumed (`apps/web/src/shared/lib/env.ts` → trade-widget `large-value.ts`, default 1.0); API mirror `LARGE_VALUE_ETH_THRESHOLD` added to `apps/api/src/config.ts` (Zod default `"1.0"` + `largeValueEthThresholdWei`; tsc clean, 138/138 api tests) + env-inventory §2 row. Value-only follow-up (→frontend): populate `.env.example` `=1.0` (code default already correct).
- [x] **M3-11** ✅ authored 2026-07-10, **RATIFIED by robbed-architect 2026-07-11** — `docs/user-flows.md` (**36 flows**: DISC-1..4, TD-1..12, LAUNCH-1..3, COLLECT-1, ERR-1..14) covering all 9 tx types + 15 error paths (incl. sells-open-while-treasury-reverts §12.25 as full 3-layer); each with `@flow:<ID>`, actors, preconditions, steps, declared `assertable-layers`; `docs/user-flows-waivers.md` for <3-layer flows (P-7). §5-bullet→flow-ID traceability: §5.1 5/5, §5.2 8/8, §5.3 5/5 — **no gaps**. Feeds the I-5 e2e harness (env-gated). Transcribes: spec §5, web.md §3–4. — done 2026-07-11; evidence: sign-off lines filled in both docs (16 waiver rows = exactly the <3-layer flows; ERR-4/5 full 3-layer verified); ratified **as amended** for the §12.50 four-page redesign (`/launch`→`/create` route strings, `LAUNCH-*` IDs retained); **OPEN addendum recorded (user-flows.md §8 + §12.50): `PORT-*` Portfolio flows — owner robbed-frontend, due before the I-5a `e2e:coverage` baseline freezes**.

**Exit criteria:** web.md DoD green except the Playwright-on-stack rows (deferred to I-5); `bun run build` green under Bun.

## Phase I — Integration: one-command local stack, seed, e2e

**Entry criteria:** M1 deploy script, M2 services boot, M3 builds. **Plus P0-6b (USER):** GitHub remote created + pushed so CI can run (this is the point where CI first executes — nothing before Phase I gated on it).

- [ ] **P0-6b** (**USER**) — see Phase 0: create the GitHub remote + push. Verify: `git remote get-url origin` set; first CI run exists.
- [ ] **I-1** (hoodpad-indexer) One-command wrapper over the **M2-0** `docker-compose.yml` (Postgres+pg_trgm, Redis, minio — already built at M2 entry, not rebuilt here): ensure it is part of `dev:stack` bring-up. Verify: `docker compose up -d && docker compose ps --format json` all healthy.
- [ ] **I-2** (hoodpad-contracts) Local chain bring-up `tools/localstack/chain.ts`: anvil (fork of Robinhood Chain when `ROBINHOOD_RPC_URL` set — real V3/WETH; else local deploy of V3 core/periphery bytecode + WETH9 per contracts.md §6 harness), run `Deploy.s.sol` (which **emits the shared `addresses`/ABI codegen** — that codegen is authored in M1-14, P-4; here it is re-run against the local deployment). Verify: script exit 0; generated addresses file exists and is imported by indexer config + web `lib/addresses.ts`.
- [ ] **I-3** (hoodpad-indexer) One-command orchestration: root scripts `dev:stack` (compose → chain → indexer → API → WS → web, readiness-gated) and `dev:health` (checks per G-1). Verify: **G-1 command passes from a clean checkout**.
- [ ] **I-4** (hoodpad-contracts, chain leg; hoodpad-indexer wires the script) Seed script `dev:seed`: ≥3 demo tokens through the **real launch path** (upload → metadata → `Router.createToken`), multi-actor buys/sells on one, one driven to `GRADUATION_ETH` → `graduate()` → V3 swaps → `collect()`. Verify: **G-2 commands pass**.
- [ ] **I-5a** (hoodpad-frontend, chain helpers by hoodpad-contracts) **E2E harness infrastructure:** wagmi **mock connector** wired to anvil's funded dev accounts (real txs + real signatures, no browser-extension automation — the standard anti-flake pattern), anvil time-warp helpers (`evm_increaseTime`/`evm_mine` for anti-sniper window + graduation scenarios), `@flow:<ID>` test tagging convention, and the `e2e:coverage` script that diffs catalog IDs against tagged tests **and checks each tagged test asserts its declared `assertable-layers` (honoring `docs/user-flows-waivers.md`) so error-path flows never livelock the loop (P-7).** Transcribes: web.md §8.2, docs/user-flows.md (M3-11). Verify: harness boots against the G-1 stack; `bun run e2e:coverage` runs (may list uncovered IDs at this point).
- [ ] **I-5b** (hoodpad-frontend) Playwright **full flow-catalog matrix** green against the stack: every flow ID in docs/user-flows.md covered — all transaction types and all user flows verified locally at three layers (the seven web.md §8.2 scenarios are the core subset). Verify: `bunx playwright test` green + `bun run e2e:coverage` exit 0 (**G-5, G-5b**).
- [ ] **I-6** (hoodpad-indexer + architect) CI e2e job: compose + anvil + stack + Playwright in CI; wire into `ci.yml`. Verify: **G-6** — all CI jobs green on main.

**Exit criteria:** G-1, G-2, G-5, G-5b, G-6 pass.

## Phase T — Testnet deploy + lifecycle (fulfills spec §11 M1 "testnet deploy" line)

**Entry criteria:** Phase I exit; O-5 pin confirmed (M1-2). **§13 note:** Robinhood Chain **testnet parameters (chain ID, RPC, Blockscout URL, faucet)** are pulled from official Robinhood docs at T start — never invented; startup/deploy fails if unset (recorded as a §13 item).

- [ ] **T-1** (hoodpad-contracts) Testnet constants: `bun run --cwd tools/m0 derive` refreshed with live inputs + testnet `external.*` addresses (V3 registry on testnet, or documented substitute if Uniswap is mainnet-only there — escalate to architect if so, do not improvise) → `tools/m0/out/constants.json`. Verify: `bun run --cwd tools/m0 derive --reuse-snapshot` exit 0 (M0-2 validation) on the testnet output.
- [ ] **T-2** (hoodpad-architect + ops) Testnet treasury Safe: verify/deploy **canonical** Safe contracts (never bespoke — §6.6) with dev signers; record address in constants. Mainnet signer set stays **OPEN §13 O-6 (NEEDS-USER)**. Verify: Safe address responds to `getOwners()` on testnet RPC.
- [ ] **T-3** (hoodpad-contracts) Deploy + verify: `Deploy.s.sol` steps 1–8 on testnet incl. canary launch and Blockscout verification of all six contracts + canary token/curve; write `tools/deployments/testnet.json` (addresses + GUIDs). Transcribes: contracts.md §7.2. Verify: **G-7**.
- [ ] **T-4** (hoodpad-contracts) Lifecycle exercise: scripted create → buys/sells → clamp-to-threshold → `graduate()` (arb-back path observed) → V3 swap → `collect()`; tx hashes into `docs/runbooks/testnet-lifecycle.md`. Verify: **G-8** first half.
- [ ] **T-5** (hoodpad-indexer) Staging stack against testnet: indexer backfill from deploy block, API + web env pointed at testnet; confirms OI-8 behavior against the real RPC (`safe`/`finalized` tags) and metadata verification against real R2. Verify: **G-8** second half (`/v1/confirmations` advancing; lifecycle rows indexed with correct venue continuity).

**Exit criteria:** G-7, G-8 pass.

## Phase P — Production preparation (prepared, not launched)

**Entry criteria:** Phase T exit.

- [ ] **P-1** (hoodpad-architect, inputs from all agents) `docs/runbooks/env-inventory.md`: every env var per service (indexer.md §2, api.md, web.md §2.3) — purpose, secret?, source, owner, dev/testnet/prod values-or-placeholders; `.env.example` per app kept in sync (CI check). Verify: **G-9** env leg.
- [ ] **P-2** (hoodpad-architect + hoodpad-contracts) `docs/runbooks/deploy.md`: mainnet contracts deploy order (contracts.md §7.2 with beta caps ACTIVE per gate 7 — values O-10 **NEEDS-USER/security, out of goal**), Safe creation + `transferOwnership`/`acceptOwnership` handover choreography (signer set O-6 **NEEDS-USER**), indexer/API/WS/web hosting steps, DNS/CDN/R2 setup, rollback procedures, monitoring bring-up (indexer.md §9.4). Verify: **G-9** runbook leg — doc exists covering each enumerated section.
- [ ] **P-3** (hoodpad-indexer + hoodpad-frontend) Infra configs: Dockerfiles (indexer Node container per spec §8; api + web on Bun), prod compose/manifests (host-agnostic — concrete host is an M4 decision per architecture.md §6), monitoring/alert-rule config files for the gate-7 metrics. Verify: `docker build` exits 0 for all three images; config files present.
- [ ] **P-4** (hoodpad-architect) M4/M5 handoff register: consolidated list of everything intentionally left for launch (gates 5–10 checklist, §13 human decisions, beta cap process, **the anti-sniper decaying+size-based redesign §12.27**, and the **pre-M4 operations runbooks — RPC failover, Ponder schema-migration re-index strategy, Redis seq-reset heal stampede, lost-publish gap signal + poll cadence, graduate/collect cron + stuck-graduation alert, incident runbook — findings Bucket 6, sources exist today**) appended to `docs/runbooks/deploy.md`. Verify: section exists and enumerates each Bucket-6 runbook + §12.27; **G-10** `/spec-check` clean on the final tree.

**Exit criteria:** the Goal end-state checklist (G-1…G-10) is fully green. Goal complete; M4 (gates 5–8, capped beta) begins only on explicit user direction.

---

## Gate G-A — mainnet go/no-go (spec §14, v1.2) — the Phase-A ⇄ Phase-B boundary

**Gate G-A sits exactly at Goal completion** (Phase P exit above = spec §14 Phase-A exit). It is **NEEDS-USER in full** — the `/goal` loop stops here; it neither evaluates nor decides G-A. Phase B (M4/M5) is entered only if the user confirms **all four** conditions hold:

| # | Condition | Evidence source (in-goal artifacts feed it) |
|---|---|---|
| G-A.1 | **Market** — Robinhood Chain memecoin activity not collapsed | own indexer's **organic-volume estimate** (§8.5, built M2-13) vs the **organic-volume floor** set in M0 (M0-4) — never headline metrics |
| G-A.2 | **Competition** — hood.fun has not consolidated the niche | **weekly hood.fun traction snapshot** (M2-14 job / manual until M2) vs the daily-token-creation order-of-magnitude test |
| G-A.3 | **Personal** — B2B contract situation resolved / on a clear path | USER only |
| G-A.4 | **Legal** — MiCA/JDG consultation done with a workable path | USER only; the §13 legal item **moves from open → blocking at this gate** |

If G-A **fails**: repo is archived as a portfolio artifact, the write-up is published, loss = zero beyond the timeboxed effort (§14). If G-A **passes**: Phase B = M4/M5 per v1.1 (spec §10 gates 5–10, capped beta, bounty, caps lift). **`/goal` does not cross this line without explicit user direction.**

### Blocked-on-user additions (v1.2)

| Blocker | §14/§13 item | Blocks | Workaround inside this goal |
|---|---|---|---|
| Gate G-A decision (market/competition/personal/legal) | §14 | Phase B (M4/M5) entry only | none needed — Phase A (this Goal) is fully independent of G-A inputs |
| MiCA/JDG legal consultation | §13 (blocking at G-A) | Phase B | none in Phase A (testnet-only, no fees collected) |

---

## Cross-cutting tracker

| Concern | Items |
|---|---|
| Interface artifacts (shared schemas, OpenAPI, events.json) | M1-3, M2-1, M2-2 |
| Tests-as-spec before implementation | M1-4 (invariant/fuzz skeletons precede all Solidity) |
| Local stack / one command | I-1, I-2, I-3 |
| Seed script | I-4 |
| Deploy runbook + env inventory | P-1, P-2 |
| §13 resolution checkpoints | M0-3, M0-4, M1-1, M1-2, M2-3, M3-1, M3-10, T-2 |
| Security gates in-goal (1–4) | M1-4…M1-13, M1-15 |
| **(v1.2) Adversary/organic-flow (spec §2.2/§8.5/§14)** | M0-4 (floor + cluster thresholds), M1 hard timebox, M2-13 (bot heuristics + dashboard), M2-14 (hood.fun snapshot), M3-5 (Trust-panel organic metrics), Gate G-A |
