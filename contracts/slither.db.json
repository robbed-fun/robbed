[
  {
    "id": "8cd8c192717a416a82120f37d95b13eb9dc32a935f62530f00e1025730e94631",
    "check": "arbitrary-send-eth",
    "impact": "High",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/CreatorVault.sol#L82-L90",
    "description": "CreatorVault.claim(address) (src/CreatorVault.sol#82-90) sends eth to arbitrary user\n\tDangerous calls:\n\t- (ok,None) = creator.call{value: amount}() (src/CreatorVault.sol#86)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE (\u00a712.63). CreatorVault.claim(creator) sends the creator's OWN accrued balance (balanceOf[creator]) to that SAME `creator` address \u2014 the destination is the fee-earner, never caller-supplied or arbitrary. Permissionless in WHO calls (pays gas), FIXED in WHERE funds go (mirrors LPFeeVault.collect \u2192 fixed treasury, an accepted pattern). nonReentrant + CEI (balance zeroed before the send). A hostile creator only reverts its own claim (retriable), never a trade."
  },
  {
    "id": "ce84859cf1a48b4d7cde8b9488792a0f4ed7a620c578f89b37d5ca2fd790c21b",
    "check": "arbitrary-send-eth",
    "impact": "High",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L237-L240",
    "description": "Router._sendEth(address,uint256) (src/Router.sol#237-240) sends eth to arbitrary user\n\tDangerous calls:\n\t- (ok,None) = to.call{value: amount}() (src/Router.sol#238)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Router._sendEth is `private` and reached from exactly one site (createToken, the creation-fee push) with `factoryContract.treasury()` \u2014 a factory-configured address, NEVER caller-supplied (spec \u00a74.1). Guarded by nonReentrant + CEI. Not an arbitrary send. contracts.md \u00a72.4/\u00a75.6."
  },
  {
    "id": "b8349b8f441d3c85ccc4620bffffd21a51808564f3d2b8d8219e224fc4927f67",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L321-L324",
    "description": "V3Migrator._withinTolerance(V3Migrator.Ctx) (src/V3Migrator.sol#321-324) has external calls inside a loop: (None,curTick,None,None,None,None,None) = IUniswapV3Pool(c.pool).slot0() (src/V3Migrator.sol#322)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "b2e37bb90c2f2549b75e5757d07b9f0a0baf4a93cd1b0cd4625bad336e7e8d81",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: IUniswapV3Pool(c.pool).swap(address(this),zeroForOne,_toInt256(budget),c.targetSqrt,) (src/V3Migrator.sol#358)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "2231ceca57325dadf30a57fc0a0ee2a3eefa04dde46eec20e94774819e6e572e",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: wethArbSpent + (wethBefore - IERC20(weth).balanceOf(address(this))) (src/V3Migrator.sol#361)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "89f1b34e7e24901e7cbc10b359b36fa94f80b8e9370d48536168b6b60cff5bb8",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: inputAsset = IUniswapV3Pool(c.pool).token0() (src/V3Migrator.sol#331)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "86e4a35ad6524a6750f52814ce271a140153bcd08ba289d0712a37adf89f8c19",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: bal = IERC20(c.token).balanceOf(address(this)) (src/V3Migrator.sol#350)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "6e580c2e91cbccfecbea9213293aabdc8c376c3259ec72ac5dc4d4015d4f11f0",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: (curSqrt,None,None,None,None,None,None) = IUniswapV3Pool(c.pool).slot0() (src/V3Migrator.sol#328)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "82828025cbdad809c3877014b5a0f34393eaf02f425441ec66946b4e8bb3e041",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: wethBefore = IERC20(weth).balanceOf(address(this)) (src/V3Migrator.sol#357)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "8494cb330e150e85b125937c64321dbb4f66aad1e9bc4d07863fa2e42f9aedb3",
    "check": "calls-loop",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) has external calls inside a loop: inputAsset = IUniswapV3Pool(c.pool).token1() (src/V3Migrator.sol#331)\n\tCalls stack containing the loop:\n\t\tV3Migrator.migrate(address)\n\t\tV3Migrator._arbToTarget(V3Migrator.Ctx)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "INTENTIONAL / ACKNOWLEDGED. The bounded arb-back loop (MAX_ARB_ITERATIONS) inherently queries slot0()/balances and calls swap() per iteration \u2014 iterative price correction of a pre-seeded pool (spec \u00a76.3.2). Bounded iteration count \u2192 no unbounded-gas griefing; every call targets the trusted V3 pool / WETH. Budget exhaustion reverts cleanly (curve stays ReadyToGraduate, retriable) \u2014 never a hostile mint."
  },
  {
    "id": "4fb2ec057510a98aa6ee685e171e778dfcbb6c3c2dd9f55dccb16798b65c44a5",
    "check": "incorrect-equality",
    "impact": "Medium",
    "confidence": "High",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) uses a dangerous strict equality:\n\t- budget == 0 (src/V3Migrator.sol#355)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `m.liquidity==0` (revert InsufficientLiquidityMinted) and `budget==0` (revert ArbBudgetExceeded) are exact zero-guards on integers derived from trusted V3/NPM returns and in-contract math \u2014 strict equality is the correct and intended semantics, not a dangerous balance/timestamp equality."
  },
  {
    "id": "ef38a4e66f8892a7f9d1dbc64ff6b32a4aad7c493225f3188524f1e172a91057",
    "check": "incorrect-equality",
    "impact": "Medium",
    "confidence": "High",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L386-L441",
    "description": "V3Migrator._mintAndSettle(V3Migrator.Ctx) (src/V3Migrator.sol#386-441) uses a dangerous strict equality:\n\t- m.liquidity == 0 (src/V3Migrator.sol#433)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `m.liquidity==0` (revert InsufficientLiquidityMinted) and `budget==0` (revert ArbBudgetExceeded) are exact zero-guards on integers derived from trusted V3/NPM returns and in-contract math \u2014 strict equality is the correct and intended semantics, not a dangerous balance/timestamp equality."
  },
  {
    "id": "c41165234b8afa01b8badb9effdd36b7346a5c6a5b01c2ea1e2690842d1f6e14",
    "check": "missing-zero-check",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/CreatorVault.sol#L82",
    "description": "CreatorVault.claim(address).creator (src/CreatorVault.sol#82) lacks a zero-check on :\n\t\t- (ok,None) = creator.call{value: amount}() (src/CreatorVault.sol#86)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE (\u00a712.63). A zero `creator` is unreachable-with-value: deposit(creator) reverts ZeroAddress on a zero creator, so balanceOf[address(0)] is always 0, and claim() guards the send with `if (amount != 0)` \u2014 a zero-balance claim sends nothing. No ETH can reach address(0). A redundant zero-check would only add bytecode to the minimal-surface vault (spec \u00a76.6)."
  },
  {
    "id": "de1ffaededec95de34ee952e37569026d15b44d701da1299ca20e92cb558fc2b",
    "check": "reentrancy-balance",
    "impact": "High",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L386-L441",
    "description": "Reentrancy in V3Migrator._mintAndSettle(V3Migrator.Ctx) (src/V3Migrator.sol#386-441):\n\tExternal call allowing reentrancy:\n\t- _mint(m) (src/V3Migrator.sol#432)\n\t\t- (m.tokenId,m.liquidity,m.used0,m.used1) = INonfungiblePositionManager(positionManager).mint(INonfungiblePositionManager.MintParams({token0:m.token0,token1:m.token1,fee:FEE_TIER,tickLower:TICK_LOWER,tickUpper:TICK_UPPER,amount0Desired:m.amt0Desired,amount1Desired:m.amt1Desired,amount0Min:m.amt0Min,amount1Min:m.amt1Min,recipient:vault,deadline:block.timestamp})) (src/V3Migrator.sol#446-461)\n\tBalance read before the call:\n\t- wethBal = IERC20(weth_).balanceOf(address(this)) (src/V3Migrator.sol#398)\n\tPossible stale balance used after the call in a condition:\n\t- m.liquidity == 0 (src/V3Migrator.sol#433)\n\t\t- stale variable `REF_467`\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. The flagged external call is NPM.mint() to the real, trusted, immutable Uniswap NonfungiblePositionManager (\u00a712.28 pinned address, asserted at deploy). The 'stale' tokenBal/wethBal feed only amount0/1Desired and the post-mint `m.liquidity==0` guard (InsufficientLiquidityMinted \u2014 defense-in-depth). LaunchToken & WETH have no transfer hooks; the only reentrancy surface (the pool swap callback) is closed by the `_activePool` guard in _arbToTarget, which has fully returned before mint. No attacker-controlled reentry. contracts.md \u00a72.5/\u00a75.4."
  },
  {
    "id": "38181ac98b941ae1c26673223ec48e99bc887322a49c2a8d74a49092c55411e5",
    "check": "reentrancy-balance",
    "impact": "High",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L386-L441",
    "description": "Reentrancy in V3Migrator._mintAndSettle(V3Migrator.Ctx) (src/V3Migrator.sol#386-441):\n\tExternal call allowing reentrancy:\n\t- _mint(m) (src/V3Migrator.sol#432)\n\t\t- (m.tokenId,m.liquidity,m.used0,m.used1) = INonfungiblePositionManager(positionManager).mint(INonfungiblePositionManager.MintParams({token0:m.token0,token1:m.token1,fee:FEE_TIER,tickLower:TICK_LOWER,tickUpper:TICK_UPPER,amount0Desired:m.amt0Desired,amount1Desired:m.amt1Desired,amount0Min:m.amt0Min,amount1Min:m.amt1Min,recipient:vault,deadline:block.timestamp})) (src/V3Migrator.sol#446-461)\n\tBalance read before the call:\n\t- tokenBal = IERC20(c.token).balanceOf(address(this)) (src/V3Migrator.sol#397)\n\tPossible stale balance used after the call in a condition:\n\t- m.liquidity == 0 (src/V3Migrator.sol#433)\n\t\t- stale variable `REF_467`\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. The flagged external call is NPM.mint() to the real, trusted, immutable Uniswap NonfungiblePositionManager (\u00a712.28 pinned address, asserted at deploy). The 'stale' tokenBal/wethBal feed only amount0/1Desired and the post-mint `m.liquidity==0` guard (InsufficientLiquidityMinted \u2014 defense-in-depth). LaunchToken & WETH have no transfer hooks; the only reentrancy surface (the pool swap callback) is closed by the `_activePool` guard in _arbToTarget, which has fully returned before mint. No attacker-controlled reentry. contracts.md \u00a72.5/\u00a75.4."
  },
  {
    "id": "449b90720dcd293ee4b93c221a51dabf1b4bfed0f61fb02e2a3296a61954ecfd",
    "check": "reentrancy-benign",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/BondingCurve.sol#L174-L267",
    "description": "Reentrancy in BondingCurve.buy(address,address,address,uint256) (src/BondingCurve.sol#174-267):\n\tExternal calls:\n\t- ICurveFactory(factory).recordEthDelta(int256(net)) (src/BondingCurve.sol#239)\n\tState variables written after the call(s):\n\t- _realTokenReserves = realTok - tokensOut (src/BondingCurve.sol#253)\n\t- _virtualEthReserves += net (src/BondingCurve.sol#250)\n\t- _virtualTokenReserves -= tokensOut (src/BondingCurve.sol#251)\n\t- accruedCreatorFees += creatorFee (src/BondingCurve.sol#255)\n\t- accruedFees += fee (src/BondingCurve.sol#254)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "BENIGN (Slither's own classification). State written after a call to a trusted contract (factory/migrator/NPM) with no security relevance and no attacker reentry path (nonReentrant entrypoints; trusted callees; no token hooks). Same trust analysis as reentrancy-no-eth above."
  },
  {
    "id": "748c1f7de4001e189dd94c3c26e0751411c3754793e86d3ebcf588d666b7532a",
    "check": "reentrancy-benign",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/CurveFactory.sol#L257-L289",
    "description": "Reentrancy in CurveFactory.createToken(address,string,string,bytes32,string) (src/CurveFactory.sol#257-289):\n\tExternal calls:\n\t- pool = IV3Migrator(migrator).initializePool(token) (src/CurveFactory.sol#277)\n\tState variables written after the call(s):\n\t- curveOf[token] = curve (src/CurveFactory.sol#280)\n\t- isCurve[curve] = true (src/CurveFactory.sol#282)\n\t- tokenOf[curve] = token (src/CurveFactory.sol#281)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "BENIGN (Slither's own classification). State written after a call to a trusted contract (factory/migrator/NPM) with no security relevance and no attacker reentry path (nonReentrant entrypoints; trusted callees; no token hooks). Same trust analysis as reentrancy-no-eth above."
  },
  {
    "id": "262cdd6469287ec62f3cc120a98bccafdbc26e2c57014e8276d375d23f17b9af",
    "check": "reentrancy-benign",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L265-L302",
    "description": "Reentrancy in V3Migrator.migrate(address) (src/V3Migrator.sol#265-302):\n\tExternal calls:\n\t- IWETH9(weth).deposit{value: msg.value}() (src/V3Migrator.sol#284)\n\t- _arbToTarget(c) (src/V3Migrator.sol#294)\n\t\t- IUniswapV3Pool(c.pool).swap(address(this),zeroForOne,_toInt256(budget),c.targetSqrt,) (src/V3Migrator.sol#358)\n\tExternal calls sending eth:\n\t- IWETH9(weth).deposit{value: msg.value}() (src/V3Migrator.sol#284)\n\tState variables written after the call(s):\n\t- _arbToTarget(c) (src/V3Migrator.sol#294)\n\t\t- _activePool = c.pool (src/V3Migrator.sol#312)\n\t\t- _activePool = address(0) (src/V3Migrator.sol#317)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "BENIGN (Slither's own classification). State written after a call to a trusted contract (factory/migrator/NPM) with no security relevance and no attacker reentry path (nonReentrant entrypoints; trusted callees; no token hooks). Same trust analysis as reentrancy-no-eth above."
  },
  {
    "id": "0de965481bd91d0787929c2b9d52ea3354f1fe10c2c0984c514c66cd3148cd5e",
    "check": "reentrancy-benign",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L309-L318",
    "description": "Reentrancy in V3Migrator._arbToTarget(V3Migrator.Ctx) (src/V3Migrator.sol#309-318):\n\tExternal calls:\n\t- wethArbSpent = _arbStep(c,wethArbBudget,wethArbSpent) (src/V3Migrator.sol#315)\n\t\t- IUniswapV3Pool(c.pool).swap(address(this),zeroForOne,_toInt256(budget),c.targetSqrt,) (src/V3Migrator.sol#358)\n\tState variables written after the call(s):\n\t- _activePool = address(0) (src/V3Migrator.sol#317)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "BENIGN (Slither's own classification). State written after a call to a trusted contract (factory/migrator/NPM) with no security relevance and no attacker reentry path (nonReentrant entrypoints; trusted callees; no token hooks). Same trust analysis as reentrancy-no-eth above."
  },
  {
    "id": "0d1552d28f77ef837c6ef44b6cfc6524dcd86bb9dfacaa8f3a9f75aa9587a7a0",
    "check": "reentrancy-events",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/CurveFactory.sol#L257-L289",
    "description": "Reentrancy in CurveFactory.createToken(address,string,string,bytes32,string) (src/CurveFactory.sol#257-289):\n\tExternal calls:\n\t- pool = IV3Migrator(migrator).initializePool(token) (src/CurveFactory.sol#277)\n\tEvent emitted after the call(s):\n\t- TokenCreated(token,curve,creator,name,symbol,metadataHash,metadataUri,pool) (src/CurveFactory.sol#288)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Events (Trade/Graduated/PoolInitialized/FeesCollected) are emitted after an external call to a trusted contract; emission ordering affects only log ordering, never funds or control flow. LPFeeVault.collect emits after NPM.collect (NPM trusted, \u00a712.28)."
  },
  {
    "id": "a8d10b76d529a6eccf01a1c0f0497dc861d42156a7c47386c1ef6bd2ffce3094",
    "check": "reentrancy-events",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/LPFeeVault.sol#L51-L58",
    "description": "Reentrancy in LPFeeVault.collect(uint256) (src/LPFeeVault.sol#51-58):\n\tExternal calls:\n\t- (amount0,amount1) = positionManager.collect(INonfungiblePositionManager.CollectParams({tokenId:tokenId,recipient:treasury,amount0Max:type()(uint128).max,amount1Max:type()(uint128).max})) (src/LPFeeVault.sol#52-56)\n\tEvent emitted after the call(s):\n\t- FeesCollected(tokenId,amount0,amount1) (src/LPFeeVault.sol#57)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Events (Trade/Graduated/PoolInitialized/FeesCollected) are emitted after an external call to a trusted contract; emission ordering affects only log ordering, never funds or control flow. LPFeeVault.collect emits after NPM.collect (NPM trusted, \u00a712.28)."
  },
  {
    "id": "4538c970aca57f9c18578d75375c7f645143937621fbe7632922055943c10fe1",
    "check": "reentrancy-events",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L227-L236",
    "description": "Reentrancy in V3Migrator.initializePool(address) (src/V3Migrator.sol#227-236):\n\tExternal calls:\n\t- pool = INonfungiblePositionManager(positionManager).createAndInitializePoolIfNecessary(token0,token1,FEE_TIER,sqrtP) (src/V3Migrator.sol#233-234)\n\tEvent emitted after the call(s):\n\t- PoolInitialized(token,pool,sqrtP,preExisting) (src/V3Migrator.sol#235)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Events (Trade/Graduated/PoolInitialized/FeesCollected) are emitted after an external call to a trusted contract; emission ordering affects only log ordering, never funds or control flow. LPFeeVault.collect emits after NPM.collect (NPM trusted, \u00a712.28)."
  },
  {
    "id": "8887b7e6115946d8834165eb989b394430fc9e3994dba39160e1d5929ba5ee2c",
    "check": "reentrancy-events",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L265-L302",
    "description": "Reentrancy in V3Migrator.migrate(address) (src/V3Migrator.sol#265-302):\n\tExternal calls:\n\t- IWETH9(weth).deposit{value: msg.value}() (src/V3Migrator.sol#284)\n\t- _arbToTarget(c) (src/V3Migrator.sol#294)\n\t\t- IUniswapV3Pool(c.pool).swap(address(this),zeroForOne,_toInt256(budget),c.targetSqrt,) (src/V3Migrator.sol#358)\n\t- (tokenId,liquidity) = _mintAndSettle(c) (src/V3Migrator.sol#301)\n\t\t- (m.tokenId,m.liquidity,m.used0,m.used1) = INonfungiblePositionManager(positionManager).mint(INonfungiblePositionManager.MintParams({token0:m.token0,token1:m.token1,fee:FEE_TIER,tickLower:TICK_LOWER,tickUpper:TICK_UPPER,amount0Desired:m.amt0Desired,amount1Desired:m.amt1Desired,amount0Min:m.amt0Min,amount1Min:m.amt1Min,recipient:vault,deadline:block.timestamp})) (src/V3Migrator.sol#446-461)\n\tExternal calls sending eth:\n\t- IWETH9(weth).deposit{value: msg.value}() (src/V3Migrator.sol#284)\n\tEvent emitted after the call(s):\n\t- Graduated(c.token,c.pool,m.tokenId,m.liquidity,m.wethInPosition,m.tokensInPosition,c.gradFee,tx.origin,IBondingCurve(msg.sender).CALLER_REWARD(),m.tokensBurned,m.wethDust) (src/V3Migrator.sol#487-499)\n\t\t- (tokenId,liquidity) = _mintAndSettle(c) (src/V3Migrator.sol#301)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Events (Trade/Graduated/PoolInitialized/FeesCollected) are emitted after an external call to a trusted contract; emission ordering affects only log ordering, never funds or control flow. LPFeeVault.collect emits after NPM.collect (NPM trusted, \u00a712.28)."
  },
  {
    "id": "0ec99ce0557cbefb5c81ff87dbae28a1f3dbc35f2614927f828ca0cd51212178",
    "check": "reentrancy-events",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L386-L441",
    "description": "Reentrancy in V3Migrator._mintAndSettle(V3Migrator.Ctx) (src/V3Migrator.sol#386-441):\n\tExternal calls:\n\t- _mint(m) (src/V3Migrator.sol#432)\n\t\t- (m.tokenId,m.liquidity,m.used0,m.used1) = INonfungiblePositionManager(positionManager).mint(INonfungiblePositionManager.MintParams({token0:m.token0,token1:m.token1,fee:FEE_TIER,tickLower:TICK_LOWER,tickUpper:TICK_UPPER,amount0Desired:m.amt0Desired,amount1Desired:m.amt1Desired,amount0Min:m.amt0Min,amount1Min:m.amt1Min,recipient:vault,deadline:block.timestamp})) (src/V3Migrator.sol#446-461)\n\tEvent emitted after the call(s):\n\t- Graduated(c.token,c.pool,m.tokenId,m.liquidity,m.wethInPosition,m.tokensInPosition,c.gradFee,tx.origin,IBondingCurve(msg.sender).CALLER_REWARD(),m.tokensBurned,m.wethDust) (src/V3Migrator.sol#487-499)\n\t\t- _settleDust(c,m) (src/V3Migrator.sol#439)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. Events (Trade/Graduated/PoolInitialized/FeesCollected) are emitted after an external call to a trusted contract; emission ordering affects only log ordering, never funds or control flow. LPFeeVault.collect emits after NPM.collect (NPM trusted, \u00a712.28)."
  },
  {
    "id": "a8d231524d045abd94e91236e44281c1e2872e1a9181287e18038e1a419686d1",
    "check": "reentrancy-no-eth",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/BondingCurve.sol#L174-L267",
    "description": "Reentrancy in BondingCurve.buy(address,address,address,uint256) (src/BondingCurve.sol#174-267):\n\tExternal calls:\n\t- ICurveFactory(factory).recordEthDelta(int256(net)) (src/BondingCurve.sol#239)\n\tState variables written after the call(s):\n\t- _realEthReserves = newRealEth (src/BondingCurve.sol#252)\n\tBondingCurve._realEthReserves (src/BondingCurve.sol#123) can be used in cross function reentrancies:\n\t- BondingCurve.quoteBuy(uint256) (src/BondingCurve.sol#377-409)\n\t- BondingCurve.reserves() (src/BondingCurve.sol#422-429)\n\t- phase = Phase.ReadyToGraduate (src/BondingCurve.sol#257)\n\tBondingCurve.phase (src/BondingCurve.sol#131) can be used in cross function reentrancies:\n\t- BondingCurve.constructor() (src/BondingCurve.sol#138-169)\n\t- BondingCurve.phase (src/BondingCurve.sol#131)\n\t- BondingCurve.quoteBuy(uint256) (src/BondingCurve.sol#377-409)\n\t- BondingCurve.quoteSell(uint256) (src/BondingCurve.sol#412-419)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. The pre-write external call is to the trusted, immutable factory (CurveFactory.createToken\u2192migrator.initializePool; BondingCurve.buy\u2192factory.recordEthDelta). initializePool only touches the V3 factory/pool and recordEthDelta is a bounded accumulator \u2014 neither calls back into the flagged contract. Both entrypoints are nonReentrant (createToken is onlyRouter behind the nonReentrant Router; buy is nonReentrant) so cross-function reentry is impossible. State written after the call is pre-computed before it. (BondingCurve.buy: recordEthDelta is an interaction slightly ahead of the effects block \u2014 acknowledged CEI-ordering nuance, safe because the callee is trusted and cannot reenter.) contracts.md \u00a75.4."
  },
  {
    "id": "5efb23885f171956d8a3daee1894a42e265ad4bb8127a744c19cd56255ecfb99",
    "check": "reentrancy-no-eth",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/CurveFactory.sol#L257-L289",
    "description": "Reentrancy in CurveFactory.createToken(address,string,string,bytes32,string) (src/CurveFactory.sol#257-289):\n\tExternal calls:\n\t- pool = IV3Migrator(migrator).initializePool(token) (src/CurveFactory.sol#277)\n\tState variables written after the call(s):\n\t- ++ tokenCounter (src/CurveFactory.sol#284)\n\tCurveFactory.tokenCounter (src/CurveFactory.sol#149) can be used in cross function reentrancies:\n\t- CurveFactory.createToken(address,string,string,bytes32,string) (src/CurveFactory.sol#257-289)\n\t- CurveFactory.tokenCounter (src/CurveFactory.sol#149)\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. The pre-write external call is to the trusted, immutable factory (CurveFactory.createToken\u2192migrator.initializePool; BondingCurve.buy\u2192factory.recordEthDelta). initializePool only touches the V3 factory/pool and recordEthDelta is a bounded accumulator \u2014 neither calls back into the flagged contract. Both entrypoints are nonReentrant (createToken is onlyRouter behind the nonReentrant Router; buy is nonReentrant) so cross-function reentry is impossible. State written after the call is pre-computed before it. (BondingCurve.buy: recordEthDelta is an interaction slightly ahead of the effects block \u2014 acknowledged CEI-ordering nuance, safe because the callee is trusted and cannot reenter.) contracts.md \u00a75.4."
  },
  {
    "id": "b89a7ba4fdf0578da9218753af3c883a4ce9392af6ad534686f7e4f8fdafd883",
    "check": "timestamp",
    "impact": "Low",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/BondingCurve.sol#L174-L267",
    "description": "BondingCurve.buy(address,address,address,uint256) (src/BondingCurve.sol#174-267) uses timestamp for comparisons\n\tDangerous comparisons:\n\t- block.timestamp < EARLY_WINDOW_END && grossIn > MAX_EARLY_BUY (src/BondingCurve.sol#187)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REQUIRED BY SPEC \u2014 NOT A VULN. BondingCurve.buy compares `block.timestamp < EARLY_WINDOW_END` for the anti-sniper window. Using block.timestamp is MANDATED: block.number is banned on Orbit (spec \u00a72/\u00a76.5, CLAUDE.md hard rule). ~100ms blocks vs a seconds-scale window make sub-second sequencer skew immaterial; the acknowledged bypass is multi-wallet, not timestamp gaming (contracts.md \u00a75.2)."
  },
  {
    "id": "f00e9f510b1f90e41695078266b21c464cf622a29beaecf907c7511367a5238b",
    "check": "uninitialized-local",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/BondingCurve.sol#L199",
    "description": "BondingCurve.buy(address,address,address,uint256).refund (src/BondingCurve.sol#199) is a local variable never initialized\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `Ctx memory c` and `MintLocals memory m` are structs whose fields are all populated before first read; `wethArbSpent` and `refund` are uint256 intentionally starting at 0 (arb accumulator / no-refund default). Standard Slither memory-struct false positive."
  },
  {
    "id": "582e1b0c964f022b9db7e2fddef1b631b72a59e558ed3f61feae72e796f9bcc9",
    "check": "uninitialized-local",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L234",
    "description": "V3Migrator.migrate(address).c (src/V3Migrator.sol#234) is a local variable never initialized\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `Ctx memory c` and `MintLocals memory m` are structs whose fields are all populated before first read; `wethArbSpent` and `refund` are uint256 intentionally starting at 0 (arb accumulator / no-refund default). Standard Slither memory-struct false positive."
  },
  {
    "id": "c076498d1e00e38e02439c08aace65eb176ce60dd44b07873d364b39475551f6",
    "check": "uninitialized-local",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L270",
    "description": "V3Migrator._arbToTarget(V3Migrator.Ctx).wethArbSpent (src/V3Migrator.sol#270) is a local variable never initialized\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `Ctx memory c` and `MintLocals memory m` are structs whose fields are all populated before first read; `wethArbSpent` and `refund` are uint256 intentionally starting at 0 (arb accumulator / no-refund default). Standard Slither memory-struct false positive."
  },
  {
    "id": "d4281e2b54cf993536aec22d96df314236d21b4ec01b6b6044c75b626631472c",
    "check": "uninitialized-local",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L346",
    "description": "V3Migrator._mintAndSettle(V3Migrator.Ctx).m (src/V3Migrator.sol#346) is a local variable never initialized\n",
    "disposition_class": "false-positive",
    "disposition": "FALSE-POSITIVE. `Ctx memory c` and `MintLocals memory m` are structs whose fields are all populated before first read; `wethArbSpent` and `refund` are uint256 intentionally starting at 0 (arb accumulator / no-refund default). Standard Slither memory-struct false positive."
  },
  {
    "id": "2486b3f9231043a0f2742a651b6cb8d9a6835a22f1e9bbfee1d1448bf9d533fd",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/BondingCurve.sol#L343-L372",
    "description": "BondingCurve.graduate() (src/BondingCurve.sol#343-372) ignores return value by IV3Migrator(migrator).migrate{value: ethForMigrator}(token) (src/BondingCurve.sol#370)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "1770621a420aedecf92827f508bde620249462e74e2a9bf934eacd228ceff5e0",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L124-L129",
    "description": "Router._deploy(string,string,bytes32,string) (src/Router.sol#124-129) ignores return value by (token,curve,None) = factoryContract.createToken(msg.sender,name,symbol,metadataHash,metadataUri) (src/Router.sol#128)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "9313fedcf0a4fa7dbb4aa0371132be0ac006588b3340f46779bd5bd6e78a6ebf",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L143-L156",
    "description": "Router.buy(address,address,uint256,uint256) (src/Router.sol#143-156) ignores return value by (tokensOut,None,None) = IBondingCurve(curve).buy{value: msg.value}(msg.sender,recipient,msg.sender,minTokensOut) (src/Router.sol#155)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "ffa2790baf99efaf42459882941ce373dd7ad345e174dddf9db6a9f941b66fd8",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L195-L202",
    "description": "Router.quoteBuy(address,uint256) (src/Router.sol#195-202) ignores return value by IBondingCurve(_curveOf(token)).quoteBuy(ethInGross) (src/Router.sol#201)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "c665005386af8b19db544481bf92310653a4fac1aa274247b1bc9fcd30f1da42",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L205-L212",
    "description": "Router.quoteSell(address,uint256) (src/Router.sol#205-212) ignores return value by IBondingCurve(_curveOf(token)).quoteSell(tokenAmount) (src/Router.sol#211)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "d0dec6ee49ada55c1aa316d8a4db8af5893170a280e7501be16bbe421fe42371",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L219-L227",
    "description": "Router._sell(address,uint256,address,uint256) (src/Router.sol#219-227) ignores return value by (ethOut,None) = IBondingCurve(curve).sell(msg.sender,recipient,tokenAmount,minEthOut) (src/Router.sol#226)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "e11e3bd11654817239db146847ecd8bba720af070fdda95361ecbc7789d844a9",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/Router.sol#L82-L120",
    "description": "Router.createToken(string,string,bytes32,string,uint256,uint256) (src/Router.sol#82-120) ignores return value by (tokensOut,None,None) = IBondingCurve(curve).buy{value: initialBuy}(msg.sender,msg.sender,msg.sender,minTokensOut) (src/Router.sol#117-118)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "27f75e7e770dbff043114bc48c29ec61555bde0474d1a7edb2329a9b6417e50c",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L265-L302",
    "description": "V3Migrator.migrate(address) (src/V3Migrator.sol#265-302) ignores return value by (None,finalTick,None,None,None,None,None) = IUniswapV3Pool(c.pool).slot0() (src/V3Migrator.sol#295)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "8405fb57752fadc8f5476deae22ec509aae8d66e4c58f64066f62d6f52fd0de9",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L321-L324",
    "description": "V3Migrator._withinTolerance(V3Migrator.Ctx) (src/V3Migrator.sol#321-324) ignores return value by (None,curTick,None,None,None,None,None) = IUniswapV3Pool(c.pool).slot0() (src/V3Migrator.sol#322)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "d2576bda67c48de14eb6dbc670d5428c98330b968b93276fb653e93a26bf2487",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) ignores return value by IUniswapV3Pool(c.pool).swap(address(this),zeroForOne,_toInt256(budget),c.targetSqrt,) (src/V3Migrator.sol#358)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  },
  {
    "id": "a78ad71df68a6e5fffdf64a72b930032d6453243ab8526c2bc9a719135c90270",
    "check": "unused-return",
    "impact": "Medium",
    "confidence": "Medium",
    "first_markdown_element": "contracts/src/V3Migrator.sol#L327-L364",
    "description": "V3Migrator._arbStep(V3Migrator.Ctx,uint256,uint256) (src/V3Migrator.sol#327-364) ignores return value by (curSqrt,None,None,None,None,None,None) = IUniswapV3Pool(c.pool).slot0() (src/V3Migrator.sol#328)\n",
    "disposition_class": "acknowledged-by-design",
    "disposition": "REVIEWED \u2014 INTENTIONAL. Router create/buy/sell/quote + _deploy discard tuple fields that are either re-emitted elsewhere (pool addr in factory TokenCreated; acceptedEthGross/fee in curve Trade) or not needed by the deliberately-thin Router; no security decision depends on a dropped value. V3Migrator slot0() partial-destructures to read only sqrtPriceX96/tick (rest of slot0 genuinely unused). V3Migrator pool.swap() return is unneeded \u2014 spend is measured via WETH balance delta and the swap is price-capped to targetSqrt. BondingCurve.graduate discards migrate()'s (tokenId,liquidity); the migrator emits Graduated. contracts.md \u00a72.4/\u00a72.5."
  }
]