/**
 * Next route file for Discover `/` — ROUTING ONLY (FSD Next.js guide). All screen
 * composition + data fetching lives in the `views/discover` slice; this file just
 * re-exports its default so the `app/` directory stays a thin router.
 */
export { default } from "@/views/discover";
