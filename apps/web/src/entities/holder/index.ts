/**
 * Public API for the `holder` entity (FSD reference/public-api). The holder is a
 * domain noun; its funding-cluster grouping + flag-label helpers (pure, §5.2/§8.5)
 * are its model, consumed by the holder-table widget.
 */
export * from "./model/cluster";
