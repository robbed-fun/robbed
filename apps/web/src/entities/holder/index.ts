/**
 * Public API for the `holder` entity (FSD reference/public-api). The holder is a
 * domain noun; its funding-cluster grouping + structural flag-label helpers
 * (pure) are its model, consumed by the holder-table widget.
 */
export * from "./model/cluster";
