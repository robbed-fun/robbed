/**
 * Public API for the Discover screen (FSD `pages` layer, renamed `views` to avoid
 * the Next `pages` collision — feature-sliced.design/docs/guides/tech/with-nextjs).
 * The Next route file (`app/page.tsx`) re-exports this default.
 */
export { default } from "./ui/DiscoverView";
