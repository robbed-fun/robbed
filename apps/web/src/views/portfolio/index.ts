/**
 * Public API for the Portfolio screen (FSD `views` layer). NEW route added by
 * the ROBBED_ redesign (user-directed; overrides §5 "exactly three pages" /
 * §5.4 Phase-2 — deviation recorded for hoodpad-architect §12). Phase F ships
 * the shell only; the Portfolio page agent (Phase P) fills it per the mockup
 * (address header · TOTAL VALUE/LOOT ALL-TIME/WALLET ETH stat cells ·
 * HOLDINGS/ACTIVITY/CREATED tabs · holdings table).
 */
export { default } from "./ui/PortfolioView";
