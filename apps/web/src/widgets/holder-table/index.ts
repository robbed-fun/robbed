/**
 * Public API for the holder-table widget (§5.2). Top-20 holders with structural
 * flags + v1.2 funding-cluster grouping / advisory botFlags (entities/holder).
 */
export { HolderTable } from "./ui/HolderTable";
