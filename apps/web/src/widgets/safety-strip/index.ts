/**
 * Public API for the safety-strip widget (§12.57). The compact relocation of the
 * deleted Trust panel's must-render floor — LP copy, graduation progress, live
 * curve reserves — plus the cheap ownerless/fixed-supply/metadata/fee ticks.
 */
export { SafetyStrip } from "./ui/SafetyStrip";
