/**
 * Public API for the trust-panel widget (§5.2). The first-class trust card that
 * composes live on-chain curve reads (entities/curve) with the indexer's
 * metadata verdict + v1.2 organic metrics.
 */
export { TrustPanel } from "./ui/TrustPanel";
